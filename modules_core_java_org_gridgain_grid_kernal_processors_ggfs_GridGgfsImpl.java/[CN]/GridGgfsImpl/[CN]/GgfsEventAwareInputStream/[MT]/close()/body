{
  if (closeGuard.compareAndSet(false,true)) {
    super.close();
    metrics.decrementFilesOpenedForRead();
    if (evts.isRecordable(EVT_GGFS_FILE_CLOSED_READ))     evts.record(new GridGgfsEvent(path,locNodeId,locNode,EVT_GGFS_FILE_CLOSED_READ,bytes()));
  }
}
