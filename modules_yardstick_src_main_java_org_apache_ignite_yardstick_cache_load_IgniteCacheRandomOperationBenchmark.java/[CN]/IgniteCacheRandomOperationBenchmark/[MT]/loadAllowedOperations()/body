{
  allowedOperations=new ArrayList<>();
  if (args.allowOperations().isEmpty()) {
    for (    Operation op : Operation.values())     allowedOperations.add(op);
  }
 else {
    for (    String opName : args.allowOperations())     allowedOperations.add(Operation.valueOf(opName.toUpperCase()));
  }
}
