{
  String sql=queries.get(nextRandom(queries.size()));
  int count=StringUtils.countOccurrencesOf(sql,"%s");
  Integer[] sub=new Integer[count];
  for (int i=0; i < count; i++)   sub[i]=nextRandom(args.range());
  sql=String.format(sql,sub);
  return sql;
}
