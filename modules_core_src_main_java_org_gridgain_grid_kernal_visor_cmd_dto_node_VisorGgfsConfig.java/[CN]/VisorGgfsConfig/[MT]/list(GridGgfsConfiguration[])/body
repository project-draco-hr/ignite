{
  if (ggfss == null)   return Collections.emptyList();
  final Collection<VisorGgfsConfig> cfgs=new ArrayList<>(ggfss.length);
  for (  GridGgfsConfiguration ggfs : ggfss)   cfgs.add(from(ggfs));
  return cfgs;
}
