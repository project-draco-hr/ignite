{
  while (disc.allNodes().size() < 2)   Thread.sleep(1000);
  return disc.nodes(new GridPredicate<GridNode>(){
    @Override public boolean apply(    GridNode e){
      return !disc.localNode().equals(e);
    }
  }
).iterator().next();
}
