{
  if (log.isDebugEnabled())   log.debug("Completed child execution for node [fut=" + this + ", childNodeId="+ childNodeId+ ", err="+ err+ ']');
  if (err != null)   onFailed(childNodeId,err);
 else {
    childExecs.remove(childNodeId);
    if (childExecs.isEmpty())     onDone();
  }
}
