{
  if (execNodeIds.contains(leftNodeId))   onFailed(leftNodeId,new ClusterTopologyException("Failed to wait for streamer pipeline future completion " + "(execution node has left the grid). All running stages will be cancelled " + "[fut=" + this + ", leftNodeId="+ leftNodeId+ ']'));
}
