{
  if (types == null)   return Collections.emptyList();
  final Collection<VisorCacheTypeMetadata> cfgs=new ArrayList<>(types.size());
  for (  CacheTypeMetadata type : types)   cfgs.add(from(type));
  return cfgs;
}
