{
  Collection<PojoField> res=new ArrayList<>();
  Set<String> desc=tbl.descendingColumns();
  for (  PojoField field : fields)   if (field.use() && desc.contains(field.dbName()))   res.add(field);
  return res;
}
