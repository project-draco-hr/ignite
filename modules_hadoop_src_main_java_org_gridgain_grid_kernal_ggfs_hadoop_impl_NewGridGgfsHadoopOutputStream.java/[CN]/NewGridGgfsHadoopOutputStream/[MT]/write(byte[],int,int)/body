{
  check();
  writeStart();
  try {
    delegate.hadoop().writeData(delegate,b,off,len);
    total+=len;
  }
 catch (  GridException e) {
    throw new IOException(e);
  }
 finally {
    writeEnd();
  }
}
