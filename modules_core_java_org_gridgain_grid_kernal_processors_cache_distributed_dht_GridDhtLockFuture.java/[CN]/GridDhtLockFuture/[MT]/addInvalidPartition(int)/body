{
  invalidParts.add(invalidPart);
  if (tx != null)   tx.addInvalidPartition(invalidPart);
  if (log.isDebugEnabled())   log.debug("Added invalid partition to future [invalidPart=" + invalidPart + ", fut="+ this+ ']');
}
