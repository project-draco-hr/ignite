{
  Object id0=grid(0).localNode().consistentId();
  int port0=getDiscoveryPort(grid(0));
  for (int i=0; i < 10; ++i) {
    stopAllGrids();
    startGrids(1);
    if (port0 == getDiscoveryPort(grid(0)))     assertEquals(id0,grid(0).localNode().consistentId());
  }
}
