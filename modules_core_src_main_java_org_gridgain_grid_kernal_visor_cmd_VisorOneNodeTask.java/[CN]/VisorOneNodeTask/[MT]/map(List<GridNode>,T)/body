{
  for (  GridNode node : subgrid)   if (node.id() == arg.nodeId())   return Collections.singletonMap(job(arg),node);
  throw new GridEmptyProjectionException("Target node to execute Visor job not found in grid [id=" + arg.nodeId() + ", prj="+ subgrid+ "]");
}
