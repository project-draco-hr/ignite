{
  URL hibernateCfg=ExamplesUtils.classLoader().getResource(HIBERNATE_CFG);
  if (hibernateCfg == null)   throw new RuntimeException("Cannot find hibernate configuration file in CLASSPATH: " + HIBERNATE_CFG);
  return hibernateCfg;
}
