{
  ctx.denyOnFlag(READ);
  if (F.isEmpty(keys)) {
    return;
  }
  validateCacheKeys(keys);
  GridCacheVersion obsoleteVer=ctx.versions().next();
  for (  K k : keys)   clear(obsoleteVer,k,filter);
}
