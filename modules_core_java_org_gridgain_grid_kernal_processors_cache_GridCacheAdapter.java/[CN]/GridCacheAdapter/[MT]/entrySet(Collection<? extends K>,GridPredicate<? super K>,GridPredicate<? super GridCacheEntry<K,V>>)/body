{
  if (F.isEmpty(keys))   return emptySet();
  validateCacheKeys(keys);
  return new GridCacheEntrySet<>(ctx,F.<K,GridCacheEntry<K,V>>viewReadOnly(keys,CU.<K,V>cacheKey2Entry(ctx),keyFilter),filter);
}
