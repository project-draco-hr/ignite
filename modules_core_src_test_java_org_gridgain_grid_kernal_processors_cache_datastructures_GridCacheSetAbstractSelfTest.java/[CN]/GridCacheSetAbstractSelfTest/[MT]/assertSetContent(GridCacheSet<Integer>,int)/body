{
  Collection<Integer> data=new HashSet<>(size);
  for (  Integer val : set.iteratorEx())   assertTrue(data.add(val));
  assertEquals(size,data.size());
  for (int val=0; val < size; val++)   assertTrue(data.contains(val));
}
