{
  if (closeGuard.compareAndSet(false,true)) {
    super.onClose();
    metrics.decrementFilesOpenedForWrite();
    if (evts.isRecordable(EVT_GGFS_FILE_CLOSED_WRITE))     evts.record(new IgniteFsEvent(path,localNode(),EVT_GGFS_FILE_CLOSED_WRITE,bytes()));
  }
}
