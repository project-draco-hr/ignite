{
  super.setUp(cfg);
  jcommander(cfg.commandLineArguments(),args,"<ignite-driver>");
  if (Ignition.state() != IgniteState.STARTED) {
    node=new IgniteNode(args.isClientOnly());
    node.start(cfg);
  }
 else   node=new IgniteNode(args.isClientOnly(),Ignition.ignite());
  waitForNodes();
}
