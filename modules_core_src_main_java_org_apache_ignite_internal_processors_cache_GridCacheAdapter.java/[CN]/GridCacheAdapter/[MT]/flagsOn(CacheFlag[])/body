{
  if (F.isEmpty(flags))   return this;
  GridCacheProjectionImpl<K,V> prj=new GridCacheProjectionImpl<>(this,ctx,null,EnumSet.copyOf(F.asList(flags)),null,false,null);
  return new GridCacheProxyImpl<>(ctx,prj,prj);
}
