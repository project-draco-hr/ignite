{
  boolean removed=map.removeEntry(entry);
  if (log.isDebugEnabled()) {
    if (removed)     log.debug("Removed entry from cache: " + entry);
 else     log.debug("Remove will not be done for key (entry got replaced or removed): " + entry.key());
  }
}
