{
  Map<ComputeJob,ClusterNode> jobs=new HashMap();
  for (  ClusterNode node : subgrid) {
    jobs.put(keys == null ? new GlobalClearAllJob(cacheName,topVer) : new GlobalClearKeySetJob<K>(cacheName,topVer,keys),node);
  }
  return jobs;
}
