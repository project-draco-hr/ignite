{
  writer.setBuffer(buf);
  if (!super.writeTo(buf,writer))   return false;
  if (!writer.isTypeWritten()) {
    if (!writer.writeMessageType(directType()))     return false;
    writer.onTypeWritten();
  }
switch (writer.state()) {
case 0:
    if (!writer.writeField("order",order,MessageFieldType.LONG))     return false;
  writer.incrementState();
case 1:
if (!writer.writeField("res",res,MessageFieldType.BOOLEAN)) return false;
writer.incrementState();
}
return true;
}
