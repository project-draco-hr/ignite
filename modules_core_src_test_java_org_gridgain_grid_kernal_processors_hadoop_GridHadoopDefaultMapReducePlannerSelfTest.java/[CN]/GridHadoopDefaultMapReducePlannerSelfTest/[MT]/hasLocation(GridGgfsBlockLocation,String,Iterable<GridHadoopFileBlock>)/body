{
  for (  GridHadoopFileBlock block : mappers) {
    if (block.file().getPath().equals(name) && loc.start() == block.start() && loc.length() == block.length())     return true;
  }
  return false;
}
