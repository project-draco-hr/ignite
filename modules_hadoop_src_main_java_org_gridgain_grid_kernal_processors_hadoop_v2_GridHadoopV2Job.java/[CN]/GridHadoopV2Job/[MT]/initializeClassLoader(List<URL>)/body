{
  if (!jobLocalDir.exists())   return;
  URL[] urls=new URL[classPath.size()];
  classPath.toArray(urls);
  final URLClassLoader urlLdr=new URLClassLoader(urls);
  jobLdr=new ClassLoaderWrapper(urlLdr,getClass().getClassLoader());
}
