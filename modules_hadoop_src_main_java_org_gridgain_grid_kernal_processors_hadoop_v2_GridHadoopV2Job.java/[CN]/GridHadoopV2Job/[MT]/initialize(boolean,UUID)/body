{
  jobJarsDir=new File(new File(U.resolveWorkDirectory("hadoop",false),"Job_" + jobId),"jars-" + locNodeId);
  if (!external)   prepareJobFiles();
  initializeClassLoader();
  if (jobLdr != null)   ctx.getJobConf().setClassLoader(jobLdr);
}
