{
  Class<?> res=ctx.getJobConf().getCombinerClass();
  try {
    if (res == null)     res=ctx.getCombinerClass();
    return res;
  }
 catch (  ClassNotFoundException e) {
    throw new GridRuntimeException(e);
  }
}
