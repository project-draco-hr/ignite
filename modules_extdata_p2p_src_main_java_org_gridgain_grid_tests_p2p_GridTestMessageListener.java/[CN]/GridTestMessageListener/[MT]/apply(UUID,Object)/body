{
  grid.log().info("Received message [nodeId=" + nodeId + ", locNodeId="+ grid.localNode().id()+ ", msg="+ msg+ ']');
  Integer cnt=grid.<String,Integer>nodeLocalMap().get("msgCnt");
  if (cnt == null)   cnt=0;
  grid.<String,Integer>nodeLocalMap().put("msgCnt",cnt + 1);
  return true;
}
