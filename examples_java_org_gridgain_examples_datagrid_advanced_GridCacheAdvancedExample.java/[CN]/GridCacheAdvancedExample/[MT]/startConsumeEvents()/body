{
  GridGain.grid().events().remoteListen(null,new GridPredicate<GridCacheEvent>(){
    @Override public boolean apply(    GridCacheEvent e){
      System.out.println(e.shortDisplay());
      return true;
    }
  }
,EVT_CACHE_OBJECT_PUT,EVT_CACHE_OBJECT_READ,EVT_CACHE_OBJECT_REMOVED).get();
}
