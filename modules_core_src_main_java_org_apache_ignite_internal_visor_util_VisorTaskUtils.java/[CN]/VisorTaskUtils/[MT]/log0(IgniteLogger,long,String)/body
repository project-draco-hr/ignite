{
  if (log != null) {
    if (log.isDebugEnabled())     log.debug(msg);
 else     log.warning(msg);
  }
 else   X.println("[" + DEBUG_DATE_FMT.get().format(time) + "]"+ String.format("%30s %s","<" + Thread.currentThread().getName() + ">",msg));
}
