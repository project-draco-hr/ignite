{
  removeListeners(ignite1);
  removeListeners(ignite2);
  lsnrs.clear();
  for (int i=1; i <= 3; i++) {
    jcache(i).clear();
    assertTrue("Cache isn't empty [i=" + i + ", entries="+ ((IgniteKernal)grid(i)).internalCache().entries()+ "]",jcache(i).localSize() == 0);
  }
}
