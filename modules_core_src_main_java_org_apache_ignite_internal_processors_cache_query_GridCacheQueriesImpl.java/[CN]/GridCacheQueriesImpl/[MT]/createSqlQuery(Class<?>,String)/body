{
  A.notNull(cls,"cls");
  A.notNull(clause,"clause");
  return new GridCacheQueryAdapter<>(ctx,SQL,ctx.kernalContext().query().typeName(U.box(cls)),clause,null,false,prj != null && prj.isKeepPortable());
}
