{
  try {
    return ggfs.globalSpace();
  }
 catch (  IllegalStateException e) {
    throw new IgfsHadoopCommunicationException("Failed to get file system status because Grid is " + "stopping.");
  }
}
