{
  return new JdbcResultSet(conn.createStatement0(),Collections.<String>emptyList(),Arrays.asList("TABLE_CAT"),Arrays.<String>asList(String.class.getName()),Collections.<List<Object>>emptyList());
}
