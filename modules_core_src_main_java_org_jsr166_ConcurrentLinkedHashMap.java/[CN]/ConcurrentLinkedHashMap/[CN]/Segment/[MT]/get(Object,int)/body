{
  if (cnt != 0) {
    HashEntry<K,V> e=getFirst(hash);
    while (e != null) {
      if (e.hash == hash && key.equals(e.key)) {
        V v=e.val;
        if (v != null)         return v;
        v=readValueUnderLock(e);
        return v;
      }
      e=e.next;
    }
  }
  return null;
}
