{
  if (cnt != 0) {
    writeLock().lock();
    try {
      HashEntry<K,V>[] tab=tbl;
      for (int i=0; i < tab.length; i++)       tab[i]=null;
      ++modCnt;
      cnt=0;
    }
  finally {
      writeLock().unlock();
    }
  }
}
