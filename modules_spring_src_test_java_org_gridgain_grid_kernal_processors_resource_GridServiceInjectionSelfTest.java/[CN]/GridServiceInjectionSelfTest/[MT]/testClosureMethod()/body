{
  grid(0).compute().call(new GridCallable<Object>(){
    @GridServiceResource(serviceName="testService") private void service(    DummyService srvc){
      assertNotNull(srvc);
    }
    @Override public Object call() throws Exception {
      return null;
    }
  }
);
}
