{
  grid(0).compute().call(new GridCallable<Object>(){
    @GridServiceResource(serviceName="nonExistentService") private DummyService srvc;
    @Override public Object call() throws Exception {
      assertNull(srvc);
      return null;
    }
  }
);
}
