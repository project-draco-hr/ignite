{
  grid(0).compute().call(new GridCallable<Object>(){
    @GridServiceResource(serviceName="nonExistentService") private void service(    DummyService srvc){
      assertNull(srvc);
    }
    @Override public Object call() throws Exception {
      return null;
    }
  }
).get();
}
