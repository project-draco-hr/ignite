{
  grid(0).compute().call(new GridCallable<Object>(){
    @GridServiceResource(serviceName="testService") private DummyService srvc;
    @Override public Object call() throws Exception {
      assertNotNull(srvc);
      return null;
    }
  }
).get();
}
