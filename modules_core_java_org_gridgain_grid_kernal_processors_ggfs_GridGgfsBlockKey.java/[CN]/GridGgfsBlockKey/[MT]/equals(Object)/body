{
  if (o == this)   return true;
  if (o == null || o.getClass() != getClass())   return false;
  GridGgfsBlockKey that=(GridGgfsBlockKey)o;
  return blockId == that.blockId && fileId.equals(that.fileId) && F.eq(affKey,that.affKey) && evictExclude == that.evictExclude;
}
