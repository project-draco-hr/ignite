{
  super.initialize(uri,conf);
  setConf(conf);
  String initWorkDir=conf.get(GridHadoopFileSystemsUtils.LOC_FS_WORK_DIR_PROP);
  if (initWorkDir != null)   setWorkingDirectory(new Path(initWorkDir));
}
