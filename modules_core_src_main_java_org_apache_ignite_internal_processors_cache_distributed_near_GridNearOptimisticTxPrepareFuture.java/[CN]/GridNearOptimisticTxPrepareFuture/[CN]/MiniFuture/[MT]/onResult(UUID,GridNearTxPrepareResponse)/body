{
  if (isDone())   return;
  if (rcvRes.compareAndSet(false,true)) {
    if (res.error() != null) {
      onError(nodeId,mappings,res.error());
    }
 else {
      onPrepareResponse(m,res);
      if (mappings != null)       proceedPrepare(mappings);
      onDone(tx);
    }
  }
}
