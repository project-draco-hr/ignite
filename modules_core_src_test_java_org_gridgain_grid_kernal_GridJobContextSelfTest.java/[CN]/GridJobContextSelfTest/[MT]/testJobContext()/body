{
  Grid grid=startGrid(1);
  try {
    startGrid(2);
    try {
      grid.compute().execute(JobContextTask.class,null);
    }
  finally {
      stopGrid(2);
    }
  }
  finally {
    stopGrid(1);
  }
}
