{
  Map<Object,IgniteBiTuple<byte[],GridCacheVersion>> entries0=entries;
  if (entries0 == null) {
synchronized (this) {
      entries0=entries;
      if (entries0 == null)       entries0=entries=new ConcurrentHashMap8<>();
    }
  }
  entries0.put(key,new IgniteBiTuple<>(keyBytes,ver));
}
