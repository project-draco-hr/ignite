{
  if (target == null || keys == null)   return null;
  if (keys.contains(target))   return target;
  for (  Object key : keys) {
    Class<?> clazz=(Class<?>)key;
    if (clazz.isAssignableFrom(target))     return key;
  }
  return null;
}
