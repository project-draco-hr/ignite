{
  if (target == null || keys == null)   return null;
  if (target.getSimpleName().startsWith("Visor") || GridCacheSqlMetadata.class.isAssignableFrom(target) || GridCacheSqlIndexMetadata.class.isAssignableFrom(target))   return LessNamingProcessor.class;
  if (keys.contains(target))   return target;
  for (  Object key : keys) {
    Class<?> clazz=(Class<?>)key;
    if (clazz.isAssignableFrom(target))     return key;
  }
  return null;
}
