{
  Collection<PortableMetadata> metas=cacheObjProc.metadata();
  writer.writeInt(metas.size());
  for (  org.apache.ignite.portable.PortableMetadata m : metas)   writeMetadata0(writer,cacheObjProc.typeId(m.typeName()),m);
}
