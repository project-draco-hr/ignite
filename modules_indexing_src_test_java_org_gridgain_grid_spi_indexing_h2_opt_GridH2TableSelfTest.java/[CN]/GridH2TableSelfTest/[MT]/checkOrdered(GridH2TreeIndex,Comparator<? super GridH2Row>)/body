{
  Iterator<GridH2Row> rows=idx.rows();
  GridH2Row min=null;
  while (rows.hasNext()) {
    GridH2Row row=rows.next();
    assertNotNull(row);
    assertFalse("Incorrect row order in index: " + idx + "\n min: "+ min+ "\n row: "+ row,min != null && cmp.compare(min,row) > 0);
    min=row;
  }
}
