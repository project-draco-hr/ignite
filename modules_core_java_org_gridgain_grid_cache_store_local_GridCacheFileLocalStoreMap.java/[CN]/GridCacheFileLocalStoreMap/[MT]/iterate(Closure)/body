{
  for (int i=0; i < cap; i++) {
    long off=offset(i,0,cap);
    int key=key(data,off);
    if (key != 0) {
      long val=value(data,off);
      c.apply(val);
    }
  }
  if (next != null)   next.iterate(c);
}
