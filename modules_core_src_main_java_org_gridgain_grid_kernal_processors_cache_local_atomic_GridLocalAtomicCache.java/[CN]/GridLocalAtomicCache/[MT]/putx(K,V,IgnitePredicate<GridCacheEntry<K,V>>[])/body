{
  A.notNull(key,"key",val,"val");
  ctx.denyOnLocalRead();
  return (Boolean)updateAllInternal(UPDATE,Collections.singleton(key),Collections.singleton(val),expiryPerCall(),false,false,filter,ctx.isStoreEnabled());
}
