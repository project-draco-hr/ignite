{
  boolean statsEnabled=ctx.config().isStatisticsEnabled();
  long start=statsEnabled ? System.nanoTime() : 0L;
  A.notNull(key,"key");
  ctx.denyOnLocalRead();
  Boolean removed=(Boolean)updateAllInternal(DELETE,Collections.singleton(key),null,null,expiryPerCall(),false,false,filter,ctx.isStoreEnabled());
  if (statsEnabled && removed)   metrics0().addRemoveTimeNanos(System.nanoTime() - start);
  return removed;
}
