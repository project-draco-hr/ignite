{
  for (  Ignite g : grids) {
    GridAffinityAssignmentCache affCache=getFieldValue(((IgniteKernal)g).internalCache(PARTITIONED_CACHE_NAME).context().affinity(),"aff");
    CacheAffinityFunction aff=getFieldValue(affCache,"aff");
    info("Affinity [nodeId=" + g.cluster().localNode().id() + ", affinity="+ aff+ "]");
  }
}
