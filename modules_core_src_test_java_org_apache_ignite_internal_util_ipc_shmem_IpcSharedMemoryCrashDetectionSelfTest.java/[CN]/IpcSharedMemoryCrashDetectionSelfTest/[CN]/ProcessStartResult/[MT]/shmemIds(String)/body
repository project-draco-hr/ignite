{
  if (shmemIds == null)   this.shmemIds=null;
 else {
    String[] tokens=shmemIds.split(",");
    this.shmemIds=new ArrayList<>(tokens.length);
    for (    String token : tokens)     this.shmemIds.add(Long.valueOf(token).intValue());
  }
}
