{
  CacheProjection<?,?> prj=cache(g,cacheName).forSubjectId(clientId).setSkipStore(skipStore);
  return op.apply((CacheProjection<Object,Object>)prj,((IgniteKernal)g).context()).chain(resultWrapper((CacheProjection<Object,Object>)prj,key)).get();
}
