{
  if (last == null)   last=new HashSet<>();
  if (last.add(p)) {
    msgSize+=4;
    Collection<byte[]> serInfo=infoBytesMap.get(p);
    if (serInfo == null)     infoBytesMap.put(p,new LinkedList<byte[]>());
  }
}
