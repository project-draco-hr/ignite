{
  for (  ClassWithAnnotation aClass : fieldCache.keySet()) {
    if (aClass.getKey().getName().equals(clsName))     return true;
  }
  for (  ClassWithAnnotation aClass : mtdCache.keySet()) {
    if (aClass.getKey().getName().equals(clsName))     return true;
  }
  return false;
}
