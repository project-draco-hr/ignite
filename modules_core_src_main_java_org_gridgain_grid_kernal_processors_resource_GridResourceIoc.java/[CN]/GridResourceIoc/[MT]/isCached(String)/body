{
  for (  GridResourceClass aClass : fieldCache.keySet()) {
    if (aClass.getClass().getName().equals(clsName))     return true;
  }
  for (  GridResourceClass aClass : mtdCache.keySet()) {
    if (aClass.getClass().getName().equals(clsName))     return true;
  }
  return false;
}
