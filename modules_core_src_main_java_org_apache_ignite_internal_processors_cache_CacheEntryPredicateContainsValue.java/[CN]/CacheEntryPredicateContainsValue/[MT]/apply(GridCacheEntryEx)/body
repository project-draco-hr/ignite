{
  try {
    CacheObject val=entry.rawGetOrUnmarshal(true);
    return F.eq(this.val.value(entry.context().cacheObjectContext(),false),CU.value(val,entry.context(),false));
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException(e);
  }
}
