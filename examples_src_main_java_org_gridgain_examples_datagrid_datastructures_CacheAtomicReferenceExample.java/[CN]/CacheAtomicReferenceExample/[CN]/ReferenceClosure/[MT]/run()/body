{
  try {
    GridCacheAtomicReference<String> ref=Ignition.grid().cache(cacheName).dataStructures().atomicReference(refName,null,true);
    System.out.println("Atomic reference value is " + ref.get() + '.');
  }
 catch (  GridException e) {
    throw new GridRuntimeException(e);
  }
}
