{
  stopping=true;
  IgniteCheckedException err=new IgniteCheckedException("Failed to wait for topology update, node is stopping.");
  for (  AffinityReadyFuture fut : readyFuts.values())   fut.onDone(err);
}
