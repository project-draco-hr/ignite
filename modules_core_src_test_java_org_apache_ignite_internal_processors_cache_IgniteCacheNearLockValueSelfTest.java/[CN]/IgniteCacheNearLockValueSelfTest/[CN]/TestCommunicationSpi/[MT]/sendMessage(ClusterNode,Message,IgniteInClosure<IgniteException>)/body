{
  if (msg instanceof GridIoMessage) {
    GridIoMessage ioMsg=(GridIoMessage)msg;
    if (ioMsg.message() instanceof GridNearLockRequest)     reqs.add((GridNearLockRequest)ioMsg.message());
  }
  super.sendMessage(node,msg,ackC);
}
