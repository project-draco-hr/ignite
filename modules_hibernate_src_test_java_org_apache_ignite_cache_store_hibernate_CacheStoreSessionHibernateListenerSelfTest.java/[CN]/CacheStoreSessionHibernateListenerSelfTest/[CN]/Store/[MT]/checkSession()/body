{
  Session hibSes=session();
  assertNotNull(hibSes);
  assertTrue(hibSes.isOpen());
  if (ses.isWithinTransaction())   assertNotNull(hibSes.getTransaction());
 else   assertNull(hibSes.getTransaction());
  verifySameInstance(hibSes);
}
