{
  for (  GridStreamerStage s : c.getStages())   ctx.resource().injectGeneric(s);
  if (router == null)   router=new GridStreamerLocalEventRouter();
  ctx.resource().injectGeneric(router);
  for (  GridStreamerWindow w : c.getWindows())   ctx.resource().injectGeneric(w);
}
