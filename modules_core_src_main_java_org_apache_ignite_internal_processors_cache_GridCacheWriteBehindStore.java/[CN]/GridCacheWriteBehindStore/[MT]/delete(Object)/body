{
  try {
    if (log.isDebugEnabled())     log.debug("Store remove [key=" + key + ']');
    updateCache((K)key,null,StoreOperation.RMV);
  }
 catch (  IgniteInterruptedException e) {
    throw new CacheWriterException(e);
  }
}
