{
  A.notNull(key,"key");
  return withReconnectHandling(new ClientProjectionClosure<GridPortableObject>(){
    @Override public GridClientFuture<GridPortableObject> apply(    GridClientConnection conn,    UUID destNodeId) throws GridClientConnectionResetException, GridClientClosedException {
      return conn.cacheGet(cacheName,key,flags,destNodeId);
    }
  }
,cacheName,key);
}
