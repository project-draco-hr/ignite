{
synchronized (this) {
    if (closed)     return;
    closed=true;
    notifyAll();
  }
  executor.shutdown();
  for (  GridTuple3<InetSocketAddress,Socket,Exception> tuple : queue)   U.closeQuiet(tuple.get2());
}
