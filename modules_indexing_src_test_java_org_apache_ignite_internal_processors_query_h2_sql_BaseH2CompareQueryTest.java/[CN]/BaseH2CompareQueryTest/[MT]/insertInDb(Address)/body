{
  try (PreparedStatement st=conn.prepareStatement("insert into \"repl\".ADDRESS (_key, _val, id, street) values(?, ?, ?, ?)")){
    st.setObject(1,a.id);
    st.setObject(2,a);
    st.setObject(3,a.id);
    st.setObject(4,a.street);
    st.executeUpdate();
  }
 }
