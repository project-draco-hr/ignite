{
  System.out.println();
  System.out.println(">>> Lifecycle event occurred: " + evt);
  System.out.println(">>> Ignite name: " + ignite.name());
  if (evt == AFTER_GRID_START)   isStarted=true;
 else   if (evt == AFTER_GRID_STOP)   isStarted=false;
}
