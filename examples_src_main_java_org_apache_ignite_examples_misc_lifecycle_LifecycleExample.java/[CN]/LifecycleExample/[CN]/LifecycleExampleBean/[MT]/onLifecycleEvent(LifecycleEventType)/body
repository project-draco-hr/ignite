{
  System.out.println();
  System.out.println(">>> Grid lifecycle event occurred: " + evt);
  System.out.println(">>> Grid name: " + ignite.name());
  if (evt == AFTER_IGNITE_START)   isStarted=true;
 else   if (evt == AFTER_IGNITE_STOP)   isStarted=false;
}
