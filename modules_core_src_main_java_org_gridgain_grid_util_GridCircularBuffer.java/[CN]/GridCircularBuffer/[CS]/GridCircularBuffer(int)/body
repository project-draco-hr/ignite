{
  A.ensure(size > 0,"Size should be greater than 0: " + size);
  A.ensure((size & (size - 1)) == 0,"Size should be power of two: " + size);
  sizeMask=size - 1;
  arr=(Item<T>[])new Item[size];
  for (int i=0; i < arr.length; i++)   arr[i]=new Item<>();
}
