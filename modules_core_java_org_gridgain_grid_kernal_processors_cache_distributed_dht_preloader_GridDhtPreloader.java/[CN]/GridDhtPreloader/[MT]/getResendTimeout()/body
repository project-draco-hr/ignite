{
  try {
    return Long.parseLong(X.getSystemOrEnv(GridSystemProperties.GG_PRELOAD_RESEND_TIMEOUT,String.valueOf(DFLT_PRELOAD_RESEND_TIMEOUT)));
  }
 catch (  NumberFormatException ignored) {
    return DFLT_PRELOAD_RESEND_TIMEOUT;
  }
}
