{
  try {
    outBuffer.reset();
    serializer.open(outBuffer);
    serializer.serialize((T)obj);
    serializer.close();
    outBuffer.writeTo(out);
  }
 catch (  IOException e) {
    throw new GridException(e);
  }
}
