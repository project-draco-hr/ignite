{
  String schema=schema(ccfg.getName());
  createSchema(schema);
  schemas.put(schema,new Schema(schema,ccfg.getOffHeapMaxMemory() >= 0 ? new GridUnsafeMemory(0) : null,ccfg.getSqlOnheapRowCacheSize()));
  createSqlFunctions(schema,ccfg.getSqlFunctionClasses());
}
