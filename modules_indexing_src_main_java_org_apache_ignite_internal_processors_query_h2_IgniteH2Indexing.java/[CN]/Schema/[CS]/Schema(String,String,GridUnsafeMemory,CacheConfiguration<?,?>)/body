{
  this.spaceName=spaceName;
  this.schemaName=schemaName;
  this.offheap=offheap;
  this.ccfg=ccfg;
  if (offheap != null)   rowCache=new CacheLongKeyLIRS<>(ccfg.getSqlOnheapRowCacheSize(),1,128,256);
 else   rowCache=null;
}
