{
  if (coctx != null && coctx.processor().isPortableObject(obj))   return Utils.serialize(obj,null);
  try {
    return obj.valueBytes(coctx);
  }
 catch (  IgniteCheckedException e) {
    throw DbException.convert(e);
  }
}
