{
  return PREFIX + " [" + (discoOrdered ? "ver=" + topSnap.get().topVer.topologyVersion() + ", " : "")+ "server nodes="+ srvNodesNum+ ", client nodes="+ clientNodesNum+ ", CPUs="+ totalCpus+ ", heap="+ heap+ "GB"+ ']';
}
