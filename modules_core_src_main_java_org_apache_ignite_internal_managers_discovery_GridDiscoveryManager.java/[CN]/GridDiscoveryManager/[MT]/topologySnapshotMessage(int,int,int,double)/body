{
  return PREFIX + " [" + (discoOrdered ? "ver=" + topSnap.get().topVer.topologyVersion() + ", " : "")+ "server nodes="+ serverNodesNum+ ", client nodes="+ clientNodesNum+ ", CPUs="+ totalCpus+ ", heap="+ heap+ "GB"+ ']';
}
