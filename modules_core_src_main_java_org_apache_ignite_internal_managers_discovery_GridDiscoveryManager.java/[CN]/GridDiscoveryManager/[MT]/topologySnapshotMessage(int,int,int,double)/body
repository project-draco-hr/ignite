{
  return PREFIX + " [" + (discoOrdered ? "ver=" + topSnap.get().topVer.topologyVersion() + ", " : "")+ "servers="+ srvNodesNum+ ", clients="+ clientNodesNum+ ", CPUs="+ totalCpus+ ", heap="+ heap+ "GB]";
}
