{
  while (true) {
    AffinityTopologyVersion cur=topVer.get();
    if (updated.compareTo(cur) > 0) {
      if (topVer.compareAndSet(cur,updated))       return true;
    }
 else     return false;
  }
}
