{
  if (nodes == null)   return Collections.emptyList();
  return nodes.isEmpty() || topVer < 0 || topVer >= maxOrder ? nodes : F.view(nodes,new P1<ClusterNode>(){
    @Override public boolean apply(    ClusterNode node){
      return node.order() <= topVer;
    }
  }
);
}
