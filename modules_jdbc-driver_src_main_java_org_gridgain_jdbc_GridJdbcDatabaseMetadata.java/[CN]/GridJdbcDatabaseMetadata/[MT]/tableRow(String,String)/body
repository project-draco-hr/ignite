{
  List<Object> row=new ArrayList<>(10);
  row.add((String)null);
  row.add(schema);
  row.add(tbl.toUpperCase());
  row.add("TABLE");
  row.add((String)null);
  row.add((String)null);
  row.add((String)null);
  row.add((String)null);
  row.add((String)null);
  row.add((String)null);
  return row;
}
