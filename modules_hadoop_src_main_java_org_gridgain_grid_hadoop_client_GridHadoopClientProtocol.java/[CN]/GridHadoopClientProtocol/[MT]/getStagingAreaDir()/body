{
  String usr=UserGroupInformation.getCurrentUser().getShortUserName();
  return new SB(conf.get(PROP_STAGING_DIR,DFLT_STAGING_DIR)).a(Path.SEPARATOR).a(usr).a(Path.SEPARATOR).a(STAGING_DIR_CONST).toString();
}
