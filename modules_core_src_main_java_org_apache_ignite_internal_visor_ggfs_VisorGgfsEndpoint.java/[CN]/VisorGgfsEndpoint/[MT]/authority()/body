{
  String addr=hostName + ":" + port;
  if (ggfsName == null && gridName == null)   return addr;
 else   if (ggfsName == null)   return gridName + "@" + addr;
 else   if (gridName == null)   return ggfsName + "@" + addr;
 else   return ggfsName + ":" + gridName+ "@"+ addr;
}
