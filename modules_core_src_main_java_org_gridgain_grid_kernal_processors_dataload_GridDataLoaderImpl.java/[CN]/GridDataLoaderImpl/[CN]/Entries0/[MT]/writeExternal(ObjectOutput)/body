{
  out.writeInt(delegate.size());
  for (  Map.Entry<K,V> entry : delegate) {
    out.writeObject(entry.getKey());
    if (portable != null)     out.writeObject(portable.marshalToPortable(entry.getValue()));
 else     out.writeObject(entry.getValue());
  }
}
