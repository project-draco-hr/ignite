{
  int sz=in.readInt();
  if (sz > 0 && delegate instanceof ArrayList)   ((ArrayList)delegate).ensureCapacity(sz);
  for (int i=0; i < sz; i++)   add(new Entry0<>((K)in.readObject(),(V)in.readObject()));
}
