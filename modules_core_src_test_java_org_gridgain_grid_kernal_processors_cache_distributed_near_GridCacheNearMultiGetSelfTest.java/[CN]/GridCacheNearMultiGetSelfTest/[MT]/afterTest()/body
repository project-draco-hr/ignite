{
  for (int i=0; i < GRID_CNT; i++) {
    Grid g=grid(i);
    GridCache<Integer,String> c=g.cache(null);
    c.removeAll();
    assertEquals("Cache size mismatch for grid [grid=" + g.name() + ", entrySet="+ c.entrySet()+ ']',0,c.size());
  }
}
