{
  for (  Class<?> aClass : fieldCache.keySet()) {
    if (aClass.getName().equals(clsName))     return true;
  }
  for (  Class<?> aClass : mtdCache.keySet()) {
    if (aClass.getName().equals(clsName))     return true;
  }
  return false;
}
