{
  if (tx == null)   return;
  txs.add(tx);
  GridCacheTxEx tx0=(GridCacheTxEx)tx;
  if (!tx0.local())   throw new IgniteCheckedException("Tx is not local: " + tx);
  if (tx0.dht())   throw new IgniteCheckedException("Tx is DHT: " + tx);
}
