{
  if (tx == null)   return;
  txs.add(tx);
  GridCacheTxEx tx0=(GridCacheTxEx)tx;
  if (!tx0.local())   throw new GridException("Tx is not local: " + tx);
  if (tx0.dht())   throw new GridException("Tx is DHT: " + tx);
}
