{
  checkTx(session());
  lastMtd="putAll";
  checkOperation();
  for (  Cache.Entry<? extends Integer,? extends String> e : entries)   this.map.put(e.getKey(),e.getValue());
  putAllCnt.incrementAndGet();
}
