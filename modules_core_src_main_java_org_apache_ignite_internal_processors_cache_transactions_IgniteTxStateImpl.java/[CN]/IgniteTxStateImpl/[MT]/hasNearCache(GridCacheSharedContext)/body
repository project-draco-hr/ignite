{
  for (int i=0; i < activeCacheIds.size(); i++) {
    int cacheId=(int)activeCacheIds.get(i);
    GridCacheContext cacheCtx=cctx.cacheContext(cacheId);
    if (cacheCtx.isNear())     return true;
  }
  return false;
}
