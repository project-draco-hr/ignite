{
  for (int i=0; i < activeCacheIds.size(); i++) {
    int cacheId=(int)activeCacheIds.get(i);
    if (cctx.cacheContext(cacheId).config().getWriteSynchronizationMode() == FULL_SYNC)     return true;
  }
  return false;
}
