{
  int size0=size;
  size+=bytes;
  if (size > arr.length) {
    byte[] arr0=new byte[size << 1];
    UNSAFE.copyMemory(arr,BYTE_ARR_OFF,arr0,BYTE_ARR_OFF,size0);
    arr=arr0;
  }
  return size0;
}
