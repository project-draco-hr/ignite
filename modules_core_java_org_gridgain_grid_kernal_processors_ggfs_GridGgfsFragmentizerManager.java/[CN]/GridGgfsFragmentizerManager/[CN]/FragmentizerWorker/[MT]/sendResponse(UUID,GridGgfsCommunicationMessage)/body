{
  try {
    sendWithRetries(nodeId,msg);
  }
 catch (  GridException e) {
    if (e.hasCause(GridTopologyException.class)) {
      if (log.isDebugEnabled())       log.debug("Failed to send sync response to GGFS fragmentizer coordinator " + "(originating node left the grid): " + nodeId);
    }
 else     log.error("Failed to send sync response to GGFS fragmentizer coordinator: " + nodeId,e);
  }
}
