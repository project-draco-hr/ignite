{
  if (!fullSup)   throw new IllegalStateException("Sessions attributes and checkpoints are disabled by default " + "for better performance (to enable, annotate task class with " + "@GridComputeTaskSessionFullSupport annotation).");
}
