{
  if (val != null) {
    stream.writeShortArray(val);
    return stream.lastFinished();
  }
 else {
    if (stream.remaining() < 4)     return false;
    stream.writeInt(-1);
    return true;
  }
}
