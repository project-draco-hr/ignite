{
  if (val != null) {
    stream.writeDoubleArray(val);
    return stream.lastFinished();
  }
 else {
    if (stream.remaining() < 4)     return false;
    stream.writeInt(-1);
    return true;
  }
}
