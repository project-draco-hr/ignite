{
  if (failedKeys == null)   failedKeys=new ArrayList<>(keys.size());
  failedKeys.addAll(keys);
  if (err == null)   err=new GridException("Failed to update keys on primary node.");
  err.addSuppressed(e);
}
