{
  if (cctx.portableEnabled()) {
    GridPortableObject obj=(GridPortableObject)cctx.portable().unmarshal(bytes);
    return obj.deserialize();
  }
 else   return (GridCacheSwapEntry<V>)cctx.marshaller().unmarshal(bytes,ldr);
}
