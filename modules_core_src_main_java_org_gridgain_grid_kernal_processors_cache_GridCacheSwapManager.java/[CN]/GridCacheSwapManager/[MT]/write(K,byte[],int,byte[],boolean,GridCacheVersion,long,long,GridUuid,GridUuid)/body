{
  if (!offheapEnabled && !swapEnabled)   return;
  checkIteratorQueue();
  int part=cctx.affinity().partition(key);
  GridCacheSwapEntry<V> entry=new GridCacheSwapEntryImpl<>(keyHash,val,valIsByteArr,ver,ttl,expireTime,keyClsLdrId,valClsLdrId);
  if (offheapEnabled) {
    offheap.put(spaceName,part,key,keyBytes,marshal(entry));
    if (cctx.events().isRecordable(EVT_CACHE_OBJECT_TO_OFFHEAP))     cctx.events().addEvent(part,key,cctx.nodeId(),(GridUuid)null,null,EVT_CACHE_OBJECT_TO_OFFHEAP,null,false,null,true,null,null,null);
  }
 else   if (swapEnabled)   writeToSwap(part,key,keyBytes,marshal(entry));
  GridCacheQueryManager<K,V> qryMgr=cctx.queries();
  if (qryMgr != null)   qryMgr.onSwap(spaceName,key);
}
