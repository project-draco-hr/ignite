{
  GridTestUtils.assertThrows(log,new Callable<Object>(){
    @Override public Object call() throws Exception {
      return GridIpcServerEndpointDeserializer.deserialize(GridHadoopTestUtils.jsonToMap(JSONSerializer.toJSON(shmemSrvEndpoint).toString()));
    }
  }
,GridException.class,"Failed to create server endpoint (type is not specified)");
}
