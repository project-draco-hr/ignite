{
  GridTestUtils.assertThrows(log,new Callable<Object>(){
    @Override public Object call() throws Exception {
      return GridIpcServerEndpointDeserializer.deserialize(GridTestUtils.jsonToMap(JSONSerializer.toJSON(tcpSrvEndpoint).toString()));
    }
  }
,GridException.class,null);
}
