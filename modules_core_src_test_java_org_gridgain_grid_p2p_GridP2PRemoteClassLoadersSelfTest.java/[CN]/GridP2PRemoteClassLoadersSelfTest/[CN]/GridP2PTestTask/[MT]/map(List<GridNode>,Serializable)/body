{
  Map<GridComputeJob,GridNode> map=new HashMap<>(subgrid.size());
  for (  GridNode node : subgrid) {
    if (!node.id().equals(nodeId))     map.put(new GridP2PTestJob(null),node);
  }
  return map;
}
