{
  A.notNull(fieldName,"fieldName");
  if (!allowFields)   throw new GridPortableException("Fields are not allowed.");
  if (useNames)   doWriteByteArray(fieldName.getBytes(UTF_8));
 else   doWriteInt(fieldName.hashCode());
}
