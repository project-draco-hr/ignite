{
  boolean res=method.getParameterTypes().length == 0 && method.getName().startsWith("test") && method.getReturnType().equals(Void.TYPE);
  if (res) {
    boolean hasIgnore=method.isAnnotationPresent(IgniteIgnore.class);
    res=hasIgnore == ignoredOnly;
  }
  return res;
}
