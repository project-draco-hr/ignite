{
  ClusterNodeLocalMap<QueryExecutionKey,QueryFutureWrapper> locMap=g.cluster().nodeLocalMap();
  return fetchQueryResults(req.queryId(),locMap.get(new QueryExecutionKey(req.queryId())),locMap,g.cluster().localNode().id());
}
