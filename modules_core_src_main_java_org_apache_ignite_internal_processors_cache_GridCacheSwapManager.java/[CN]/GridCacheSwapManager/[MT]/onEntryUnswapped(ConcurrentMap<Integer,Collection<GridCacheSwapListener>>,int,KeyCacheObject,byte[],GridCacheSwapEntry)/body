{
  Collection<GridCacheSwapListener> lsnrs=map.get(part);
  if (lsnrs == null) {
    if (log.isDebugEnabled())     log.debug("Skipping unswapped notification [key=" + key + ", part="+ part+ ']');
    return;
  }
  for (  GridCacheSwapListener lsnr : lsnrs)   lsnr.onEntryUnswapped(part,key,keyBytes,e);
}
