{
  return isLocNode && isRmtNodes ? (Collection<GridNode>)grid : isLocNode ? Collections.singletonList(getSpiContext().localNode()) : isRmtNodes ? (Collection<GridNode>)F.view(grid,F.remoteNodes(locNodeId)) : Collections.<GridNode>emptyList();
}
