{
  GridCacheAdapter<K,V> cache=((GridKernal)ignite).context().cache().internalCache(cacheName);
  cache.context().gate().enter();
  try {
    if (update)     cache.localLoadAndUpdate(keys);
 else     cache.localLoad(keys);
  }
  finally {
    cache.context().gate().leave();
  }
  return null;
}
