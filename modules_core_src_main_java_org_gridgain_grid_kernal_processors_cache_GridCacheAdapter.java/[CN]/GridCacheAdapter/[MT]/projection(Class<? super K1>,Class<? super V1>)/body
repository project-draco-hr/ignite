{
  if (ctx.deploymentEnabled()) {
    try {
      ctx.deploy().registerClasses(keyType,valType);
    }
 catch (    GridException e) {
      throw new GridRuntimeException(e);
    }
  }
  GridCacheProjectionImpl<K1,V1> prj=new GridCacheProjectionImpl<>((GridCacheProjection<K1,V1>)this,(GridCacheContext<K1,V1>)ctx,CU.<K1,V1>typeFilter(keyType,valType),null,null,null,GridPortableObject.class.isAssignableFrom(keyType),GridPortableObject.class.isAssignableFrom(valType));
  return new GridCacheProxyImpl<>((GridCacheContext<K1,V1>)ctx,prj,prj);
}
