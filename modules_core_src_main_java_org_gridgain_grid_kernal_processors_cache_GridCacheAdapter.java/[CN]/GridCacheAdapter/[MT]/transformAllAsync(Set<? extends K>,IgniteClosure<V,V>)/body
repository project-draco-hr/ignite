{
  if (F.isEmpty(keys))   return new GridFinishedFuture<>(ctx.kernalContext());
  return transformAllAsync(F.viewAsMap(keys,new C1<K,IgniteClosure<V,V>>(){
    @Override public IgniteClosure<V,V> apply(    K k){
      return transformer;
    }
  }
));
}
