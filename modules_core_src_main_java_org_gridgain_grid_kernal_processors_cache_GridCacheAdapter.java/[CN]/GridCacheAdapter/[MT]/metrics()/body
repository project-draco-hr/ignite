{
  GridCacheMetricsAdapter cp=GridCacheMetricsAdapter.copyOf(metrics);
  if (cp != null) {
    GridDrSenderCacheMetricsAdapter drSndMetrics=cp.drSendMetrics0();
    if (drSndMetrics != null)     drSndMetrics.backupQueueSize(drBackupQueueSize());
  }
  return cp;
}
