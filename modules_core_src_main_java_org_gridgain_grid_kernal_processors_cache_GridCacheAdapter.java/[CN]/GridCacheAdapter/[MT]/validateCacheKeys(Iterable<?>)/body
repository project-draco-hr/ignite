{
  if (keyChecks <= MAX_KEY_CHECKS) {
    for (    Object key : keys) {
      if (key == null)       continue;
      if (keyChecks > MAX_KEY_CHECKS)       return;
      CU.validateCacheKey(log,key);
      keyChecks++;
    }
  }
}
