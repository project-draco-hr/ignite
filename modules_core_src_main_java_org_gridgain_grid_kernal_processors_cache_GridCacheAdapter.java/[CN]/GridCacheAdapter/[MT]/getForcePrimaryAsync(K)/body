{
  ctx.denyOnFlag(LOCAL);
  return getAllAsync(Collections.singletonList(key),true,false,null,null,true).chain(new CX1<GridFuture<Map<K,V>>,V>(){
    @Override public V applyx(    GridFuture<Map<K,V>> e) throws GridException {
      return e.get().get(key);
    }
  }
);
}
