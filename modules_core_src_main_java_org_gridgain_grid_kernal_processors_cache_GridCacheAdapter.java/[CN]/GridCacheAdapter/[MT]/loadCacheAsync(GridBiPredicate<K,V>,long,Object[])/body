{
  return ctx.closures().callLocalSafe(ctx.projectSafe(new Callable<Object>(){
    @Nullable @Override public Object call() throws GridException {
      loadCache(p,ttl,args);
      return null;
    }
  }
),true);
}
