{
  if (this == o)   return true;
  if (!(o instanceof GridHadoopFileBlock))   return false;
  GridHadoopFileBlock that=(GridHadoopFileBlock)o;
  return len == that.len && start == that.start && file.equals(that.file);
}
