{
  if (!adapter.getSpiContext().isStopping() && sockWriter.isOnline()) {
    TcpDiscoveryClientHeartbeatMessage msg=new TcpDiscoveryClientHeartbeatMessage(getLocalNodeId(),adapter.metricsProvider.metrics());
    msg.client(true);
    sockWriter.sendMessage(msg);
  }
}
