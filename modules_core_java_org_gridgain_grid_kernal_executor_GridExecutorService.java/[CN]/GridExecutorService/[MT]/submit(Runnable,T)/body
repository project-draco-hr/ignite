{
  A.notNull(task,"task != null");
  checkShutdown();
  deployTask(GridExecutorCallableTask.class,task);
  return addFuture(prj.compute().execute(new GridExecutorCallableTask<T>(task.getClass()),new GridExecutorRunnableAdapter<>(task,res),0));
}
