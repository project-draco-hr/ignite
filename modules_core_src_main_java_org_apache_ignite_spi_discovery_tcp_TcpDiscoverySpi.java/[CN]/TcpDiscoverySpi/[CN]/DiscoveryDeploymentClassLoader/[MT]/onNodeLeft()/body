{
  lock.writeLock().lock();
  try {
    if (sock != null) {
      if (log.isDebugEnabled())       log.debug("Closing deployment class loader connection on node left [node=" + node.id() + ']');
      U.closeQuiet(sock);
      sock=null;
    }
  }
  finally {
    lock.writeLock().unlock();
  }
}
