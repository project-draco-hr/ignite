{
  if (sock == null) {
    for (    InetSocketAddress addr : getNodeAddresses(node,U.sameMacs(locNode,node))) {
      sock=connect(addr);
      if (sock != null)       break;
    }
  }
  return sock;
}
