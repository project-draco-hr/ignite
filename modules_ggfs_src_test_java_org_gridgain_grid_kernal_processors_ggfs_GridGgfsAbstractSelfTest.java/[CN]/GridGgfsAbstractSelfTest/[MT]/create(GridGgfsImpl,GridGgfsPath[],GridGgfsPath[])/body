{
  if (dirs != null) {
    for (    GridGgfsPath dir : dirs)     ggfs.mkdirs(dir);
  }
  if (files != null) {
    for (    GridGgfsPath file : files) {
      GridGgfsOutputStream os=ggfs.create(file,true);
      os.close();
    }
  }
}
