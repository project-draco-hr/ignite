{
  if (po == null)   doWriteByte(NULL);
 else {
    doWriteByte(PORTABLE_OBJ);
    byte[] poArr=po.array();
    doWriteInt(poArr.length);
    wCtx.out.writeByteArray(poArr);
    doWriteInt(po.start());
  }
}
