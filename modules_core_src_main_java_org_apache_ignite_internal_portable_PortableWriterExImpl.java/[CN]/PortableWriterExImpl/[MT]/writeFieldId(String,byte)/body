{
  A.notNull(fieldName,"fieldName");
  if (!allowFields)   throw new PortableException("Individual field can't be written after raw writer is acquired " + "via rawWriter() method. Consider fixing serialization logic for class: " + cls.getName());
  int id=ctx.fieldId(typeId,fieldName);
  if (metaEnabled)   metaHashSum=31 * metaHashSum + (id + fieldType);
  doWriteInt(id);
}
