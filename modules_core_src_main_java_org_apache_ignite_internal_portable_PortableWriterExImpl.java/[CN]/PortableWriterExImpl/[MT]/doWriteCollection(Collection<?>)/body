{
  if (col == null)   doWriteByte(NULL);
 else {
    if (tryWriteAsHandle(col))     return;
    doWriteByte(COL);
    doWriteInt(col.size());
    doWriteByte(ctx.collectionType(col.getClass()));
    for (    Object obj : col)     doWriteObject(obj);
  }
}
