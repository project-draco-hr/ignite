{
  final int keyCnt=10000;
  final int valCnt=10000;
  final Grid g=grid(0);
  final GridCache<Integer,TestValue> c=g.cache(null);
  Random rnd=new Random();
  for (; ; )   c.putx(rnd.nextInt(keyCnt),new TestValue(rnd.nextInt(valCnt)));
}
