{
  for (  Map.Entry<Integer,IgniteBiTuple<Class,Boolean>> e : clsById.entrySet()) {
    if (!e.getValue().get2() && ldr.equals(e.getValue().get1().getClassLoader()))     clsById.remove(e.getKey());
  }
}
