{
  ClusterNode localNode=getLocalNode();
  UUID id;
  if (localNode == null) {
    U.warn(log,"Local node is not started or fully initialized [isStopping=" + getSpiContext().isStopping() + ']');
    id=new UUID(0,0);
  }
 else   id=localNode.id();
  return new NodeIdMessage(id);
}
