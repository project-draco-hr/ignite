{
  if (maxSize < 0)   throw new IgniteCheckedException("Failed to initialize window (maximumSize cannot be negative) " + "[windowClass=" + getClass().getSimpleName() + ", maxSize="+ maxSize+ ", unique="+ unique+ ']');
}
