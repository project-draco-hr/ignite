{
  Collection col=cnt >= 0 ? idx0.values() : idx0.descendingMap().values();
  return (Collection<StreamerIndexEntry<E,K,V>>)(cnt == 0 ? Collections.unmodifiableCollection(col) : F.limit(col,U.safeAbs(cnt)));
}
