{
  if (fs instanceof IgniteHadoopFileSystem)   ((IgniteHadoopFileSystem)fs).setUser(userName);
 else   if (fs instanceof HadoopDistributedFileSystem)   ((HadoopDistributedFileSystem)fs).setUser(userName);
}
