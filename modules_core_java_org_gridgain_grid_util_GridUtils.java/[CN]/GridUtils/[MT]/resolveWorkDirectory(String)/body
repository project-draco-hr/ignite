{
  File dir=new File(path);
  String ggHome;
  if (dir.isAbsolute())   return dir;
 else   if (!F.isEmpty(ggHome=U.getGridGainHome()))   return new File(ggHome,path);
 else   throw new IOException("Failed to resolve directory path: " + path);
}
