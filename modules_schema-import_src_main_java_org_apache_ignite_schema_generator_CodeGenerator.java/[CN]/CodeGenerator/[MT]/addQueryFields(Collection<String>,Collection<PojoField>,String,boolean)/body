{
  if (fields.isEmpty())   return first;
  add2(src,comment);
  add2(src,(first ? "LinkedHashMap<String, String> " : "") + "fields = new LinkedHashMap<>();");
  add0(src,"");
  for (  PojoField field : fields)   add2(src,"fields.put(\"" + field.javaName() + "\", \""+ javaTypeName(field)+ "\");");
  add0(src,"");
  add2(src,"qryEntity.setFields(fields);");
  add0(src,"");
  return false;
}
