{
  if (ttl != -1L) {
    if (nearTtls == null) {
      nearTtls=new GridLongList();
      for (int i=0; i < idx - 1; i++)       nearTtls.add(-1L);
    }
  }
  if (nearTtls != null)   nearTtls.add(ttl);
}
