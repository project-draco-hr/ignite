{
  IgniteCache<CacheAffinityKey<UUID>,Person> cache=Ignition.ignite().jcache(CACHE_NAME);
  String joinSql="from Person, Organization " + "where Person.orgId = Organization.id " + "and lower(Organization.name) = lower(?)";
  print("Following people are 'Ignite' employees: ",cache.query(new QuerySqlPredicate(Person.class,joinSql,"Ignite")).getAll());
  print("Following people are 'Other' employees: ",cache.query(new QuerySqlPredicate(Person.class,joinSql,"Other")).getAll());
}
