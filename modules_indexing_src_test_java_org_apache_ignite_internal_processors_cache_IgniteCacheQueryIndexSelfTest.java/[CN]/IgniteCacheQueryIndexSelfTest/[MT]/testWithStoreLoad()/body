{
  for (int i=0; i < ENTRY_CNT; i++)   storeStgy.putToStore(i,new CacheValue(i));
  IgniteCache<Integer,CacheValue> cache0=grid(0).cache(null);
  cache0.loadCache(null);
  checkCache(cache0);
  checkQuery(cache0);
}
