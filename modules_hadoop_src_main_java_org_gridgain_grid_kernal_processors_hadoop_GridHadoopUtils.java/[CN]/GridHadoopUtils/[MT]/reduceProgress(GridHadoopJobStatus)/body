{
  if (status.reduceStartTime() == 0)   return 0.0f;
  return progress(status.totalReducerCnt(),status.totalReducerCnt() - status.pendingReducerCnt(),Math.min(status.totalReducerCnt(),status.concurrencyLevel()),status.reduceStartTime());
}
