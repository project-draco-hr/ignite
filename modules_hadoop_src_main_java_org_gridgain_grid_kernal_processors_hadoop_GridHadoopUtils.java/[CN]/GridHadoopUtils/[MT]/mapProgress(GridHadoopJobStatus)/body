{
  if (status.reduceStartTime() > 0)   return 1.0f;
  return progress(status.totalMapperCnt(),status.totalMapperCnt() - status.pendingMapperCnt(),Math.min(status.totalMapperCnt(),status.concurrencyLevel()),status.mapStartTime());
}
