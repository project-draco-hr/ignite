{
  if (!hasTaskId()) {
    return false;
  }
  if (!hasFinished()) {
    return false;
  }
  if (hasResultBean()) {
    if (!getResultBean().isInitialized()) {
      return false;
    }
  }
  return true;
}
