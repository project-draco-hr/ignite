{
  if (keyCheck) {
    keyCheck=false;
    if (key == null)     return;
    if (!U.overridesEqualsAndHashCode(key))     throw new IllegalArgumentException("Index key must override hashCode() and equals() methods: " + key.getClass().getName());
  }
}
