{
  writeLock().lock();
  try {
    if (evts == null)     return Collections.emptyList();
    Collection<E> res=new ArrayList<>(evts.sizex());
    E o;
    while ((o=evts.poll()) != null)     res.add(o);
    evts=null;
    return res;
  }
  finally {
    writeLock().unlock();
  }
}
