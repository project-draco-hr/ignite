{
  List<GridCacheTxEntry<K,V>> col=null;
  int size=in.readInt();
  if (size != -1) {
    col=new ArrayList<>(size);
    for (int i=0; i < size; i++)     col.add((GridCacheTxEntry<K,V>)in.readObject());
  }
  return col == null ? Collections.<GridCacheTxEntry<K,V>>emptyList() : col;
}
