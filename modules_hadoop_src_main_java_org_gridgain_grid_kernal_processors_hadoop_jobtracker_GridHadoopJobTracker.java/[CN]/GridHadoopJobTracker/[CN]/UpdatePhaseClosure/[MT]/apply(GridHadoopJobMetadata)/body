{
  GridHadoopJobMetadata cp=new GridHadoopJobMetadata(meta);
  cp.phase(phase);
  if (phase == PHASE_MAP)   cp.setupCompleteTimestamp(System.currentTimeMillis());
 else   if (phase == PHASE_COMPLETE)   cp.completeTimestamp(System.currentTimeMillis());
  return cp;
}
