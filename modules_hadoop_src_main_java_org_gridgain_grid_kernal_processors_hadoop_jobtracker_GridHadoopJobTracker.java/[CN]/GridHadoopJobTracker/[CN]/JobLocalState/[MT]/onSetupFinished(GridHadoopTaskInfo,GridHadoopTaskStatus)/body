{
  final GridHadoopJobId jobId=taskInfo.jobId();
  if (status.state() == FAILED || status.state() == CRASHED)   transform(jobId,new CancelJobClosure(status.failCause()));
 else   transform(jobId,new UpdatePhaseClosure(PHASE_MAP));
}
