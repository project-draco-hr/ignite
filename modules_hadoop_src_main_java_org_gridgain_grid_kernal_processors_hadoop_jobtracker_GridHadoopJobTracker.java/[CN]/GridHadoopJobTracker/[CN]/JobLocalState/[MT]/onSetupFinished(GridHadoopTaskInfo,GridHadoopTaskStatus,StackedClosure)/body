{
  final GridHadoopJobId jobId=taskInfo.jobId();
  if (status.state() == FAILED || status.state() == CRASHED)   transform(jobId,new CancelJobClosure(prev,status.failCause()));
 else   transform(jobId,new UpdatePhaseClosure(prev,PHASE_MAP));
}
