{
  JobLocalState state=activeJobs.get(jobId);
  if (state != null)   return state.job;
  GridHadoopJobMetadata meta=jobMetaCache().get(jobId);
  if (meta == null)   return null;
  return ctx.jobFactory().createJob(jobId,meta.jobInfo());
}
