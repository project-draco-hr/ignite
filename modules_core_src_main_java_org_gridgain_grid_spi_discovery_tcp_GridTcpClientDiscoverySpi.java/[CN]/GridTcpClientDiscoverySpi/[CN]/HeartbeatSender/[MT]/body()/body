{
  try {
    while (!isInterrupted()) {
      writeToSocket(sock,new GridTcpDiscoveryHeartbeatMessage(locNodeId));
    }
  }
 catch (  IOException|GridException e) {
    if (log.isDebugEnabled())     U.error(log,"Failed to send heartbeat message [sock=" + sock + ']',e);
  }
}
