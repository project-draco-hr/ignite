{
  if (optimistic() && !cacheCtx.config().isBatchUpdateOnCommit() && !F.isEmptyOrNulls(filter))   throw new GridException("Operations that receive non-empty predicate filters cannot be used for " + "optimistic mode if 'batchUpdateOnCommit' configuration flag is set to 'false': " + this);
  checkValid();
}
