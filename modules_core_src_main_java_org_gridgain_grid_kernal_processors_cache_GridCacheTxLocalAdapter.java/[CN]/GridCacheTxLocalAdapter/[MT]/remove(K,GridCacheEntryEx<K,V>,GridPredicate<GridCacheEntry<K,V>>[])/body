{
  V ret=removeAllAsync(Collections.singletonList(key),cached,implicit,true,filter).get().value();
  if (cctx.config().getInterceptor() != null)   return (V)cctx.config().getInterceptor().onBeforeRemove(key,ret).get2();
  return ret;
}
