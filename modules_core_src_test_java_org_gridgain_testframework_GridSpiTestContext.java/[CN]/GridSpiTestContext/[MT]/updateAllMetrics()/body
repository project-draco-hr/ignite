{
  notifyListener(new GridDiscoveryEvent(locNode.id(),"Metrics updated",EVT_NODE_METRICS_UPDATED,locNode.id()));
  for (  GridNode node : rmtNodes) {
    notifyListener(new GridDiscoveryEvent(locNode.id(),"Metrics updated",EVT_NODE_METRICS_UPDATED,node.id()));
  }
}
