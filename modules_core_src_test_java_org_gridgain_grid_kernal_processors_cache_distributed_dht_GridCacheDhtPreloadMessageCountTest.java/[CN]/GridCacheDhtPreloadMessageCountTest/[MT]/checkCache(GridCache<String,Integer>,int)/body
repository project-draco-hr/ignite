{
  Grid g=c.gridProjection().grid();
  for (int i=0; i < keyCnt; i++) {
    String key=Integer.toString(i);
    if (c.affinity().isPrimaryOrBackup(g.localNode(),key))     assertEquals(Integer.valueOf(i),c.peek(key));
  }
}
