{
  try {
    if (isAsync())     return (T)this;
    AsyncSupportAdapter<T> clone=(AsyncSupportAdapter<T>)clone();
    clone.curFut=new ThreadLocal<>();
    return (T)clone;
  }
 catch (  CloneNotSupportedException e) {
    throw new GridRuntimeException(e);
  }
}
