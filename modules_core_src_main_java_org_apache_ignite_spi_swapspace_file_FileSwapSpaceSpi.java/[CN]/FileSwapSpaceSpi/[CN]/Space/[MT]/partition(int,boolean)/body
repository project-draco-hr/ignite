{
  ConcurrentMap<SwapKey,SwapValue> map=parts.get(part);
  if (map == null && create) {
    ConcurrentMap<SwapKey,SwapValue> old=parts.putIfAbsent(part,map=new ConcurrentHashMap<>());
    if (old != null)     map=old;
  }
  return map;
}
