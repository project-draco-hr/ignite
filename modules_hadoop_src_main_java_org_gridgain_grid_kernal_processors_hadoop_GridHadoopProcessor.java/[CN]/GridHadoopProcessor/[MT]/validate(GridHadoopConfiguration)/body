{
  if (ctx.config().isPeerClassLoadingEnabled())   throw new GridException("Peer class loading cannot be used with Hadoop (disable it using " + "GridConfiguration.setPeerClassLoadingEnabled()).");
}
