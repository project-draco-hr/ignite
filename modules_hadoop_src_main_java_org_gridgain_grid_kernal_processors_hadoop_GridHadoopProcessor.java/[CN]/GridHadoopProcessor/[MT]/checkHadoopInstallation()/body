{
  String hadoopHome=System.getenv("HADOOP_HOME");
  if (F.isEmpty(hadoopHome))   U.warn(log,"HADOOP_HOME environment variable is not set.");
 else   if (log.isInfoEnabled())   log.info("Apache Hadoop is found at " + hadoopHome);
}
