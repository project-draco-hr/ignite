{
  RemoteRoutineInfo info;
  stopLock.lock();
  try {
    info=rmtInfos.remove(routineId);
    if (info == null)     stopped.add(routineId);
  }
  finally {
    stopLock.unlock();
  }
  if (info != null)   unregisterHandler(routineId,info.hnd,false);
}
