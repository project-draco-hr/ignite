{
  System.gc();
  long start=U.currentTimeMillis();
  TreePVector<Integer> list=TreePVector.empty();
  for (int i=0; i < ITER_CNT; i++)   list=list.plus(RND.nextInt(100));
  for (int i=0; i < ITER_CNT; i++)   list=list.minus(new Integer(RND.nextInt(100)));
  System.out.println("testPVectorRemove time: " + (U.currentTimeMillis() - start));
}
