{
  return isEmpty(ps) || isAlwaysTrue(ps) ? F.<Cache.Entry<K,V>>alwaysTrue() : isAlwaysFalse(ps) ? F.<Cache.Entry<K,V>>alwaysFalse() : new IgnitePredicate<Cache.Entry<K,V>>(){
    @Override public boolean apply(    Cache.Entry<K,V> e){
      V v=e.getValue();
      return v != null && F.isAll(v,ps);
    }
  }
;
}
