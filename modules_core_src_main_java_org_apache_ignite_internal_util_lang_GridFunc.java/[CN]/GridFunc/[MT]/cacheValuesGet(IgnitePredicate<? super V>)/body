{
  return isEmpty(ps) || isAlwaysTrue(ps) ? F.<Entry<K,V>>alwaysTrue() : isAlwaysFalse(ps) ? F.<Entry<K,V>>alwaysFalse() : new IgnitePredicate<Entry<K,V>>(){
    @Override public boolean apply(    Entry<K,V> e){
      V v=e.getValue();
      return v != null && F.isAll(v,ps);
    }
  }
;
}
