{
  if (col == null)   return null;
  int[] res=new int[col.size()];
  Iterator<? extends Number> iter=col.iterator();
  for (int i=0; i < res.length; i++)   res[i]=iter.next().intValue();
  return res;
}
