{
  A.notNull(c,"c");
  return new GridAbsPredicate(){
    @Override public boolean apply(){
      try {
        return c.call();
      }
 catch (      Exception e) {
        throw wrap(e);
      }
    }
  }
;
}
