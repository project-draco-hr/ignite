{
  if (isEmpty(nodeIds))   return alwaysFalse();
  return new P1<T>(){
    /** 
 */
    private static final long serialVersionUID=-5664060422647374863L;
    @Override public boolean apply(    ClusterNode e){
      return nodeIds.contains(e.id());
    }
  }
;
}
