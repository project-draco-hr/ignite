{
  return isEmpty(keys) ? F.<Entry<K,V>>alwaysFalse() : new IgnitePredicate<Entry<K,V>>(){
    @Override public boolean apply(    Entry<K,V> e){
      return keys != null && keys.contains(e.getKey());
    }
  }
;
}
