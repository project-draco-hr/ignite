{
  return isEmpty(keys) ? F.<CacheEntry<K,V>>alwaysFalse() : new IgnitePredicate<CacheEntry<K,V>>(){
    @Override public boolean apply(    CacheEntry<K,V> e){
      return keys != null && keys.contains(e.getKey());
    }
  }
;
}
