{
  return isEmpty(keys) ? F.<Cache.Entry<K,V>>alwaysFalse() : new IgnitePredicate<Cache.Entry<K,V>>(){
    @Override public boolean apply(    Cache.Entry<K,V> e){
      return keys != null && keys.contains(e.getKey());
    }
  }
;
}
