{
  A.notNull(fut,"fut");
  return new CO<T>(){
    @Override public T apply(){
      try {
        return fut.get();
      }
 catch (      Exception e) {
        throw new IgniteException(e);
      }
    }
  }
;
}
