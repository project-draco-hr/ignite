{
  A.notNull(m,"m");
  if (isAlwaysFalse(p))   return false;
 else   if (isAlwaysTrue(p))   return true;
 else {
    for (    Map.Entry<K,V> e : m.entrySet())     if (!p.apply(e))     return false;
  }
  return true;
}
