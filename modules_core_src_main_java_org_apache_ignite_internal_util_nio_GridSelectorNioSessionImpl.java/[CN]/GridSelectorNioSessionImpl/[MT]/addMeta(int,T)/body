{
  if (val instanceof GridNioRecoveryDescriptor) {
    recovery=(GridNioRecoveryDescriptor)val;
    if (!accepted())     recovery.connected();
    return null;
  }
 else   return super.addMeta(key,val);
}
