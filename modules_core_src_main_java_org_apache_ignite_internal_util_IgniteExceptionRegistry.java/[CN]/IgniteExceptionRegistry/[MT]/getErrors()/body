{
  int size=queue.size();
  List<IgniteExceptionInfo> errors=new ArrayList<>(size);
  int cnt=0;
  for (  IgniteExceptionInfo entry : queue) {
    if (cnt < size)     errors.add(entry);
 else     break;
    ++cnt;
  }
  return errors;
}
