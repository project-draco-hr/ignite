{
  errorCnt.incrementAndGet();
  while (queue.size() >= maxSize)   queue.pollLast();
  queue.offerFirst(new IgniteExceptionInfo(e,msg,Thread.currentThread().getId(),Thread.currentThread().getName(),U.currentTimeMillis()));
}
