{
  long order=errorCnt.incrementAndGet();
  while (queue.size() >= maxSize)   queue.pollLast();
  queue.offerFirst(new ExceptionInfo(order,e,msg,Thread.currentThread().getId(),Thread.currentThread().getName(),U.currentTimeMillis()));
}
