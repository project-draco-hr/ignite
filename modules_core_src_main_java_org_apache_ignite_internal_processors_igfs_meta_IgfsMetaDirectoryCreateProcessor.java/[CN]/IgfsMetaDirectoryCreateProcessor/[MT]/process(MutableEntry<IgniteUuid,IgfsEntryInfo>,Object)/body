{
  IgfsEntryInfo info=IgfsUtils.createDirectory(entry.getKey(),null,props,createTime,createTime);
  if (childName != null)   info=info.listing(Collections.singletonMap(childName,childEntry));
  entry.setValue(info);
  return info;
}
