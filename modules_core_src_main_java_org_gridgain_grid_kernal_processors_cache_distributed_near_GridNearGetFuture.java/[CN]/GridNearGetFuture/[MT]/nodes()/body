{
  return F.viewReadOnly(futures(),new IgniteClosure<IgniteFuture<Map<K,V>>,ClusterNode>(){
    @Nullable @Override public ClusterNode apply(    IgniteFuture<Map<K,V>> f){
      if (isMini(f))       return ((MiniFuture)f).node();
      return cctx.discovery().localNode();
    }
  }
);
}
