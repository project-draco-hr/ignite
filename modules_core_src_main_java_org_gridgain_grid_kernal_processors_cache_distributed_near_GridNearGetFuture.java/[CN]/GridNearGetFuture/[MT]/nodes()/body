{
  return F.viewReadOnly(futures(),new GridClosure<GridFuture<Map<K,V>>,ClusterNode>(){
    @Nullable @Override public ClusterNode apply(    GridFuture<Map<K,V>> f){
      if (isMini(f))       return ((MiniFuture)f).node();
      return cctx.discovery().localNode();
    }
  }
);
}
