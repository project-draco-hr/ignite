{
  if (!cctx.isAll(this.<K,V>wrap(),filter))   return F.t((CacheObject)CU.failed(failFast));
synchronized (this) {
    if (checkExpired())     return null;
  }
  GridCacheSwapEntry e=cctx.swap().read(this,false,true,true);
  return e != null ? F.t(e.value()) : null;
}
