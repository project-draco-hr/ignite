{
  A.notNull(key,"key",newVal,"newVal",curVal,"curVal");
synchronized (this) {
    if (hasMeta(key)) {
      V1 val=this.<V1>meta(key);
      if (val != null && val.equals(curVal)) {
        addMeta(key,newVal);
        return true;
      }
    }
    return false;
  }
}
