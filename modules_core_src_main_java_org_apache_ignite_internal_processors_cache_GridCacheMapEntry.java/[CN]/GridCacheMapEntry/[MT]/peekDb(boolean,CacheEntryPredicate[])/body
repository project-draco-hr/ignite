{
  if (!cctx.isAll(this,filter))   return CU.failed(failFast);
synchronized (this) {
    if (checkExpired())     return null;
  }
  return cctx.toCacheObject(cctx.store().loadFromStore(cctx.tm().localTxx(),key));
}
