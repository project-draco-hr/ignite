{
  if (this == o)   return true;
  if (!(o instanceof BinaryInternalMapper))   return false;
  BinaryInternalMapper mapper=(BinaryInternalMapper)o;
  return checkOnZeroId == mapper.checkOnZeroId && idMapper.equals(mapper.idMapper) && nameMapper.equals(mapper.nameMapper);
}
