{
  guard();
  try {
    return (affKey == null || job == null) ? new GridFinishedFuture<R>(ctx) : ctx.closure().callAsync(BALANCE,wrapCall(cacheName,affKey,job),prj.nodes());
  }
 catch (  GridException e) {
    return new GridFinishedFuture<>(ctx,e);
  }
 finally {
    unguard();
  }
}
