{
  A.notNull(mode,"mode");
  guard();
  try {
    return job == null ? new GridFinishedFuture<T>(ctx) : ctx.closure().runAsync(mode,F.curry(job,arg),prj.nodes());
  }
  finally {
    unguard();
  }
}
