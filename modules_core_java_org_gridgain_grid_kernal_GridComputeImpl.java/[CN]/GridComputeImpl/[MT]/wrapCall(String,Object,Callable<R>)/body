{
  final Object affKey0=ctx.affinity().affinityKey(cacheName,affKey);
  return new GridCallable<R>(){
    @GridCacheName private final String cn=cacheName;
    @GridCacheAffinityKeyMapped private final Object ak=affKey0;
    @Override public R call(){
      try {
        return call.call();
      }
 catch (      Exception e) {
        throw F.wrap(e);
      }
    }
  }
;
}
