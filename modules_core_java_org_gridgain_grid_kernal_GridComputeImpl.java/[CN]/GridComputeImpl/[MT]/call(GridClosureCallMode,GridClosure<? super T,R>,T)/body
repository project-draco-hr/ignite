{
  A.notNull(mode,"mode",job,"job");
  guard();
  try {
    return job == null ? new GridFinishedFuture<R>(ctx) : ctx.closure().callAsync(mode,F.curry(job,arg),prj.nodes());
  }
  finally {
    unguard();
  }
}
