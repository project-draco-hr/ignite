{
  guard();
  try {
    return (affKey == null || job == null) ? new GridFinishedFuture(ctx) : ctx.closure().runAsync(BALANCE,wrapRun(cacheName,affKey,job),prj.nodes());
  }
 catch (  GridException e) {
    return new GridFinishedFuture<>(ctx,e);
  }
 finally {
    unguard();
  }
}
