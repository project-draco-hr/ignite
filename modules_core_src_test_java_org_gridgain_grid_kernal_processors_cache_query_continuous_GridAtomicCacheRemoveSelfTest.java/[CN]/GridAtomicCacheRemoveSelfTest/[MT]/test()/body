{
  while (true) {
    grid(0).cache(null).putx(1,1);
    for (int i=0; i < GRID_CNT; i++)     grid(i).cache(null).removeAll();
    for (int i=0; i < GRID_CNT; i++)     assertEquals("Cache is not empty: " + grid(i).cache(null).entrySet(),0,grid(i).cache(null).size());
  }
}
