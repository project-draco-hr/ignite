{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(COL);
    if (flag == Flag.NORMAL)     return (Collection<T>)doReadCollection(true,cls);
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
