{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(LONG_ARR);
    if (flag == Flag.NORMAL)     return doReadLongArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
