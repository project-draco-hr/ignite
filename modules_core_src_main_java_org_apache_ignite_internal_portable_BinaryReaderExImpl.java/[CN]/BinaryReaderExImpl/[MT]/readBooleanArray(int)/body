{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(BOOLEAN_ARR);
    if (flag == Flag.NORMAL)     return doReadBooleanArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
