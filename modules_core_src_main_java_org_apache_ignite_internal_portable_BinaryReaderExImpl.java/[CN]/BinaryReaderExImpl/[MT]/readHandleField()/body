{
  int handle=(in.position() - 1) - in.readInt();
  int retPos=in.position();
  Object obj=rCtx.get(handle);
  if (obj == null) {
    in.position(handle);
    obj=doReadObject();
    in.position(retPos);
  }
  return (T)obj;
}
