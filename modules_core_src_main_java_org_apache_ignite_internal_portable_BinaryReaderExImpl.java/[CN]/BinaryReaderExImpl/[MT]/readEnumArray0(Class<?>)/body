{
switch (checkFlag(ENUM_ARR)) {
case NORMAL:
    Class<?> cls0=doReadClass();
  if (cls == null)   cls=cls0;
return doReadEnumArray(cls);
case HANDLE:
return readHandleField();
default :
return null;
}
}
