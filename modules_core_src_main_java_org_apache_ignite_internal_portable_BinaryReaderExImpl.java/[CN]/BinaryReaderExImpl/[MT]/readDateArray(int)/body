{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(DATE_ARR);
    if (flag == Flag.NORMAL)     return doReadDateArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
