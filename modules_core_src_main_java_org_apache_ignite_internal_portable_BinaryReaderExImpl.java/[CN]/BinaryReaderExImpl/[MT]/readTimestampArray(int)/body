{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(TIMESTAMP_ARR);
    if (flag == Flag.NORMAL)     return doReadTimestampArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
