{
  if (findFieldById(fieldId)) {
    Flag flag=checkFlag(MAP_ENTRY);
    if (flag == Flag.NORMAL)     return PortableUtils.doReadMapEntry(in,ctx,ldr,this,true);
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
