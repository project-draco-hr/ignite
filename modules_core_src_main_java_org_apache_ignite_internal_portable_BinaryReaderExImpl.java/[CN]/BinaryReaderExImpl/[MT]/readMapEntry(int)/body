{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(MAP_ENTRY);
    if (flag == Flag.NORMAL)     return doReadMapEntry(true);
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
