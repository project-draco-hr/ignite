{
  if (stackTrace == null)   stackTrace=stackTrace();
  rwLock.readLock();
  if (state.get() == GridKernalState.DISCONNECTED)   throw new IgniteClientDisconnectedException(reconnectFut,"Client node disconnected: " + gridName);
}
