{
  startStopwatch();
  assertParameter(probe != null,"loadProbe != null");
  if (log.isDebugEnabled())   log.debug(configInfo("loadProbe",probe));
  registerMBean(gridName,this,GridAdaptiveLoadBalancingSpiMBean.class);
  if (log.isDebugEnabled())   log.debug(startInfo());
}
