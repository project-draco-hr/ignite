{
  commState.setBuffer(buf);
  if (!super.writeTo(buf))   return false;
  if (!commState.typeWritten) {
    if (!commState.putByte(directType()))     return false;
    commState.typeWritten=true;
  }
switch (commState.idx) {
case 2:
    if (!commState.putLong(id))     return false;
  commState.idx++;
case 3:
if (!commState.putInt(pageSize)) return false;
commState.idx++;
case 4:
if (!commState.putGridUuid(setId)) return false;
commState.idx++;
case 5:
if (!commState.putLong(topVer)) return false;
commState.idx++;
}
return true;
}
