{
  final UUID rmtNodeId=getRemoteNodeId(name,sticky);
  if (rmtNodeId != null)   return getServiceProxy(name,svc,rmtNodeId);
 else   throw new GridException("No deployed service with given name: " + name);
}
