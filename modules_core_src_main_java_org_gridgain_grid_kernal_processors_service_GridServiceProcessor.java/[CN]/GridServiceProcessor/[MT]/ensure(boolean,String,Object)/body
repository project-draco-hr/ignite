{
  if (!cond)   if (v != null)   throw new GridRuntimeException("Service configuration check failed (" + desc + "): "+ v);
 else   throw new GridRuntimeException("Service configuration check failed (" + desc + ")");
}
