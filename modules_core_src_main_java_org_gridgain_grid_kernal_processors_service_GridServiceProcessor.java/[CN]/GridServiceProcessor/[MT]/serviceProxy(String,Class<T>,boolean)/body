{
synchronized (locSvcs) {
    Collection<GridServiceContextImpl> srvcContexts=locSvcs.get(name);
    if (srvcContexts == null) {
      return remoteServiceProxy(name,svc,sticky);
    }
 else     return (T)srvcContexts.iterator().next().service();
  }
}
