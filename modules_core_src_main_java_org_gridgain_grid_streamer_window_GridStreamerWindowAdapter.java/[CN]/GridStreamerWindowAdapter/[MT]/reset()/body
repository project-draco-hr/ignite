{
  lock.writeLock();
  try {
    if (idxs != null) {
      for (      GridStreamerIndexProvider<E,?,?> idx : idxs)       idx.reset();
    }
    reset0();
  }
  finally {
    lock.writeUnlock();
  }
}
