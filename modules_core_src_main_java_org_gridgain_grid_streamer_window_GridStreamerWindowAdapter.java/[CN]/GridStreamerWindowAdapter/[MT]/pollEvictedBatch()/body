{
  lock.readLock();
  try {
    Collection<E> evts=pollEvictedBatch0();
    if (!evts.isEmpty() && idxs != null) {
      for (      E evt : evts)       updateIndexes(evt,true);
    }
    return evts;
  }
  finally {
    lock.readUnlock();
  }
}
