{
  if (idxs != null) {
    Collection<GridStreamerIndex<E,?,?>> res=new ArrayList<>(idxs.length);
    for (    GridStreamerIndexProvider<E,?,?> idx : idxs)     res.add(idx.index());
    return res;
  }
 else   return Collections.emptyList();
}
