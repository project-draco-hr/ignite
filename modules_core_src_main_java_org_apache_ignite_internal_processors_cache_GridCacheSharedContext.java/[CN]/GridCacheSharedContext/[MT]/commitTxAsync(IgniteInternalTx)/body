{
  GridCacheContext ctx=tx.txState().singleCacheContext(this);
  if (ctx == null) {
    tx.txState().awaitLastFut(this);
    return tx.commitAsync();
  }
 else   return ctx.cache().commitTxAsync(tx);
}
