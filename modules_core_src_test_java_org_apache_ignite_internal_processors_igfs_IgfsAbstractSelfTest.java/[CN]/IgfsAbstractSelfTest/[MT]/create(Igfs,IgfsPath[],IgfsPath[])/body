{
  if (dirs != null) {
    for (    IgfsPath dir : dirs)     igfs.mkdirs(dir);
  }
  if (files != null) {
    for (    IgfsPath file : files) {
      OutputStream os=igfs.create(file,true);
      os.close();
    }
  }
}
