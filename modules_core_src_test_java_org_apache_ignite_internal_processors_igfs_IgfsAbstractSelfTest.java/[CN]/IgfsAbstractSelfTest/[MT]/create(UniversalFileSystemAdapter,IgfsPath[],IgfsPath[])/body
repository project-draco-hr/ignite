{
  if (dirs != null) {
    for (    IgfsPath dir : dirs)     uni.mkdirs(dir.toString());
  }
  if (files != null) {
    for (    IgfsPath file : files)     try (OutputStream os=uni.openOutputStream(file.toString(),false)){
    }
   }
}
