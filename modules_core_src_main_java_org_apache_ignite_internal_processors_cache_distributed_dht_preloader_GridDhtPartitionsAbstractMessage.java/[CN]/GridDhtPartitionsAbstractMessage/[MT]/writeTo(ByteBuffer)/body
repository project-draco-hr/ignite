{
  writer.setBuffer(buf);
  if (!super.writeTo(buf))   return false;
  if (!typeWritten) {
    if (!writer.writeByte(null,directType()))     return false;
    typeWritten=true;
  }
switch (state) {
case 3:
    if (!writer.writeMessage("exchId",exchId))     return false;
  state++;
case 4:
if (!writer.writeMessage("lastVer",lastVer)) return false;
state++;
}
return true;
}
