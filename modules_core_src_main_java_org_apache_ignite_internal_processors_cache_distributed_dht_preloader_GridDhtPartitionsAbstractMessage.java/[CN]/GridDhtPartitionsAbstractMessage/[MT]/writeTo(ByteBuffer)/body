{
  writer.setBuffer(buf);
  if (!super.writeTo(buf))   return false;
  if (!typeWritten) {
    if (!writer.writeByte(null,directType()))     return false;
    typeWritten=true;
  }
switch (state) {
case 3:
    if (!writer.writeMessage("exchId",exchId != null ? exchId.clone() : null))     return false;
  state++;
case 4:
if (!writer.writeMessage("lastVer",lastVer != null ? lastVer.clone() : null)) return false;
state++;
}
return true;
}
