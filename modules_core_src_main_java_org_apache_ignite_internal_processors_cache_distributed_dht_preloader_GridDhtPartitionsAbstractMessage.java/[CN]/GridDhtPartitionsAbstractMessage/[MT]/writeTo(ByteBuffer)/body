{
  commState.setBuffer(buf);
  if (!super.writeTo(buf))   return false;
  if (!commState.typeWritten) {
    if (!commState.putByte(null,directType()))     return false;
    commState.typeWritten=true;
  }
switch (commState.idx) {
case 3:
    if (!commState.putDhtPartitionExchangeId("exchId",exchId))     return false;
  commState.idx++;
case 4:
if (!commState.putCacheVersion("lastVer",lastVer)) return false;
commState.idx++;
}
return true;
}
