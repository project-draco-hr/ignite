{
  writer.setBuffer(buf);
  if (!super.writeTo(buf,writer))   return false;
  if (!writer.isTypeWritten()) {
    if (!writer.writeMessageType(directType()))     return false;
    writer.onTypeWritten();
  }
switch (writer.state()) {
case 3:
    if (!writer.writeField("exchId",exchId,MessageFieldType.MSG))     return false;
  writer.incrementState();
case 4:
if (!writer.writeField("lastVer",lastVer,MessageFieldType.MSG)) return false;
writer.incrementState();
}
return true;
}
