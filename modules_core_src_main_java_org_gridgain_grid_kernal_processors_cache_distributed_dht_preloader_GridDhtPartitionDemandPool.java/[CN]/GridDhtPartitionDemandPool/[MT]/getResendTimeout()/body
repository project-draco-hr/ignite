{
  try {
    return Long.parseLong(GridSystemProperties.getString(GG_PRELOAD_RESEND_TIMEOUT,String.valueOf(DFLT_PRELOAD_RESEND_TIMEOUT)));
  }
 catch (  NumberFormatException ignored) {
    return DFLT_PRELOAD_RESEND_TIMEOUT;
  }
}
