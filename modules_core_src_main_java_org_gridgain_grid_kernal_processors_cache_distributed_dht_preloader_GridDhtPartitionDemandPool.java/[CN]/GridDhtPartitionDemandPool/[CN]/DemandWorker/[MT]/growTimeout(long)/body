{
  long newTimeout=(long)(timeout * 1.5D);
  if (newTimeout < 0)   newTimeout=Long.MAX_VALUE;
  if (GridDhtPartitionDemandPool.this.timeout.compareAndSet(timeout,newTimeout))   U.warn(log,"Increased preloading message timeout from " + timeout + "ms to "+ newTimeout+ "ms.");
}
