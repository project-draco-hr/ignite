{
  Collection<IgniteFuture<?>> futs=new ArrayList<>();
  for (  Cache c : g.cachesx()) {
    if (cacheNames.contains(c.name()))     futs.add(c.forceRepartition());
  }
  for (  IgniteFuture f : futs)   f.get();
  return null;
}
