{
  final String queueName=queue.name();
  g.compute().run(new QueueClosure(CACHE_NAME,queueName,true)).get();
  print("Queue size after writing [expected=" + g.nodes().size() * RETRIES + ", actual=" + queue.size() + ']');
  print("Iterate over queue.");
  for (  SampleItem item : queue)   print("Queue item: " + item);
}
