{
  try {
    cache=true;
    for (int i=0; i < 4; i++) {
      nearOnly=i == 0;
      startGrid(i);
    }
    for (int i=0; i < 100; i++)     assertFalse("For key: " + i,grid(0).cache(null).affinity().isPrimaryOrBackup(grid(0).localNode(),i));
  }
  finally {
    stopAllGrids();
  }
}
