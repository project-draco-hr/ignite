{
  try {
    cache=true;
    for (int i=0; i < totalNodeCnt; i++) {
      cilent=nearNodeIdx == i;
      Ignite ignite=startGrid(i);
      if (cilent)       ignite.createCache((String)null,new NearCacheConfiguration());
    }
  }
  finally {
    stopAllGrids();
  }
}
