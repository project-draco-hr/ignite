{
  A.notNull(cls,"cls");
  A.notNull(clause,"clause");
  return new GridCacheQueryAdapter<>(ctx,SQL,filter(),U.box(cls).getSimpleName(),clause,null,false,prj != null && prj.portableKeys(),prj != null && prj.portableValues());
}
