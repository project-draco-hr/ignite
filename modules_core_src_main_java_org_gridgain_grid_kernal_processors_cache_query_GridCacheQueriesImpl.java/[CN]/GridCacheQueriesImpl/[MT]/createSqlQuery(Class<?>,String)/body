{
  A.notNull(cls,"cls");
  A.notNull(clause,"clause");
  return new GridCacheQueryAdapter<>(ctx,SQL,filter(),ctx.kernalContext().indexing().typeName(U.box(cls)),clause,null,false,prj != null && prj.isKeepPortable());
}
