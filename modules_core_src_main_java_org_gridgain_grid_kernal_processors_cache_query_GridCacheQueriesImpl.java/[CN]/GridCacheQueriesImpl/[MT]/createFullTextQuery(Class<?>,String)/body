{
  A.notNull(cls,"cls");
  A.notNull(search,"search");
  return new GridCacheQueryAdapter<>(ctx,TEXT,filter(),ctx.kernalContext().indexing().typeName(U.box(cls)),search,null,false,prj != null && prj.portableKeys(),prj != null && prj.portableValues());
}
