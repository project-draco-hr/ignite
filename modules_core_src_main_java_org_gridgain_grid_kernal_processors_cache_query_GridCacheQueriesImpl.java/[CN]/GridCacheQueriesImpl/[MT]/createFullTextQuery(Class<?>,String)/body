{
  A.notNull(cls,"cls");
  A.notNull(search,"search");
  return new GridCacheQueryAdapter<>(ctx,TEXT,filter(),ctx.kernalContext().query().typeName(U.box(cls)),search,null,false,prj != null && prj.isKeepPortable());
}
