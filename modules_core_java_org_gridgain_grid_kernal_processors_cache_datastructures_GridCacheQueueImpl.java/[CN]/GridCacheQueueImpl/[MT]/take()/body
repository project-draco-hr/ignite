{
  try {
    checkRemovedx();
    boolean peek=false;
    return blockReadOp(queryCallable(qryFactory.firstItemQuery(),new TerminalItemQueryLocalReducer<T>(true),peek),TAKE,peek);
  }
 catch (  GridException e) {
    throw new GridRuntimeException(e);
  }
}
