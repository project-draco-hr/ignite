{
  try {
    checkRemovedx();
    return CU.outTx(queryCallable(qryFactory.firstItemQuery(),new TerminalItemQueryLocalReducer<T>(true),true),cctx);
  }
 catch (  GridException e) {
    throw new GridRuntimeException(e);
  }
}
