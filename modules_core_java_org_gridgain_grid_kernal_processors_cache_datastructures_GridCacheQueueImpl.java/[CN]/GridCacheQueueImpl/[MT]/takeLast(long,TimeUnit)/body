{
  A.ensure(timeout >= 0,"Timeout cannot be negative: " + timeout);
  checkRemovedx();
  boolean peek=false;
  return blockReadOp(queryCallable(qryFactory.lastItemQuery(type),peek),TAKE_LAST_TIMEOUT,timeout,unit,peek);
}
