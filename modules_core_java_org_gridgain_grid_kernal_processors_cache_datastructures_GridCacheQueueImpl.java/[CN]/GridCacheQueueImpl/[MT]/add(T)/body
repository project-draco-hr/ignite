{
  A.notNull(item,"item");
  checkRemoved();
  try {
    boolean retVal=CU.outTx(addCallable(Arrays.asList(item)),cctx);
    if (!retVal)     throw new IllegalStateException();
    return true;
  }
 catch (  GridException e) {
    throw new GridRuntimeException(e);
  }
}
