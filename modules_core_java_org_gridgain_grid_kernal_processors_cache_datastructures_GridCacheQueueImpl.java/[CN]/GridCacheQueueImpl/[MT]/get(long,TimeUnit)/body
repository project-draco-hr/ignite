{
  A.ensure(timeout >= 0,"Timeout cannot be negative: " + timeout);
  checkRemovedx();
  boolean peek=true;
  return blockReadOp(queryCallable(qryFactory.firstItemQuery(type),peek),GET_TIMEOUT,timeout,unit,peek);
}
