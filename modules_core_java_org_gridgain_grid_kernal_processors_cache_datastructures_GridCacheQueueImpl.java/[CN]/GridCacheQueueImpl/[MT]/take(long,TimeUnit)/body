{
  A.ensure(timeout >= 0,"Timeout cannot be negative: " + timeout);
  checkRemovedx();
  boolean peek=false;
  return blockReadOp(queryCallable(qryFactory.firstItemQuery(type),peek),TAKE_TIMEOUT,timeout,unit,peek);
}
