{
  A.ensure(timeout >= 0,"Timeout cannot be negative: " + timeout);
  checkRemovedx();
  boolean peek=true;
  return blockReadOp(queryCallable(qryFactory.lastItemQuery(type),peek),GET_LAST_TIMEOUT,timeout,unit,peek);
}
