{
  while (cls != null) {
    Class<?>[] interfaces=cls.getInterfaces();
    for (    Class<?> iface0 : interfaces) {
      if (iface0.equals(iface))       return true;
    }
    cls=cls.getSuperclass();
  }
  return false;
}
