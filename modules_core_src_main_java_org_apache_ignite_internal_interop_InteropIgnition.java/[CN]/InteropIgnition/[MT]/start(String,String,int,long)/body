{
  IgniteConfiguration cfg=configuration(springCfgPath);
  if (gridName != null)   cfg.setGridName(gridName);
  InteropBootstrap bootstrap=bootstrap(factoryId);
  return bootstrap.start(cfg,envPtr);
}
