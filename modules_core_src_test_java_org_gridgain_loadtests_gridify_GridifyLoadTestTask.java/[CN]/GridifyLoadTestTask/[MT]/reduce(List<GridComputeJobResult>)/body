{
  int retVal=0;
  for (  GridComputeJobResult res : results) {
    if (res.getException() != null) {
      throw new GridException("Received exception in reduce method (load test jobs can never fail): " + res,res.getException());
    }
    retVal+=(Integer)res.getData();
  }
  return retVal;
}
