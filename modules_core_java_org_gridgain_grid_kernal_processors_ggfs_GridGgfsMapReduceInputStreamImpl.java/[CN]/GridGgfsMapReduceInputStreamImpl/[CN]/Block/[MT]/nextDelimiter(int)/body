{
  if (!delims.isEmpty()) {
    for (    DelimiterGroup delimGrp : delims) {
      for (      Delimiter delim : delimGrp.delimiters()) {
        if (delim.start() >= pos)         return delim;
      }
    }
  }
  return null;
}
