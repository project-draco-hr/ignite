{
  return new EntryIterator(nearSet.iterator(),F.iterator0(dhtSet,false,new P1<Entry<K,V>>(){
    @Override public boolean apply(    Entry<K,V> e){
      return !GridNearCacheAdapter.super.containsKey(e.getKey(),null);
    }
  }
));
}
