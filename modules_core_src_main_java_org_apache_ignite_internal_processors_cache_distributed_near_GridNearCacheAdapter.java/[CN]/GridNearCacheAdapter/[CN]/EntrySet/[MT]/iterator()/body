{
  return new EntryIterator(nearSet.iterator(),F.iterator0(dhtSet,false,new P1<CacheEntry<K,V>>(){
    @Override public boolean apply(    CacheEntry<K,V> e){
      return !GridNearCacheAdapter.super.containsKey(e.getKey());
    }
  }
));
}
