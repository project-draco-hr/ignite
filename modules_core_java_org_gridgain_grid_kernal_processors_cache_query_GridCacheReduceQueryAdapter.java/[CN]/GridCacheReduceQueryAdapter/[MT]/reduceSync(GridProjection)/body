{
  Collection<GridNode> nodes=nodes(grid);
  if (qryLog.isDebugEnabled())   qryLog.debug(U.compact("Executing reduce query " + toShortString(nodes)));
  if (locRdc == null)   throw new GridException("Local reducer must be set.");
  return (R2)F.first(executeSync(nodes,false,false,null,null));
}
