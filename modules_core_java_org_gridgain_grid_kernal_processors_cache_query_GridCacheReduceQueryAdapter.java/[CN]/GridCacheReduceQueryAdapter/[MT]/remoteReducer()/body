{
  try {
    if (rmtRdcBytes == null)     return null;
    return cctx.marshaller().unmarshal(rmtRdcBytes,cctx.deploy().globalLoader());
  }
 catch (  Exception e) {
    throw new GridRuntimeException("Failed to restore remote reducer state.",e);
  }
}
