{
  super.readExternal(in);
  int metricsSize=in.readInt();
  metrics=U.newHashMap(metricsSize);
  for (int i=0; i < metricsSize; i++)   metrics.put(U.readUuid(in),(MetricsSet)in.readObject());
  clientNodeIds=U.readCollection(in);
}
