{
  checkInternal(key);
  GridCacheTxEntry<K,V> txEntry=new GridCacheTxEntry<>(cctx,this,op,val,0L,-1L,cctx.cache().entryEx(key),drVer);
  txEntry.keyBytes(keyBytes);
  txEntry.valueBytes(valBytes);
  writeMap.put(key,txEntry);
}
