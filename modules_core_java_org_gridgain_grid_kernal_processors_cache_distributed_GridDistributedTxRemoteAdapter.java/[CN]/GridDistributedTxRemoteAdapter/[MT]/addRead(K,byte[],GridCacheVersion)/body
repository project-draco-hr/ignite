{
  checkInternal(key);
  GridCacheTxEntry<K,V> txEntry=new GridCacheTxEntry<>(cctx,this,READ,null,0L,-1L,cctx.cache().entryEx(key),drVer);
  txEntry.keyBytes(keyBytes);
  readMap.put(key,txEntry);
}
