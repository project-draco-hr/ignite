{
  byte flag=readByte();
  if (flag == GridPortableMarshaller.NULL)   return null;
  if (flag != GridPortableMarshaller.STRING)   throw new BinaryObjectException("Failed to deserialize String.");
  int len=readInt();
  String str=new String(arr,pos,len,UTF_8);
  pos+=len;
  return str;
}
