{
  if (delims != null) {
    out.writeBoolean(true);
    out.writeInt(delims.length);
    for (    byte[] delim : delims)     IgniteByteUtils.writeByteArray(out,delim);
  }
 else   out.writeBoolean(false);
}
