{
  pathModes(F.t("dir",PROXY));
  String errMsg=null;
  try {
    startUp();
  }
 catch (  IgniteCheckedException e) {
    errMsg=e.getCause().getMessage();
  }
  assertTrue(errMsg.startsWith("Grid configuration parameter invalid: secondaryFileSystem cannot be null when mode is SECONDARY"));
}
