{
  startGrid();
  Manager mgr=new Manager(grid().context(),new Spi());
  assertTrue(mgr.enabled());
  mgr.onKernalStart();
  mgr.onKernalStop(false);
  mgr.stop(false);
  assertTrue(mgr.enabled());
}
