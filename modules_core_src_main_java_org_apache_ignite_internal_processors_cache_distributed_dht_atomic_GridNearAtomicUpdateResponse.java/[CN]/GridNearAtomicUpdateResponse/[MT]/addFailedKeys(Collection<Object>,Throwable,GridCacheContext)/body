{
  if (failedKeys == null)   failedKeys=new ArrayList<>(keys.size());
  for (  Object key : keys)   failedKeys.add(ctx.toCacheKeyObject(key));
  if (err == null)   err=new IgniteCheckedException("Failed to update keys on primary node.");
  err.addSuppressed(e);
}
