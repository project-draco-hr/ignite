{
  int i=id & 0xff;
  DataCenterStream ds=streamById[i];
  if (ds == null) {
synchronized (this) {
      ds=streamById[i];
      if (ds == null && create) {
        ds=new DataCenterStream(id,headPosition());
        streamById[i]=ds;
      }
    }
  }
  return ds;
}
