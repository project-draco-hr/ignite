{
  if (!(o instanceof GridCacheEntryImpl))   return false;
  CacheEntry<K,V> e=(CacheEntry<K,V>)o;
  if (F.isAll(e,filter) && set.remove(e)) {
    try {
      e.removex();
    }
 catch (    IgniteCheckedException ex) {
      throw new IgniteException(ex);
    }
    return true;
  }
  return false;
}
