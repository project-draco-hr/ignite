{
  Collection<Object> toSnd=null;
synchronized (this) {
    buf.add(obj);
    if (buf.size() == bufSize) {
      toSnd=buf;
      buf=new ArrayList<>(bufSize);
      if (interval > 0)       lastSndTime=U.currentTimeMillis();
    }
  }
  return toSnd;
}
