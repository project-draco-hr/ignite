{
  if (appLdr.getParent() != null)   printUrls(appLdr.getParent());
  System.out.println(appLdr);
  if (appLdr instanceof URLClassLoader) {
    for (    URL url : ((URLClassLoader)appLdr).getURLs()) {
      System.out.println("\t" + url);
    }
  }
}
