{
  if (name.length() <= 1 || name.indexOf('.') == -1)   return false;
  for (  char c : name.toCharArray()) {
    if (!Character.isAlphabetic(c) && !Character.isDigit(c) && "_.$".indexOf(c) == -1)     return false;
  }
  return true;
}
