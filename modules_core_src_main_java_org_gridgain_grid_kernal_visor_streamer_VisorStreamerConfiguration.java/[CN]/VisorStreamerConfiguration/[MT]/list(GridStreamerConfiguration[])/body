{
  if (streamers == null)   return Collections.emptyList();
  final Collection<VisorStreamerConfiguration> cfgs=new ArrayList<>(streamers.length);
  for (  GridStreamerConfiguration streamer : streamers)   cfgs.add(from(streamer));
  return cfgs;
}
