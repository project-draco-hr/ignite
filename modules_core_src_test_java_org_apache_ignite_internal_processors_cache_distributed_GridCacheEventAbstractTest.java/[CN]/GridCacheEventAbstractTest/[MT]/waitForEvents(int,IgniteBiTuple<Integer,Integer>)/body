{
  if (!F.isEmpty(evtCnts))   try {
    TestEventListener.waitForEventCount(((GridKernal)grid(0)).context(),evtCnts);
  }
 catch (  IgniteCheckedException e) {
    printEventCounters(gridIdx,evtCnts);
    throw e;
  }
}
