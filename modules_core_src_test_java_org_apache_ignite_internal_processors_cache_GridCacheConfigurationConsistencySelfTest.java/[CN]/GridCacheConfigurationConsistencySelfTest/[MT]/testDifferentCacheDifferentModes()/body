{
  cacheEnabled=true;
  cacheName="local";
  cacheMode=LOCAL;
  depMode=SHARED;
  startGrid(1);
  cacheEnabled=true;
  cacheName="replicated";
  cacheMode=REPLICATED;
  depMode=SHARED;
  startGrid(2);
  cacheEnabled=true;
  cacheName="partitioned";
  cacheMode=PARTITIONED;
  depMode=SHARED;
  startGrid(3);
  cacheEnabled=true;
  cacheName="replicated";
  cacheMode=null;
  depMode=SHARED;
  startGrid(4);
}
