{
synchronized (zeroKeyVals) {
    for (int i=0; i < zeroKeyVals.size(); i++)     c.apply(zeroKeyVals.get(i));
  }
  for (  GridCacheFileLocalStoreMap t : segments) {
synchronized (t) {
      t.iterate(c);
    }
  }
}
