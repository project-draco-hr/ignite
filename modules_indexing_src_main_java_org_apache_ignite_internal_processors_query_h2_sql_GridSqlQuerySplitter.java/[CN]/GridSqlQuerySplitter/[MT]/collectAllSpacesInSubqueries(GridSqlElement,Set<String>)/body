{
  if (el instanceof GridSqlAlias)   el=el.child();
  if (el instanceof GridSqlOperation || el instanceof GridSqlFunction) {
    for (    GridSqlElement child : el)     collectAllSpacesInSubqueries(child,schemas);
  }
 else   if (el instanceof GridSqlSubquery)   collectAllSpaces(((GridSqlSubquery)el).select(),schemas);
}
