{
  if (qry instanceof GridSqlSelect)   return findParams((GridSqlSelect)qry,params,target);
  GridSqlUnion union=(GridSqlUnion)qry;
  findParams(union.left(),params,target);
  findParams(union.right(),params,target);
  return target;
}
