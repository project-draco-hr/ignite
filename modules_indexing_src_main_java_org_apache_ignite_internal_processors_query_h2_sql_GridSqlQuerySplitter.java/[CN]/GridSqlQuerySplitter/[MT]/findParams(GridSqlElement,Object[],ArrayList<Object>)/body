{
  if (el == null)   return;
  if (el instanceof GridSqlParameter) {
    int idx=((GridSqlParameter)el).index();
    while (target.size() < idx)     target.add(null);
    target.add(idx,params[idx]);
  }
 else   for (  GridSqlElement child : el)   findParams(child,params,target);
}
