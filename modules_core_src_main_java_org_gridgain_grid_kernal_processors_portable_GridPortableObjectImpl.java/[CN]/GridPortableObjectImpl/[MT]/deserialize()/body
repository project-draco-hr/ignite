{
  GridPortableClassDescriptor desc=ctx.descriptorForTypeId(userType,typeId);
  if (desc == null)   throw new GridPortableInvalidClassException("Unknown type [userType=" + userType + ", typeId="+ typeId+ ']');
  return (T)desc.read(reader);
}
