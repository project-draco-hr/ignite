{
  super(HadoopIgfsEndpoint.normalize(name),IGFS_SCHEME,false,-1);
  uri=name;
  user=getFsHadoopUser(cfg);
  try {
    initialize(name,cfg);
  }
 catch (  IOException e) {
    if (rmtClient != null)     rmtClient.close(false);
    throw e;
  }
  workingDir=new IgfsPath("/user/" + user);
}
