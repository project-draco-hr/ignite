{
  if (nearVals != null) {
    V val=nearVals.get(idx);
    if (val != null)     return val;
  }
  if (nearValBytes != null) {
    GridCacheValueBytes valBytes0=nearValBytes.get(idx);
    if (valBytes0 != null && valBytes0.isPlain())     return (V)valBytes0.get();
  }
  return null;
}
