{
  GridNamedInstance grid=name != null ? grids.get(name) : dfltGrid;
  GridKernal res=null;
  if (grid == null || (res=grid.gridx()) == null)   U.warn(null,"Grid instance was not properly started or was already stopped: " + name);
  return res;
}
