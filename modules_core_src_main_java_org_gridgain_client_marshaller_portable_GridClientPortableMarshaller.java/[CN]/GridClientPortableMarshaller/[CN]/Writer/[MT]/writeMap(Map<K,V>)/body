{
  if (map != null) {
    out.writeInt(map.size());
    for (    Map.Entry<K,V> e : map.entrySet()) {
      writeObject(e.getKey());
      writeObject(e.getValue());
    }
  }
 else   out.writeInt(-1);
}
