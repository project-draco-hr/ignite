{
  if (ctx.config().isPeerClassLoadingEnabled())   throw new IgniteCheckedException("Peer class loading cannot be used with Hadoop (disable it using " + "IgniteConfiguration.setPeerClassLoadingEnabled()).");
}
