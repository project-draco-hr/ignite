{
  if (ccfgs == null)   return Collections.emptyList();
  final Collection<VisorCacheConfiguration> res=new ArrayList<>(ccfgs.length);
  for (  CacheConfiguration ccfg : ccfgs)   res.add(collectCache(ignite,ccfg));
  return res;
}
