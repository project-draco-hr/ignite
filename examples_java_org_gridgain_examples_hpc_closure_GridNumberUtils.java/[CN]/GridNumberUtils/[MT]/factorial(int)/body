{
  A.ensure(num >= 0,"factorial is not defined for negative argument");
  BigInteger fact=new BigInteger("1");
  for (int i=2; i <= num; i++)   fact=fact.multiply(new BigInteger(String.valueOf(i)));
  return fact;
}
