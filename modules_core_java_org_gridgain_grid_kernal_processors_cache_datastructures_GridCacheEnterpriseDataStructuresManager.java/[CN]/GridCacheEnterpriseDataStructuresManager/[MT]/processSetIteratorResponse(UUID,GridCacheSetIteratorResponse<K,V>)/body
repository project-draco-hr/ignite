{
  log.info("Process response " + nodeId + " "+ res);
  res.nodeId(nodeId);
  SetIterator iter=iterMap.get(res.id());
  if (iter == null) {
    if (log.isDebugEnabled())     log.debug("");
  }
 else   iter.onResponse(res);
}
