{
  int len=doReadInt(raw);
  Date[] arr=new Date[len];
  for (int i=0; i < len; i++) {
    byte flag=doReadByte(raw);
    if (flag == NULL)     arr[i]=null;
 else {
      if (flag != DATE)       throw new PortableException("Invalid flag value: " + flag);
      arr[i]=doReadDate(raw);
    }
  }
  return arr;
}
