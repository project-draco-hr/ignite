{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(DOUBLE_ARR);
    if (flag == Flag.NORMAL)     return doReadDoubleArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
