{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(STRING_ARR);
    if (flag == Flag.NORMAL)     return doReadStringArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
