{
  int hPos=(raw ? rawOff : off) - 1;
  int len=doReadInt(raw);
  String[] arr=new String[len];
  setHandler(arr,hPos);
  for (int i=0; i < len; i++) {
    byte flag=doReadByte(raw);
    if (flag == NULL)     arr[i]=null;
 else {
      if (flag != STRING)       throw new PortableException("Invalid flag value: " + flag);
      arr[i]=doReadString(raw);
    }
  }
  return arr;
}
