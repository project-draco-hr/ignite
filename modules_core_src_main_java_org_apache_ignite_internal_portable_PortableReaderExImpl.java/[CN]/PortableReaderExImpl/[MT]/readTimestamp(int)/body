{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag != TIMESTAMP)     throw new PortableException("Invalid flag value: " + flag);
    return doReadTimestamp(false);
  }
 else   return null;
}
