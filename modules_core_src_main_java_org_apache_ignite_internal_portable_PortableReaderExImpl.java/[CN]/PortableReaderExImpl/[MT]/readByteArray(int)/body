{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(BYTE_ARR);
    if (flag == Flag.NORMAL)     return doReadByteArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
