{
  if (!cls.isEnum())   throw new PortableException("Class does not represent enum type: " + cls.getName());
  int ord=in.readInt();
  return ord >= 0 ? (Enum<?>)GridEnumCache.get(cls)[ord] : null;
}
