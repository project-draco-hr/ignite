{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(DECIMAL_ARR);
    if (flag == Flag.NORMAL)     return doReadDecimalArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
