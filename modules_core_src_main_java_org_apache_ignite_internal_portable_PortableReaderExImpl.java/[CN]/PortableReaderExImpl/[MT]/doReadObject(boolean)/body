{
  PortableReaderExImpl reader=new PortableReaderExImpl(ctx,in,raw ? rawOff : off,ldr,rCtx);
  Object obj=reader.deserialize();
  if (raw)   rawOff+=reader.len;
 else   off+=reader.len;
  return obj;
}
