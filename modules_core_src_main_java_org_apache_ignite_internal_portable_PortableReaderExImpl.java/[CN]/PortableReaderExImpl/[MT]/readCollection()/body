{
  byte flag=doReadByte(true);
  if (flag == NULL)   return null;
  if (flag != COL)   throw new PortableException("Invalid flag value: " + flag);
  return (Collection<T>)doReadCollection(true,true,null);
}
