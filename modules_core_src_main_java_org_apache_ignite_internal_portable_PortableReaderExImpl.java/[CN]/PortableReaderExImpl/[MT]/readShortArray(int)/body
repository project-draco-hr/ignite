{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(SHORT_ARR);
    if (flag == Flag.NORMAL)     return doReadShortArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
