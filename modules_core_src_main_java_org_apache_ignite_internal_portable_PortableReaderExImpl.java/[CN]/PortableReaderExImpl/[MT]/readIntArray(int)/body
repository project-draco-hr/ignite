{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag != INT_ARR)     throw new PortableException("Invalid flag value: " + flag);
    return doReadIntArray(false);
  }
 else   return null;
}
