{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(INT_ARR);
    if (flag == Flag.NORMAL)     return doReadIntArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
