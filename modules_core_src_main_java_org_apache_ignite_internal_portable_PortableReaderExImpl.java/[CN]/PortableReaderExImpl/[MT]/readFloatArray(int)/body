{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(FLOAT_ARR);
    if (flag == Flag.NORMAL)     return doReadFloatArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
