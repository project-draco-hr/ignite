{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag == HANDLE)     return readHandleField();
    if (flag != MAP_ENTRY)     throw new PortableException("Invalid flag value: " + flag);
    return doReadMapEntry(false,true);
  }
 else   return null;
}
