{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag != DECIMAL)     throw new PortableException("Invalid flag value: " + flag);
    return doReadDecimal(false);
  }
 else   return null;
}
