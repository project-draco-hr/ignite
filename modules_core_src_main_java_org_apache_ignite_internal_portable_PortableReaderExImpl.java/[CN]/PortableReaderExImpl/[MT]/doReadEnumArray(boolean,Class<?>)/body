{
  int len=doReadInt(raw);
  Object[] arr=(Object[])Array.newInstance(cls,len);
  for (int i=0; i < len; i++) {
    byte flag=doReadByte(raw);
    if (flag == NULL)     arr[i]=null;
 else     arr[i]=doReadEnum(raw,doReadClass(raw));
  }
  return arr;
}
