{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag != LONG)     throw new PortableException("Invalid flag value: " + flag);
    return doReadLong(false);
  }
 else   return null;
}
