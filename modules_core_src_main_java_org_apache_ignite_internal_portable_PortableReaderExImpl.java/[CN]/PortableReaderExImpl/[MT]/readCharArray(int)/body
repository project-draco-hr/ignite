{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(CHAR_ARR);
    if (flag == Flag.NORMAL)     return doReadCharArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
