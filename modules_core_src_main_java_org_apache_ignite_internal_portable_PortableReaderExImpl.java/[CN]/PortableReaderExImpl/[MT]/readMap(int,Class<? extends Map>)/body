{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(MAP);
    if (flag == Flag.NORMAL)     return doReadMap(true,cls);
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
