{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag == HANDLE)     return readHandleField();
    if (flag != MAP)     throw new PortableException("Invalid flag value: " + flag);
    return doReadMap(false,true,cls);
  }
 else   return null;
}
