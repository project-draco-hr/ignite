{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag != OBJ_ARR)     throw new PortableException("Invalid flag value: " + flag);
    return doReadObjectArray(false,true);
  }
 else   return null;
}
