{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(OBJ_ARR);
    if (flag == Flag.NORMAL)     return doReadObjectArray(true);
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
