{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag != DOUBLE)     throw new PortableException("Invalid flag value: " + flag);
    return doReadDouble(false);
  }
 else   return null;
}
