{
  if (hasField(fieldId)) {
    Flag flag=checkFlag(UUID_ARR);
    if (flag == Flag.NORMAL)     return doReadUuidArray();
 else     if (flag == Flag.HANDLE)     return readHandleField();
  }
  return null;
}
