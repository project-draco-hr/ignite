{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag == NULL)     return null;
    if (flag != ENUM_ARR)     throw new PortableException("Invalid flag value: " + flag);
    if (cls == null)     cls=doReadClass(false);
 else     doReadClass(false);
    return doReadEnumArray(false,cls);
  }
 else   return null;
}
