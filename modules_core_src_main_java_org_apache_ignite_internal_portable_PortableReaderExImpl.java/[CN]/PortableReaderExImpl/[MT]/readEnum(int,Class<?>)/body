{
  if (hasField(fieldId)) {
    if (checkFlag(ENUM) == Flag.NULL)     return null;
    if (cls == null)     cls=doReadClass();
 else     doReadClass();
    Object[] vals=GridEnumCache.get(cls);
    return (Enum<?>)vals[in.readInt()];
  }
 else   return null;
}
