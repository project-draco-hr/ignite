{
  if (dirs != null) {
    for (    IgniteFsPath dir : dirs)     ggfs.mkdirs(dir);
  }
  if (files != null) {
    for (    IgniteFsPath file : files) {
      OutputStream os=ggfs.create(file,true);
      os.close();
    }
  }
}
