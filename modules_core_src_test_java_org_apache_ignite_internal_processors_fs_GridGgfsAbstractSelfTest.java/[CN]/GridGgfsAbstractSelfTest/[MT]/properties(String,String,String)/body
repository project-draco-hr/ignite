{
  Map<String,String> props=new HashMap<>();
  if (username != null)   props.put(PROP_USER_NAME,username);
  if (grpName != null)   props.put(PROP_GROUP_NAME,grpName);
  if (perm != null)   props.put(PROP_PERMISSION,perm);
  return props;
}
