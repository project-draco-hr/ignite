{
  Lock l=lock.readLock();
  l.lock();
  try {
    if (intersection == null)     return find(filter.getSession(),null,null);
    return new GridH2Cursor(rowIterator(treeMap.findIntersectingKeys(getEnvelope(intersection,0))));
  }
  finally {
    l.unlock();
  }
}
