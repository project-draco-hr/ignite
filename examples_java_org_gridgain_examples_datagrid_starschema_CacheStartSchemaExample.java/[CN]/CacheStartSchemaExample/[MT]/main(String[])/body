{
  GridGain.start("examples/config/example-cache.xml");
  try {
    populateDimensions();
    populateFacts();
    queryStorePurchases();
    queryProductPurchases();
  }
  finally {
    GridGain.stop(false);
  }
}
