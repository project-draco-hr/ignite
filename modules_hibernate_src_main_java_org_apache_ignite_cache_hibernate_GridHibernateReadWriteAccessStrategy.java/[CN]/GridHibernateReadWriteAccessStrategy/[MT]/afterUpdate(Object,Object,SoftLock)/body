{
  try {
    TxContext ctx=txCtx.get();
    if (ctx != null) {
      cache.putx(key,val);
      unlock(ctx,key);
      return true;
    }
    return false;
  }
 catch (  IgniteCheckedException e) {
    rollbackCurrentTx();
    throw new CacheException(e);
  }
}
