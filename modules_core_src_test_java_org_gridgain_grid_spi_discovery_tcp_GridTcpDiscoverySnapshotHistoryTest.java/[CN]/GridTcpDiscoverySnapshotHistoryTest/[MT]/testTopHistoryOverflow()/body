{
  try {
    final Grid g1=startGrid(1);
    for (int i=2; i <= HISTORY_SIZE + 1; i++)     startGrid(i);
    assertNull(g1.topology(1));
    for (int i=2; i <= HISTORY_SIZE; i++)     assertEquals(i,g1.topology(i).size());
  }
  finally {
    stopAllGrids();
  }
}
