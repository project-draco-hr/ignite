{
  try {
    mgr.validateQuery(fut.query());
    if (fut.query() instanceof GridCacheFieldsQueryBase)     mgr.runFieldsQuery(localQueryInfo(fut,single,vis));
 else     mgr.runQuery(localQueryInfo(fut,single,vis));
  }
 catch (  Throwable e) {
    fut.onDone(e);
  }
}
