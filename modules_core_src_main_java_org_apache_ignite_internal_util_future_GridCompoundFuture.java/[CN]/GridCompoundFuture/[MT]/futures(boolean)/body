{
synchronized (futs) {
    Collection<IgniteInternalFuture<T>> res=new ArrayList<>(futs.size());
    for (    IgniteInternalFuture<T> fut : futs) {
      if (!pending || !fut.isDone())       res.add(fut);
    }
    return res;
  }
}
