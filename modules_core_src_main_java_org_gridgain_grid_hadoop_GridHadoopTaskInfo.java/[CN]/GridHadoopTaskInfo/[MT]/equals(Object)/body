{
  if (this == o)   return true;
  if (!(o instanceof GridHadoopTaskInfo))   return false;
  GridHadoopTaskInfo that=(GridHadoopTaskInfo)o;
  return attempt == that.attempt && taskNum == that.taskNum && jobId.equals(that.jobId) && nodeId.equals(that.nodeId) && type == that.type;
}
