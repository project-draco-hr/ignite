{
  if (!in.hasArray())   return new String(doReadByteArray(in),UTF_8);
  int strLen=in.readInt();
  int pos=in.position();
  String res=new String(in.array(),pos,strLen,UTF_8);
  in.position(pos + strLen);
  return res;
}
