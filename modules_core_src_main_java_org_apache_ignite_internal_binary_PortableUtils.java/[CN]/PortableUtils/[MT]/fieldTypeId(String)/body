{
  for (int i=0; i < FIELD_TYPE_NAMES.length; i++) {
    String typeName0=FIELD_TYPE_NAMES[i];
    if (typeName.equals(typeName0))     return i;
  }
  throw new IllegalArgumentException("Invalid metadata type name: " + typeName);
}
