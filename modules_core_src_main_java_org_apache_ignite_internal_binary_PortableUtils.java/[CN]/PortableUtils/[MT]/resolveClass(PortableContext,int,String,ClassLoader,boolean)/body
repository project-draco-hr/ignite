{
  Class cls;
  if (typeId == GridPortableMarshaller.OBJECT_TYPE_ID)   return Object.class;
  if (typeId != GridPortableMarshaller.UNREGISTERED_TYPE_ID)   cls=ctx.descriptorForTypeId(true,typeId,ldr,deserialize).describedClass();
 else {
    try {
      cls=U.forName(clsName,ldr);
    }
 catch (    ClassNotFoundException e) {
      throw new BinaryInvalidTypeException("Failed to load the class: " + clsName,e);
    }
    ctx.descriptorForClass(cls,true);
  }
  return cls;
}
