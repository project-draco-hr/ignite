{
  byte flag=in.readByte();
  if (flag != GridPortableMarshaller.STRING)   throw new BinaryObjectException("Failed to read class name [position=" + (in.position() - 1) + ']');
  return doReadString(in);
}
