{
  ctx.io().removeMessageListener(topic);
  if (dfltExecSvc)   execSvc.shutdownNow();
 else {
    if (c.isExecutorServiceShutdown())     execSvc.shutdown();
  }
  U.stopLifecycleAware(log,lifecycleAwares());
}
