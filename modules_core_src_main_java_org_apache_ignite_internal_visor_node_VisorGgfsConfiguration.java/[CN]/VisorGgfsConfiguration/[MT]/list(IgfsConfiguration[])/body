{
  if (ggfss == null)   return Collections.emptyList();
  final Collection<VisorGgfsConfiguration> cfgs=new ArrayList<>(ggfss.length);
  for (  IgfsConfiguration ggfs : ggfss)   cfgs.add(from(ggfs));
  return cfgs;
}
