{
  for (int i=0; i < gridCount(); i++) {
    if (i != nearIdx)     for (    String key : primaryKeysForCache(jcache(i),3))     jcache(i).put(key,1);
  }
  jcache().clear();
  for (int i=0; i < gridCount(); i++)   assertTrue(String.valueOf(jcache(i)),jcache(i).localSize() == 0);
}
