{
  for (int i=0; i < gridCount(); i++) {
    if (i != nearIdx)     for (    String key : primaryKeysForCache(cache(i),3))     cache(i).put(key,1);
  }
  cache().globalClearAll();
  for (int i=0; i < gridCount(); i++)   assertTrue(String.valueOf(cache(i).entrySet()),cache(i).isEmpty());
}
