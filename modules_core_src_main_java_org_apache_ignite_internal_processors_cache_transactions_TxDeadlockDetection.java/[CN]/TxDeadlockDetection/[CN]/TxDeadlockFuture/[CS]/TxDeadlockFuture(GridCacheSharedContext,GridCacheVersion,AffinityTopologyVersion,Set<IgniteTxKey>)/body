{
  this.cctx=cctx;
  this.txId=txId;
  this.topVer=topVer;
  this.keys=keys;
  if (DEADLOCK_TIMEOUT > 0) {
    timeoutObj=new DeadlockTimeoutObject();
    cctx.time().addTimeoutObject(timeoutObj);
  }
}
