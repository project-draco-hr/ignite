{
  try {
    if (obj == null)     stmt.setNull(idx,Types.VARCHAR);
 else     stmt.setObject(idx,obj);
  }
 catch (  SQLException e) {
    throw new GridException("Failed to bind parameter [idx=" + idx + ", obj="+ obj+ ']',e);
  }
}
