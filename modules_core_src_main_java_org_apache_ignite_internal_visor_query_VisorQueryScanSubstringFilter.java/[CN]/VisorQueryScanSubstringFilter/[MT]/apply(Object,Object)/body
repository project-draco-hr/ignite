{
  String k=key.toString();
  String v=val.toString();
  if (caseSensitive)   return k.contains(ptrn) || v.contains(ptrn);
  return k.toUpperCase().contains(ptrn) || v.toUpperCase().contains(ptrn);
}
