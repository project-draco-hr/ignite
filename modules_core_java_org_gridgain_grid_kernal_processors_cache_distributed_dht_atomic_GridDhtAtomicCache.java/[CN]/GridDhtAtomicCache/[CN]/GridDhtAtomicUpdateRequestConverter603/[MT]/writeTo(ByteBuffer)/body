{
  commState.setBuffer(buf);
switch (commState.idx) {
case 13:
    if (!commState.putInt(-1))     return false;
  commState.idx++;
case 14:
if (!commState.putInt(-1)) return false;
commState.idx++;
}
return true;
}
