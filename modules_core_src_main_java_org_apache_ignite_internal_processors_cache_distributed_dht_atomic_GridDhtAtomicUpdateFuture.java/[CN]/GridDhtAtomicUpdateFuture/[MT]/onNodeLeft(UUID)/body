{
  boolean res=registerResponse(nodeId);
  if (res && msgLog.isDebugEnabled()) {
    msgLog.debug("DTH update fut, node left [futId=" + futVer + ", writeVer="+ writeVer+ ", node="+ nodeId+ ']');
  }
  return res;
}
