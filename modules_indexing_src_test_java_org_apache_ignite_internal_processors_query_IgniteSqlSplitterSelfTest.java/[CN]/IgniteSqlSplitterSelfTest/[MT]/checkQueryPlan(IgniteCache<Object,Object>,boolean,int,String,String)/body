{
  checkQueryPlan(cache,enforceJoinOrder,expBatchedJoins,new SqlFieldsQuery(sql),expText);
  checkQueryPlan(cache,enforceJoinOrder,expBatchedJoins,new SqlFieldsQuery("select * from (" + sql + ")"),expText);
}
