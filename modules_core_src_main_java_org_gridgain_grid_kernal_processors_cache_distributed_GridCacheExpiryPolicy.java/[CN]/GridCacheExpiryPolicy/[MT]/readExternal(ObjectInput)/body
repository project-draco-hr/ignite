{
  byte flags=in.readByte();
  if ((flags & CREATE_TTL_MASK) != 0)   forCreate=new Duration(TimeUnit.MILLISECONDS,in.readLong());
  if ((flags & UPDATE_TTL_MASK) != 0)   forUpdate=new Duration(TimeUnit.MILLISECONDS,in.readLong());
}
