{
  if (date == null)   out.writeByte(GridPortableMarshaller.NULL);
 else {
    out.unsafeEnsure(1 + 8);
    out.unsafeWriteByte(GridPortableMarshaller.DATE);
    out.unsafeWriteLong(date.getTime());
  }
}
