{
  if (ts == null)   out.writeByte(GridPortableMarshaller.NULL);
 else {
    out.unsafeEnsure(1 + 8 + 4);
    out.unsafeWriteByte(GridPortableMarshaller.TIMESTAMP);
    out.unsafeWriteLong(ts.getTime());
    out.unsafeWriteInt(ts.getNanos() % 1000000);
  }
}
