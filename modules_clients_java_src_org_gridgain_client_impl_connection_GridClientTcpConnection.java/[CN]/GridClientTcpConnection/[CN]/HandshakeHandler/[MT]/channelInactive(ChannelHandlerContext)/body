{
  GridClientException ex=new GridClientConnectionResetException("Connection was unexpectedly closed.");
  if (!handshake.isDone())   handshake.setFailure(ex);
  close(FAILED,false,ex);
  ctx.fireChannelInactive();
}
