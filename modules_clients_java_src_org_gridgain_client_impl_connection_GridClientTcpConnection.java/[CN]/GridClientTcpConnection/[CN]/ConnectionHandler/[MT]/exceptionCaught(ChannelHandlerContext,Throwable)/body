{
  if (log.isLoggable(Level.FINE))   log.log(Level.FINE,"Exception occurred in TCP channel: " + serverAddress(),cause);
  cause.printStackTrace();
  GridClientTcpConnection.this.close(FAILED,false,cause);
}
