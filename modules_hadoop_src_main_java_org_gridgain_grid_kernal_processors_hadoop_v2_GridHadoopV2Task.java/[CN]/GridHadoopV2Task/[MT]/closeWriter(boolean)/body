{
  RecordWriter writer=hadoopCtx.writer();
  try {
    if (writer != null)     writer.close(hadoopCtx);
  }
 catch (  Throwable e) {
    if (suppressE)     U.error(log(),"Error on close writer of " + info(),e);
 else     throw e;
  }
}
