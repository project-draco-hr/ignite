{
  checkClosed(in,len);
  long startTime=System.nanoTime();
  if (buf != null && buf.position() > 0)   sendData(true);
  try {
    storeDataBlocks(in,len);
  }
 catch (  IgniteCheckedException e) {
    throw new IOException(e.getMessage(),e);
  }
  time+=System.nanoTime() - startTime;
}
