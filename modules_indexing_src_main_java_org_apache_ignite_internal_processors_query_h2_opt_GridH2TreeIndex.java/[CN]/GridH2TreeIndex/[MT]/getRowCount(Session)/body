{
  GridIndexingQueryFilter f=filters.get();
  if (f == null || f.forSpace(((GridH2Table)getTable()).spaceName()) == null)   return treeForRead().size();
  Iterator<GridH2Row> iter=doFind(null,false,null);
  long size=0;
  while (iter.hasNext()) {
    iter.next();
    size++;
  }
  return size;
}
