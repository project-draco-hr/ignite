{
  Runnable old=data.put(fieldName,new Runnable(){
    @Override public void run(){
      doWriteByte(val);
    }
  }
);
  if (old != null)   throw new GridPortableException("Duplicate field name: " + fieldName);
}
