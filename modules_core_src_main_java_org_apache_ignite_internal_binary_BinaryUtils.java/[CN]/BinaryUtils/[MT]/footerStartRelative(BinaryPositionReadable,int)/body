{
  short flags=in.readShortPositioned(start + GridBinaryMarshaller.FLAGS_POS);
  if (hasSchema(flags))   return in.readIntPositioned(start + GridBinaryMarshaller.SCHEMA_OR_RAW_OFF_POS);
 else   return length(in,start);
}
