{
  commState.setBuffer(buf);
switch (commState.idx) {
case 0:
    byte[] bytes0=commState.getByteArrayClient(4);
  if (bytes0 == BYTE_ARR_NOT_READ)   return false;
bytes=bytes0;
commState.idx++;
}
return true;
}
