{
  GridClientTaskResultBean res=new GridClientTaskResultBean();
  res.setId(bean.getTaskId());
  res.setFinished(bean.getFinished());
  if (bean.hasError())   res.setError(bean.getError());
  if (bean.hasResultBean())   res.setResult(unwrapObject(bean.getResultBean()));
  return res;
}
