{
  if (partIt != null) {
    while (partIt.hasNext()) {
      GridDhtCacheEntry next=partIt.next();
      if (next.isInternal() || !next.visitable(CU.empty0()))       continue;
      entry=next.wrapLazyValue();
      return;
    }
  }
  entry=null;
}
