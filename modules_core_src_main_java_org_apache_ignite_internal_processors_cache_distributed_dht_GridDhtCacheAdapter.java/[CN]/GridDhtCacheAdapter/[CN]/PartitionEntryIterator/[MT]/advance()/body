{
  if (partIt != null) {
    while (partIt.hasNext()) {
      GridDhtCacheEntry<K,V> next=partIt.next();
      if (next.isInternal() || !next.visitable(CU.<K,V>empty()))       continue;
      entry=next.wrapLazyValue();
      return;
    }
  }
  entry=null;
}
