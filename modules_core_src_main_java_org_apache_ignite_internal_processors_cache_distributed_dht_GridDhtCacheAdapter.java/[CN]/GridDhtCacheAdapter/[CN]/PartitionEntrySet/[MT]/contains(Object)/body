{
  if (!(o instanceof Cache.Entry))   return false;
  Cache.Entry<K,V> entry=(Cache.Entry<K,V>)o;
  return partId == ctx.affinity().partition(entry.getKey()) && F.eq(entry.getValue(),peek(entry.getKey()));
}
