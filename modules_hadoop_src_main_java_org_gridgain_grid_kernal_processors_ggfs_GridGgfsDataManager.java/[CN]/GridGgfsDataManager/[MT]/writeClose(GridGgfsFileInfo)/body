{
  WriteCompletionFuture fut=pendingWrites.get(fileInfo.id());
  if (fut != null)   fut.markWaitingLastAck();
 else {
    if (log.isDebugEnabled())     log.debug("Failed to find write completion future for file in pending write map (most likely it was " + "failed): " + fileInfo);
  }
}
