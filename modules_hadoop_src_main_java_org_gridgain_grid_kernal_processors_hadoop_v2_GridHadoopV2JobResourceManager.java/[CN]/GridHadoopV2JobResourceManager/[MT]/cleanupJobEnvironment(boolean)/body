{
  if (!clsPath.isEmpty()) {
    ClassLoaderWrapper jobLdr=(ClassLoaderWrapper)ctx.getJobConf().getClassLoader();
    jobLdr.destroy();
  }
  if (deleteJobLocDir && jobLocDir.exists())   U.delete(jobLocDir);
}
