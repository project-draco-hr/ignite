{
  try {
    String old=cache.putIfAbsent(typeId,clsName);
    if (old != null && !old.equals(clsName))     throw new IgniteException("Collision.");
  }
 catch (  IgniteCheckedException e) {
    throw U.convertException(e);
  }
}
