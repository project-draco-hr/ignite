{
  mgr.addSpiAttributes(attrs);
  if (mgr instanceof GridDiscoveryManager)   ((GridDiscoveryManager)mgr).setNodeAttributes(attrs,IgniteProduct.VER);
  ctx.add(mgr);
  try {
    mgr.start();
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteCheckedException("Failed to start manager: " + mgr,e);
  }
}
