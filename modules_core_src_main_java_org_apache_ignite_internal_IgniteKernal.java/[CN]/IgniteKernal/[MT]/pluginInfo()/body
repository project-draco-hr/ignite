{
  Collection<? extends PluginConfiguration> pluginsCfg=cfg.getPluginConfigurations();
  if (pluginsCfg.size() == 0)   return PLUGIN_INFO + "none";
  String info=PLUGIN_INFO + NL;
  for (  PluginConfiguration cfg : pluginsCfg)   info+=">>> " + cfg.name() + " "+ cfg.version()+ NL;
  return info;
}
