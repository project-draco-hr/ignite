{
  mgr.addSpiAttributes();
  if (mgr instanceof GridDiscoveryManager)   ((GridDiscoveryManager)mgr).setNodeAttributes(VER);
  ctx.add(mgr);
  try {
    mgr.start();
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteCheckedException("Failed to start manager: " + mgr,e);
  }
}
