{
  guard();
  try {
    IgniteFs fs=ctx.igfs().igfs(name);
    if (fs == null)     throw new IllegalArgumentException("IgniteFs is not configured: " + name);
    return fs;
  }
  finally {
    unguard();
  }
}
