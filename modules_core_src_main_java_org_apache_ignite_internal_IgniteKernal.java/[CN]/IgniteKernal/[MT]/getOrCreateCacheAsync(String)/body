{
  guard();
  try {
    if (ctx.cache().cache(cacheName) == null)     return ctx.cache().getOrCreateFromTemplate(cacheName);
    return new GridFinishedFuture<>();
  }
  finally {
    unguard();
  }
}
