{
  T res=asyncInstance;
  if (res == null) {
    res=createAsyncInstance();
synchronized (mux) {
      if (asyncInstance != null)       return asyncInstance;
      asyncInstance=res;
    }
  }
  return res;
}
