{
  super.onKernalStart();
  jobTracker=ctx.jobTracker();
  exec=new GridHadoopExecutorService(log,ctx.kernalContext().gridName(),ctx.configuration().getMaxParallelTasks(),ctx.configuration().getMaxTaskQueueSize());
}
