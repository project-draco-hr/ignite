{
  if (skipQuery(cache,PERSON_CACHE_NAME,ACC_CACHE_NAME))   return;
  qry="checkPersonAccountCrossJoin";
  String sql="select p.name " + "from \"" + PERSON_CACHE_NAME + "\".Person p "+ "cross join \""+ ACC_CACHE_NAME+ "\".Account a";
  compareQueryRes0(cache,sql,distributedJoins(),new Object[0],Ordering.RANDOM);
}
