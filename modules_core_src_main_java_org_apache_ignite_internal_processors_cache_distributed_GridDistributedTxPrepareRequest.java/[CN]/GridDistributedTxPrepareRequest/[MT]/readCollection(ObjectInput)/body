{
  List<IgniteTxEntry> col=null;
  int size=in.readInt();
  if (size != -1) {
    col=new ArrayList<>(size);
    for (int i=0; i < size; i++)     col.add((IgniteTxEntry)in.readObject());
  }
  return col == null ? Collections.<IgniteTxEntry<K,V>>emptyList() : col;
}
