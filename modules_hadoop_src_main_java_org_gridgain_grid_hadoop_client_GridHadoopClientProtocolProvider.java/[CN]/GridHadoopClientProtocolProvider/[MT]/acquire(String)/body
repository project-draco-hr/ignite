{
  ClientMap map=clientMap(addr);
synchronized (map) {
    Client cli=map.get(addr);
    if (cli == null) {
      cli=new Client(createClient(addr));
      map.put(addr,cli);
    }
 else     cli.acquire();
    return cli.client();
  }
}
