{
  int cpus=0;
  for (  Collection<GridNode> nodes : neighborhood.values()) {
    GridNode first=F.first(nodes);
    if (first != null)     cpus+=first.metrics().getTotalCpus();
  }
  return cpus;
}
