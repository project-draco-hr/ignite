{
  lock.readLock();
  try {
    Collection<E> evts=dequeue0(cnt);
    if (!evts.isEmpty() && idxs != null) {
      for (      E evt : evts)       updateIndexes(evt,true);
    }
    return evts;
  }
  finally {
    lock.readUnlock();
  }
}
