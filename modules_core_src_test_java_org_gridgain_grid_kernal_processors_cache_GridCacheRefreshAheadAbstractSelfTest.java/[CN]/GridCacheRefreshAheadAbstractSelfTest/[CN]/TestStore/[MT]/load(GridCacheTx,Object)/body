{
  if (trackLoads) {
    wasAsyncLoad=wasAsyncLoad || !testThread.equals(Thread.currentThread());
    if (wasAsyncLoad)     latch.countDown();
    info("Load call was tracked on store: " + this);
  }
 else   info("Load call was not tracked on store: " + this);
  return null;
}
