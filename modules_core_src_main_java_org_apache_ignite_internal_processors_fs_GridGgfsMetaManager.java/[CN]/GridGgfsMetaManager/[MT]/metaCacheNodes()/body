{
  if (busyLock.enterBusy()) {
    try {
      return ggfsCtx.kernalContext().discovery().cacheNodes(metaCache.name(),-1);
    }
  finally {
      busyLock.leaveBusy();
    }
  }
 else   throw new IllegalStateException("Failed to get meta cache nodes because Grid is stopping.");
}
