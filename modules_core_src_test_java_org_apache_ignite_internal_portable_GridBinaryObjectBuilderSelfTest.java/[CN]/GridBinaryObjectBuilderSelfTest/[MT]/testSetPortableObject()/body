{
  BinaryObject portableObj=builder(TestObjectContainer.class.getName()).setField("foo",toPortable(new TestObjectAllTypes())).build();
  assertTrue(portableObj.<TestObjectContainer>deserialize().foo instanceof TestObjectAllTypes);
}
