{
  res.nodeId(nodeId);
  GridCacheSetIterator iter=setIterMap.get(res.id());
  if (iter == null) {
    if (log.isDebugEnabled())     log.debug("Received response for unknown set iterator [res=" + res + ']');
  }
 else   iter.onResponse(res);
}
