{
  super(cctx,keys,readThrough,forcePrimary,subjId,taskName,deserializePortable,expiryPlc,skipVals,canRemap,needVer,keepCacheObjects);
  this.topVer=topVer;
  ver=cctx.versions().next();
  if (log == null)   log=U.logger(cctx.kernalContext(),logRef,GridPartitionedGetFuture.class);
}
