{
  if (!canRemap) {
    List<ClusterNode> nodes=cctx.affinity().nodes(key,topVer);
    for (    ClusterNode node : nodes) {
      if (cctx.discovery().alive(node))       return node;
    }
    return null;
  }
 else   return cctx.affinity().primary(key,topVer);
}
