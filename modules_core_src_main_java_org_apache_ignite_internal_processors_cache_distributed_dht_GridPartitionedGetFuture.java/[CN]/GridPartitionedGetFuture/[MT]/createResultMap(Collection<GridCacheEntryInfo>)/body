{
  int keysSize=infos.size();
  if (keysSize != 0) {
    Map<K,V> map=new GridLeanMap<>(keysSize);
    for (    GridCacheEntryInfo info : infos)     cctx.addResult(map,info.key(),info.value(),skipVals,false,deserializePortable);
    return map;
  }
  return Collections.emptyMap();
}
