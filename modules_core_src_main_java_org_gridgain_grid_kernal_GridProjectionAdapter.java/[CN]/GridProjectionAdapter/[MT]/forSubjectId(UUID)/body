{
  if (subjId == null)   return this;
  guard();
  try {
    return ids != null ? new GridProjectionAdapter(this,ctx,subjId,ids) : new GridProjectionAdapter(this,ctx,subjId,p);
  }
  finally {
    unguard();
  }
}
