{
  A.notEmpty(nodes,"nodes");
  guard();
  try {
    Set<UUID> nodeIds=new HashSet<>(nodes.size());
    for (    GridNode n : nodes)     if (contains(n))     nodeIds.add(n.id());
    return new GridProjectionAdapter(this,ctx,subjId,nodeIds);
  }
  finally {
    unguard();
  }
}
