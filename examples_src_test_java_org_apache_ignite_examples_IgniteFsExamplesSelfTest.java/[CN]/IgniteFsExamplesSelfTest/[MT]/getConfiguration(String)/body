{
  String cfgPath=gridName == null ? (U.isWindows() ? GGFS_LOOPBACK_CFG : GGFS_SHMEM_CFG) : GGFS_NO_ENDPOINT_CFG;
  IgniteConfiguration cfg=GridGainEx.loadConfiguration(cfgPath).get1();
  cfg.setGridName(gridName);
  return cfg;
}
