{
  if (!enterBusy())   throw new IllegalStateException("Failed to rebuild indexes (grid is stopping).");
  try {
    return idxMgr.rebuildIndexes(spi,space,valType.getName());
  }
  finally {
    leaveBusy();
  }
}
