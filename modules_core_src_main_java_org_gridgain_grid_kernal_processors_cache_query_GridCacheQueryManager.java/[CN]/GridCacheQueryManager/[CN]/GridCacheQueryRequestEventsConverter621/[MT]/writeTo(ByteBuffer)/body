{
  commState.setBuffer(buf);
switch (commState.idx) {
case 0:
    if (!commState.putUuid(null))     return false;
  commState.idx++;
case 3:
if (!commState.putInt(0)) return false;
commState.idx++;
}
return true;
}
