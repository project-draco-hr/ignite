{
  return withReconnectHandling(new FileSystemClosure<Collection<IgniteFsPath>>(){
    @Override public Collection<IgniteFsPath> apply(    GridGgfsHadoopEx hadoop,    GridGgfsHandshakeResponse hndResp) throws GridException, IOException {
      return hadoop.listPaths(path);
    }
  }
,path);
}
