{
  return withReconnectHandling(new FileSystemClosure<Collection<GridGgfsPath>>(){
    @Override public Collection<GridGgfsPath> apply(    GridGgfsHadoopEx hadoop,    GridGgfsHandshakeResponse hndResp) throws GridException, IOException {
      return hadoop.listPaths(path);
    }
  }
,path);
}
