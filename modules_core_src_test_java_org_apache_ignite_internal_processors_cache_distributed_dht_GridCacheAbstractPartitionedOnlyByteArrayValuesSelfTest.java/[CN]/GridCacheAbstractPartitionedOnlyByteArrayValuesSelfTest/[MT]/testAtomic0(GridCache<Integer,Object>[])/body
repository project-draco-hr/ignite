{
  byte[] val=wrap(1);
  for (  GridCache<Integer,Object> cache : caches) {
    cache.put(KEY_1,val);
    for (    GridCache<Integer,Object> cacheInner : caches)     assertArrayEquals(val,(byte[])cacheInner.get(KEY_1));
    cache.remove(KEY_1);
    assertNull(cache.get(KEY_1));
  }
}
