{
  if (val != null) {
    if (clsName != null)     ctx.writeArray(writer,GridBinaryMarshaller.ENUM_ARR,(Object[])val,clsName);
 else     ctx.writeArray(writer,GridBinaryMarshaller.ENUM_ARR,(Object[])val,compTypeId);
    return;
  }
  writer.write(reader.array(),valOff,len);
}
