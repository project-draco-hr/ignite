{
  if (check || (periodicCheck && filterOn.get())) {
    if (msg instanceof GridIoMessage && ((GridIoMessage)msg).message() instanceof CacheContinuousQueryBatchAck)     fail.set(true);
  }
  super.notifyListener(sndId,msg,msgC);
}
