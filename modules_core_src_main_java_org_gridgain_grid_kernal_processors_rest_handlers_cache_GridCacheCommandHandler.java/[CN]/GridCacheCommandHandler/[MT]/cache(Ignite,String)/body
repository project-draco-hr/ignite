{
  GridCache<Object,Object> cache=ignite.cache(cacheName);
  if (cache == null)   throw new GridException("Failed to find cache for given cache name (null for default cache): " + cacheName);
  return (GridCacheProjectionEx<Object,Object>)cache;
}
