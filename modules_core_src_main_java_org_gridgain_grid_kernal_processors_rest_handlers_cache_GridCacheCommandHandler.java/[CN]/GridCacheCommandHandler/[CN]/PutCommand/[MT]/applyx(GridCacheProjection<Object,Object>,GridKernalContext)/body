{
  if (ttl != null) {
    GridCacheEntry<Object,Object> entry=c.entry(key);
    if (entry != null) {
      entry.timeToLive(ttl);
      return entry.setxAsync(val);
    }
 else     return new GridFinishedFuture<Object>(ctx,false);
  }
 else   return c.putxAsync(key,val);
}
