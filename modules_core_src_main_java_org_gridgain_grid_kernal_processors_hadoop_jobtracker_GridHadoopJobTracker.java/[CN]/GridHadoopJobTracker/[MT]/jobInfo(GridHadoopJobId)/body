{
  JobLocalState state=activeJobs.get(jobId);
  if (state != null)   return state.job.info();
  GridHadoopJobMetadata meta=jobMetaPrj.get(jobId);
  if (meta != null)   return meta.jobInfo();
  return null;
}
