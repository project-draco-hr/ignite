{
  Collection<GridNode> nodes=aff.nodes(aff.partition(key),topVer);
  if (F.isEmpty(nodes))   throw new GridException("Failed to get affinity nodes [aff=" + aff + ", key="+ key+ ", topVer="+ topVer+ ']');
  return nodes.iterator().next();
}
