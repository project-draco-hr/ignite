{
  try {
    return GridAffinityProcessor.this.affinityCache(cacheName,topologyVersion()).assignment.get(partition(key));
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException(e);
  }
}
