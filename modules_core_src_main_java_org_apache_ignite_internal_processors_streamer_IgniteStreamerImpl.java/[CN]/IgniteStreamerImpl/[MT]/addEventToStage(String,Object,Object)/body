{
  A.notNull(stageName,"stageName");
  A.notNull(evt,"evt");
  if (!F.isEmpty(evts))   addEventsToStage(stageName,F.concat(false,evt,Arrays.asList(evts)));
 else   addEventsToStage(stageName,Collections.singleton(evt));
}
