{
  for (  StreamerStage s : c.getStages())   ctx.resource().injectGeneric(s);
  if (router == null)   router=new StreamerLocalEventRouter();
  ctx.resource().injectGeneric(router);
  for (  StreamerWindow w : c.getWindows())   ctx.resource().injectGeneric(w);
}
