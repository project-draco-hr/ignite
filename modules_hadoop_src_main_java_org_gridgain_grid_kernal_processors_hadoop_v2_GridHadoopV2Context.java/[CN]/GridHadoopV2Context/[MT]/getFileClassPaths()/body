{
  ArrayList<String> list=(ArrayList<String>)cfg.getStringCollection(MRJobConfig.CLASSPATH_FILES);
  if (list.size() == 0)   return null;
  Path[] paths=new Path[list.size()];
  for (int i=0; i < list.size(); i++)   paths[i]=new Path(list.get(i));
  return paths;
}
