{
  A.notNull(key,"key",val,"val");
  ctx.denyOnLocalRead();
  if (ctx.portableEnabled())   val=(V)ctx.marshalToPortable(val);
  return (Boolean)updateAllInternal(DELETE,Collections.singleton(key),null,null,expiryPerCall(),false,false,ctx.equalsPeekArray(val),ctx.writeThrough());
}
