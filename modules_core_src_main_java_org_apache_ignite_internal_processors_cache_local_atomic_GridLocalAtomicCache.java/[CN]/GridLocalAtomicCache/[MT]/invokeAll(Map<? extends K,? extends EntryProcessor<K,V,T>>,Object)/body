{
  A.notNull(map,"map");
  if (keyCheck)   validateCacheKeys(map.keySet());
  ctx.denyOnLocalRead();
  return (Map<K,EntryProcessorResult<T>>)updateAllInternal(TRANSFORM,map.keySet(),map.values(),args,expiryPerCall(),true,false,null,ctx.writeThrough());
}
