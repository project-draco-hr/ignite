{
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return false;
    writer.writeFloatArray(name,arr);
    hdrDone=true;
  }
 else   stream.writeFloatArray(arr);
  return lastWritten();
}
