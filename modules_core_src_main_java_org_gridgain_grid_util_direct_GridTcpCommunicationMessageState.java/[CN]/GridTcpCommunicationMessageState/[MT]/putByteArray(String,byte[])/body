{
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return false;
    writer.writeByteArray(name,arr);
    hdrDone=true;
  }
 else   stream.writeByteArray(arr);
  return lastWritten();
}
