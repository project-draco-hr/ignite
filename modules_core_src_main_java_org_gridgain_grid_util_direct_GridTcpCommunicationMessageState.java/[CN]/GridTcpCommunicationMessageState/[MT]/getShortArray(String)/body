{
  short[] arr;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return SHORT_ARR_NOT_READ;
    arr=reader.readShortArray(name);
    hdrDone=true;
  }
 else   arr=stream.readShortArray(-1);
  if (arr != SHORT_ARR_NOT_READ)   hdrDone=false;
  return arr;
}
