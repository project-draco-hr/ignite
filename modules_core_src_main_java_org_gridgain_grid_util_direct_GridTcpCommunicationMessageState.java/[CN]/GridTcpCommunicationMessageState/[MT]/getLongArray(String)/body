{
  long[] arr;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return LONG_ARR_NOT_READ;
    arr=reader.readLongArray(name);
    hdrDone=true;
  }
 else   arr=stream.readLongArray(-1);
  if (arr != LONG_ARR_NOT_READ)   hdrDone=false;
  return arr;
}
