{
  char[] arr;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return CHAR_ARR_NOT_READ;
    arr=reader.readCharArray(name);
    hdrDone=true;
  }
 else   arr=stream.readCharArray(-1);
  if (arr != CHAR_ARR_NOT_READ)   hdrDone=false;
  return arr;
}
