{
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return false;
    writer.writeShortArray(name,arr);
    hdrDone=true;
  }
 else   stream.writeShortArray(arr);
  return lastWritten();
}
