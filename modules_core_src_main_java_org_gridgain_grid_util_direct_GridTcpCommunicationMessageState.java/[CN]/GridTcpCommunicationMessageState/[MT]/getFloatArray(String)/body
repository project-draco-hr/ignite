{
  float[] arr;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return FLOAT_ARR_NOT_READ;
    arr=reader.readFloatArray(name);
    hdrDone=true;
  }
 else   arr=stream.readFloatArray(-1);
  if (arr != FLOAT_ARR_NOT_READ)   hdrDone=false;
  return arr;
}
