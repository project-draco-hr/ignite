{
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return false;
    writer.writeBooleanArray(name,arr);
    if (arr == null)     return true;
    hdrDone=true;
  }
 else   stream.writeBooleanArray(arr);
  return lastWritten();
}
