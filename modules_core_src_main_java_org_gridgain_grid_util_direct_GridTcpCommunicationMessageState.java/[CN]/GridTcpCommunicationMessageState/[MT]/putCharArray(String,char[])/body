{
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return false;
    writer.writeCharArray(name,arr);
    hdrDone=true;
  }
 else   stream.writeCharArray(arr);
  return lastWritten();
}
