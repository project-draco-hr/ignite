{
  int[] arr;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return INT_ARR_NOT_READ;
    arr=reader.readIntArray(name);
    hdrDone=true;
  }
 else   arr=stream.readIntArray(-1);
  if (arr != INT_ARR_NOT_READ)   hdrDone=false;
  return arr;
}
