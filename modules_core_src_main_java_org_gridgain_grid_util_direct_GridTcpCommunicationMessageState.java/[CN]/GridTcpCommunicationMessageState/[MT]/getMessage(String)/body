{
  GridTcpCommunicationMessageAdapter msg;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN)     return MSG_NOT_READ;
    msg=reader.readObject(name);
    hdrDone=true;
  }
 else   msg=stream.readMessage();
  if (msg != MSG_NOT_READ)   hdrDone=false;
  return msg;
}
