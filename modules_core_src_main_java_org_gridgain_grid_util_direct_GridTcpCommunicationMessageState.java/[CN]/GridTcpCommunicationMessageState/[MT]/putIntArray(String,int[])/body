{
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return false;
    writer.writeIntArray(name,arr);
    if (arr == null)     return true;
    hdrDone=true;
  }
 else   stream.writeIntArray(arr);
  return lastWritten();
}
