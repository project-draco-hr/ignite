{
  byte[] arr;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return BYTE_ARR_NOT_READ;
    arr=reader.readByteArray(name);
    hdrDone=true;
  }
 else   arr=stream.readByteArray(-1);
  if (arr != BYTE_ARR_NOT_READ)   hdrDone=false;
  return arr;
}
