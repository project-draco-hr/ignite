{
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 1)     return false;
    writer.writeObject(name,msg);
    hdrDone=true;
  }
 else   stream.writeMessage(msg);
  return lastWritten();
}
