{
  double[] arr;
  if (!hdrDone) {
    if (stream.remaining() < FIELD_HDR_LEN + 4)     return DOUBLE_ARR_NOT_READ;
    arr=reader.readDoubleArray(name);
    hdrDone=true;
  }
 else   arr=stream.readDoubleArray(-1);
  if (arr != DOUBLE_ARR_NOT_READ)   hdrDone=false;
  return arr;
}
