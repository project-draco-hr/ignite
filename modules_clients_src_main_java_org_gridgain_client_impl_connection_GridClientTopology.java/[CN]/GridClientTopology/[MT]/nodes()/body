{
  lock.readLock().lock();
  try {
    if (lastError != null)     throw new GridClientDisconnectedException("Latest topology update failed.",lastError);
    return Collections.unmodifiableCollection(nodes.values());
  }
  finally {
    lock.readLock().unlock();
  }
}
