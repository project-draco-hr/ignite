{
  enter();
  try {
    IgniteInternalFuture rollbackFut=cctx.rollbackTxAsync(tx);
    if (async)     asyncRes=new IgniteFutureImpl(rollbackFut);
 else     rollbackFut.get();
  }
  finally {
    leave();
  }
}
