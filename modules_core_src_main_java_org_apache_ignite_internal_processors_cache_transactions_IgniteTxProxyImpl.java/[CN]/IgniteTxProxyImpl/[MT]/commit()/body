{
  enter();
  try {
    IgniteInternalFuture<IgniteTxEx> commitFut=cctx.commitTxAsync(tx);
    if (async)     asyncRes=new IgniteFutureImpl(commitFut);
 else     commitFut.get();
  }
 catch (  IgniteCheckedException e) {
    throw U.convertException(e);
  }
 finally {
    leave();
  }
}
