{
  commState.setBuffer(buf);
switch (commState.idx) {
case 0:
    byte[] dataBytes0=commState.getByteArray();
  if (dataBytes0 == BYTE_ARR_NOT_READ)   return false;
dataBytes=dataBytes0;
commState.idx++;
case 1:
UUID routineId0=commState.getUuid();
if (routineId0 == UUID_NOT_READ) return false;
routineId=routineId0;
commState.idx++;
case 2:
Object type0=commState.getEnum(GridContinuousMessageType.class);
if (type0 == ENUM_NOT_READ) return false;
type=(GridContinuousMessageType)type0;
commState.idx++;
}
return true;
}
