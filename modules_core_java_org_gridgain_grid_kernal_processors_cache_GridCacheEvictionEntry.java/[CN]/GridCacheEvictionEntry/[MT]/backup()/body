{
  GridCacheContext<K,V> ctx=cached.context();
  return ctx.config().getCacheMode() == PARTITIONED && ctx.affinity().backups(cached.key(),ctx.affinity().affinityTopologyVersion()).contains(ctx.localNode());
}
