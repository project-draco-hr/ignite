{
synchronized (mux) {
    if (lsnrs == null)     return;
    if (lsnr == null || lsnr.length == 0)     lsnrs.clear();
 else {
      for (Iterator<IgniteInClosure<? super IgniteFuture<R>>> it=lsnrs.iterator(); it.hasNext(); ) {
        IgniteInClosure<? super IgniteFuture<R>> l1=it.next();
        for (        IgniteInClosure<? super IgniteFuture<R>> l2 : lsnr)         if (l1.equals(l2))         it.remove();
      }
    }
  }
}
