{
  if (!(obj instanceof TcpDiscoveryClientReconnectMessage))   return false;
  TcpDiscoveryClientReconnectMessage other=(TcpDiscoveryClientReconnectMessage)obj;
  return F.eq(creatorNodeId(),other.creatorNodeId()) && F.eq(routerNodeId,other.routerNodeId) && F.eq(lastMsgId,other.lastMsgId);
}
