{
  GridCacheAdapter<?,?> cache=ctx.cache().internalCache(cacheName);
  if (cache == null)   throw new CacheException("Cache does not exist on current node: [nodeId=" + ctx.localNodeId() + ", cache="+ cacheName+ ", topVer="+ topVer+ "]");
  return cache.context();
}
