{
  ConcurrentMap<Long,QueryResults> nodeRess=qryRess.get(nodeId);
  if (nodeRess == null) {
    nodeRess=new ConcurrentHashMap8<>();
    ConcurrentMap<Long,QueryResults> old=qryRess.putIfAbsent(nodeId,nodeRess);
    if (old != null)     nodeRess=old;
  }
  return nodeRess;
}
