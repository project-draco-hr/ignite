{
  if (closed)   return;
  closed=true;
  Statement stmt;
  try {
    stmt=rs.getStatement();
  }
 catch (  SQLException e) {
    throw new IllegalStateException(e);
  }
  U.close(rs,log);
  U.close(stmt,log);
}
