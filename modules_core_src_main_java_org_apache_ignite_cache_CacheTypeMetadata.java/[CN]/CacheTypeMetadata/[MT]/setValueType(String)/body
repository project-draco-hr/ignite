{
  if (this.valType != null)   throw new CacheException("Value type can be set only once.");
  this.valType=valType;
  Class<?> cls=U.classForName(valType,null);
  simpleValType=cls == null ? valType : GridQueryProcessor.typeName(cls);
}
