{
  Entry<K,V> e=map.get(o);
  if (e == null || !F.isAll(e,filter))   return false;
  map.remove(o);
  try {
    e.removex();
  }
 catch (  IgniteCheckedException ex) {
    throw new IgniteException(ex);
  }
  return true;
}
