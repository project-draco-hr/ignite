{
switch (taskCtx.taskInfo().type()) {
case MAP:
    if (combinerMap != null)     return combinerMap.startAdding();
case COMBINE:
  return new PartitionedOutput(taskCtx);
default :
throw new IllegalStateException("Illegal type: " + taskCtx.taskInfo().type());
}
}
