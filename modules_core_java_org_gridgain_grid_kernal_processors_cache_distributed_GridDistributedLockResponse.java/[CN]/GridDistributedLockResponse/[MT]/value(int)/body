{
  if (!F.isEmpty(vals)) {
    V res=vals.get(idx);
    if (res != null)     return res;
  }
  if (!F.isEmpty(valBytes)) {
    GridCacheValueBytes res=valBytes.get(idx);
    if (res != null && res.isPlain())     return (V)res.get();
  }
  return null;
}
