{
  super.finishUnmarshal(ctx,ldr);
  if (F.isEmpty(vals) && !F.isEmpty(valBytes))   vals=unmarshalValueBytesCollection(valBytes,ctx,ldr);
  if (errBytes != null)   err=ctx.marshaller().unmarshal(errBytes,ldr);
}
