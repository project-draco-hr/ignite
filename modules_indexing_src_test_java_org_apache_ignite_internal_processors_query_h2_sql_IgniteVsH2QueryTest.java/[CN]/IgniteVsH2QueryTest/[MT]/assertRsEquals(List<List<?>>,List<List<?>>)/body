{
  assertEquals("Count of results.",expRs.size(),actualRs.size());
  for (  List<?> expectedRow : expRs)   assertTrue("An actual result set=" + actualRs + " have to contains row="+ expectedRow,actualRs.contains(expectedRow));
}
