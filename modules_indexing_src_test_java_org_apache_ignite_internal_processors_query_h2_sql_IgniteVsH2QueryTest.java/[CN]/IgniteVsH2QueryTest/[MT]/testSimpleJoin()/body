{
  testQuery("select id, firstName, lastName" + "  from \"part\".Person" + "  where Person.id = ?",3);
  testQuery("select Person.firstName" + "  from \"part\".Person, \"part\".Purchase" + "  where Person.id = ?",3);
}
