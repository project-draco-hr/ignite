{
  int rsRowsCnt=0;
  while (rs.next()) {
    boolean currRowFound=false;
    for (    List<?> row : actualRs) {
      if (rowEqualsCurrentRsRow(rs,row)) {
        currRowFound=true;
        break;
      }
    }
    assertTrue("A row with number " + rsRowsCnt + " from "+ rs+ " not found at "+ actualRs+ '.',currRowFound);
    rsRowsCnt++;
  }
  assertEquals("Count of results.",rsRowsCnt,actualRs.size());
}
