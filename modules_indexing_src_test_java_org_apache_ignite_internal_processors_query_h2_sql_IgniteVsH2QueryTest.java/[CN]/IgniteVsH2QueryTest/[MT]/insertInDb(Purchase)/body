{
  try (PreparedStatement st=conn.prepareStatement("insert into \"part\".PURCHASE (id, personId, productId) values(?, ?, ?)")){
    st.setInt(1,p.id);
    st.setInt(2,p.personId);
    st.setInt(3,p.productId);
    st.executeUpdate();
  }
 }
