{
  List<List<?>> res1=cache.queryFields(new SqlFieldsQuery(sql).setArgs(args)).getAll();
  log.info(">>> Results (count=" + res1.size() + "):");
  for (  List<?> objects : res1)   log.info(objects.toString());
}
