{
  ResultSet rs=null;
  try {
    try (PreparedStatement st=conn.prepareStatement(sql)){
      rs=st.executeQuery();
      return new RsHandler().handle(rs);
    }
   }
  finally {
    U.closeQuiet(rs);
  }
}
