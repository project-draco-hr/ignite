{
switch (plc) {
case PUBLIC_POOL:
    return pubPool;
case SYSTEM_POOL:
  return sysPool;
case IGFS_POOL:
return igfsPool;
default :
throw new IllegalArgumentException("Invalid closure execution policy: " + plc);
}
}
