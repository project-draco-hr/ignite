{
  long[] arr=new long[ARR_LEN];
  for (int i=0; i < ARR_LEN; i++)   arr[i]=RND.nextLong();
  out.writeLongArray(arr);
  byte[] outArr=array();
  for (int i=0; i < ARR_LEN; i++)   assertEquals(arr[i],getLongByByteLE(outArr,i * 8));
  Assert.assertArrayEquals(arr,in.readLongArray(ARR_LEN));
}
