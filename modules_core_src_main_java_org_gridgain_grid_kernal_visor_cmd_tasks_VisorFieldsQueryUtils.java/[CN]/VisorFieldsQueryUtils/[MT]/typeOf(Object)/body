{
  if (o != null) {
    Class<?> clazz=o.getClass();
    if (clazz.isArray())     return GridUtils.compact(clazz.getComponentType().getName()) + "[]";
 else     return GridUtils.compact(o.getClass().getName());
  }
 else   return "n/a";
}
