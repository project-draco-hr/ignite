{
  if (col == null)   return;
  boolean depEnabled=ctx.deploymentEnabled();
  for (  CacheObject obj : col) {
    if (obj != null) {
      obj.prepareMarshal(ctx.cacheObjectContext());
      if (depEnabled)       prepareObject(obj.value(ctx.cacheObjectContext(),false),ctx.shared());
    }
  }
}
