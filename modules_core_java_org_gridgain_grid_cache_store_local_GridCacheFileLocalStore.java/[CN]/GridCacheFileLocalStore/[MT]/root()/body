{
  if (root == null) {
    File dir=new File(rootPath);
    if (dir.isAbsolute())     root=dir.toPath();
 else     if (!F.isEmpty(U.getGridGainHome()))     root=new File(U.getGridGainHome(),rootPath).toPath();
 else     throw new IOException("Failed to resolve database directory path: " + rootPath);
  }
  return root;
}
