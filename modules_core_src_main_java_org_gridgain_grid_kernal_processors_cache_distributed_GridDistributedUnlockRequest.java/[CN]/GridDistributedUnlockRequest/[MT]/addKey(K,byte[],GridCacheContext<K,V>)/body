{
  boolean depEnabled=ctx.deploymentEnabled();
  if (depEnabled)   prepareObject(key,ctx);
  if (keys == null)   keys=new ArrayList<>(keysCount());
  keys.add(key);
  if (keyBytes == null)   keyBytes=new ArrayList<>(keysCount());
  keyBytes.add(bytes);
}
