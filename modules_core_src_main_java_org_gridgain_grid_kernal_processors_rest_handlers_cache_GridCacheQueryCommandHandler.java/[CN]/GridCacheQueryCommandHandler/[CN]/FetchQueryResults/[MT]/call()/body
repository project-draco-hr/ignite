{
  GridNodeLocalMap<QueryExecutionKey,QueryFutureWrapper> locMap=g.nodeLocalMap();
  return fetchQueryResults(req.queryId(),locMap.get(new QueryExecutionKey(req.queryId())),locMap,g.localNode().id());
}
