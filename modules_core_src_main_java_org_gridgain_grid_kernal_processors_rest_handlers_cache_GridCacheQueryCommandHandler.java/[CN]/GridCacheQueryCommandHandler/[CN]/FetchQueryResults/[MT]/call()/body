{
  GridNodeLocalMap<QueryExecutionKey,QueryFutureWrapper> locMap=g.nodeLocalMap();
  return fetchQueryResults(req.queryId(),req.pageSize(),locMap.get(new QueryExecutionKey(req.queryId())),locMap);
}
