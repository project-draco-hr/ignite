{
  for (int i=0; i < RETRIES; i++) {
    if (DEBUG)     grid.log().info("***");
    if (cntrRmvd.get() % LOG_FREQ == 0 || DEBUG)     grid.log().info("*** Iteration #" + i + " ***");
    if (DEBUG)     grid.log().info("***");
    onRemoveItemSimple(putCntr,grid,i);
  }
}
