{
  int len=cnt << 1;
  ensureEnoughData(len);
  char[] res=new char[cnt];
  copyAndShift(res,CHAR_ARR_OFF,len);
  if (!LITTLE_ENDIAN) {
    for (int i=0; i < res.length; i++)     res[i]=Character.reverseBytes(res[i]);
  }
  return res;
}
