{
  jobId.writeExternal(out);
  out.writeObject(jobInfo);
  out.writeBoolean(hasMappers);
  out.writeInt(reducerCnt);
  if (!F.isEmpty(reducers)) {
    out.writeInt(reducers.length);
    for (    int rdc : reducers)     out.writeInt(rdc);
  }
 else   out.writeInt(0);
}
