{
  if (o != null) {
    Class<?> clazz=o.getClass();
    return clazz.isArray() ? GridUtils.compact(clazz.getComponentType().getName()) + "[]" : GridUtils.compact(o.getClass().getName());
  }
 else   return "n/a";
}
