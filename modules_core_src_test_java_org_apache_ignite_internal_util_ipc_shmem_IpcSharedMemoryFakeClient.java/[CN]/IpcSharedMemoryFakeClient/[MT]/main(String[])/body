{
  IpcEndpointFactory.connectEndpoint("shmem:10500",new JavaLogger());
  IpcEndpointFactory.connectEndpoint("shmem:10500",new JavaLogger());
  IpcEndpointFactory.connectEndpoint("shmem:10500",new JavaLogger());
}
