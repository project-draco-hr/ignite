{
  if (closeGuard.compareAndSet(false,true)) {
    super.close();
    metrics.decrementFilesOpenedForRead();
    if (evts.isRecordable(EVT_IGFS_FILE_CLOSED_READ))     evts.record(new IgfsEvent(path,localNode(),EVT_IGFS_FILE_CLOSED_READ,bytes()));
  }
}
