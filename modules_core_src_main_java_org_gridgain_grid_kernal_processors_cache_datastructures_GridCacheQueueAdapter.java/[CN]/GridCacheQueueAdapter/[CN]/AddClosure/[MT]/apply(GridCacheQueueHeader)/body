{
  if (queueRemoved(hdr,id) || !spaceAvailable(hdr,size))   return hdr;
  return new GridCacheQueueHeader(hdr.id(),hdr.capacity(),hdr.collocated(),hdr.head(),hdr.tail() + size,hdr.removedIndexes());
}
