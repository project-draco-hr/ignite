{
  try {
    GridCacheQueueHeader hdr=(GridCacheQueueHeader)cache.get(queueKey);
    checkRemoved(hdr);
    if (hdr.empty())     return null;
    return (T)cache.get(itemKey(hdr.head()));
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException(e);
  }
}
