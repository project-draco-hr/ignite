{
  super.onKernalStart();
  jobTracker=ctx.jobTracker();
  exec=ctx.configuration().getEmbeddedExecutor();
  if (exec == null)   exec=Executors.newFixedThreadPool(Runtime.getRuntime().availableProcessors() * 2);
}
