{
  if (!rmv) {
    if (!entry.isCached())     return;
    if (touch(entry))     shrink();
  }
 else {
    Holder<K,V> holder=entry.removeMeta();
    if (holder != null)     removeHolder(holder);
  }
}
