{
  if (size() == 0)   return "()";
  StatementBuilder buff=new StatementBuilder("(");
  for (  GridSqlElement e : children) {
    buff.appendExceptFirst(", ");
    buff.append(e.getSQL());
  }
  if (size() == 1)   buff.append(',');
  return buff.append(')').toString();
}
