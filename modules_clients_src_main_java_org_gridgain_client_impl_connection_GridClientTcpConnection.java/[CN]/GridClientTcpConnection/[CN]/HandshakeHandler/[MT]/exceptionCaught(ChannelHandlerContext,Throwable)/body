{
  if (log.isLoggable(Level.FINE)) {
    log.log(Level.FINE,"Exception occurred in TCP channel: " + serverAddress(),cause);
  }
  if (!handshake.isDone())   handshake.setFailure(cause);
  close(FAILED,false,cause);
}
