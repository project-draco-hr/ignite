{
  long now=U.currentTimeMillis();
  long end=timeout == 0 ? Long.MAX_VALUE : timeout + now;
  if (end < 0) {
    end=Long.MAX_VALUE;
  }
  if (now > end) {
    throw new GridComputeTaskTimeoutException("Timeout occurred while waiting for completion.");
  }
  Collection<?> res=subgrid.compute().execute(new GridifyDefaultRangeTask(cls,nodeFilter,threshold,splitSize,false),arg,timeout == 0 ? 0L : (end - now)).get();
  return result(arg.getMethodReturnType(),res);
}
