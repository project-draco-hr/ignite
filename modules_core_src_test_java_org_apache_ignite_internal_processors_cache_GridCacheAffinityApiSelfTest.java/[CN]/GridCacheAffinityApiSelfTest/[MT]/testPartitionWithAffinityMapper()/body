{
  CacheAffinityKey<Integer> key=new CacheAffinityKey<>(1,2);
  int expPart=affinity().partition(affinityMapper().affinityKey(key));
  for (int i=0; i < gridCount(); i++)   assertEquals(expPart,grid(i).affinity(null).partition(key));
}
