{
  A.notNull(nodeId,"nodeId");
  guard();
  try {
    return F.find(F.concat(false,nodes(),ctx.discovery().daemonNodes()),null,F.<GridNode>nodeForNodeId(nodeId));
  }
  finally {
    unguard();
  }
}
