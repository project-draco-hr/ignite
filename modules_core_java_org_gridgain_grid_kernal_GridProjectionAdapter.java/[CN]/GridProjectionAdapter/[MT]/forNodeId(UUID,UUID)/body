{
  A.notNull(nodeId,"nodeId");
  Set<UUID> ids;
  if (F.isEmpty(nodeIds))   ids=new GridLeanSet<>(1);
 else {
    ids=new HashSet<>(nodeIds.length + 1);
    Collections.addAll(ids,nodeIds);
  }
  ids.add(nodeId);
  return newProjection(ids,true);
}
