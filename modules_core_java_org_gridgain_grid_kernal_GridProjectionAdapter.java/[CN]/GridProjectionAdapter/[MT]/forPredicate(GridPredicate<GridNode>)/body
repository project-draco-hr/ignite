{
  A.notNull(p,"p");
  guard();
  try {
    if (p == F.<GridNode>alwaysTrue())     return this;
    if (predicate() != F.<GridNode>alwaysTrue())     p=F.and(p,predicate());
    return new GridProjectionImpl(this,ctx,p);
  }
  finally {
    unguard();
  }
}
