{
  for (int i=0; i < SERVERS_CNT; i++)   runServer(i,i == 0);
  try (GridClient ignored=client()){
    for (int i=0; i < 2; i++)     assertEquals("Iteration: " + i,1,srvs[i].getConnectCount());
    for (int i=2; i < SERVERS_CNT; i++)     assertEquals(0,srvs[i].getConnectCount());
  }
 }
