{
  Collection<GridNode> nodes=nodes(grid);
  if (qryLog.isDebugEnabled())   qryLog.debug(U.compact("Executing fields query with visitor on nodes: " + toShortString(nodes)));
  executeSync(nodes,false,false,null,vis);
}
