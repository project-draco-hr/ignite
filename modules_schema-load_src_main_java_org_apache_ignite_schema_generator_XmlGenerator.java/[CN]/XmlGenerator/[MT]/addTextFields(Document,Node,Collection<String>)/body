{
  if (!textFields.isEmpty()) {
    Element prop=addProperty(doc,parent,"textFields",null);
    Element list=addElement(doc,prop,"list");
    for (    String textField : textFields)     addElement(doc,list,"value").setNodeValue(textField);
  }
}
