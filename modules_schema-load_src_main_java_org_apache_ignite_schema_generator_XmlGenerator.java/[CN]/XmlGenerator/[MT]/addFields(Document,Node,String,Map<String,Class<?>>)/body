{
  if (!fields.isEmpty()) {
    Element prop=addProperty(doc,parent,name,null);
    Element map=addElement(doc,prop,"map");
    for (    Map.Entry<String,Class<?>> item : fields.entrySet())     addElement(doc,map,"entry","key",item.getKey(),"value",item.getValue().getName());
  }
}
