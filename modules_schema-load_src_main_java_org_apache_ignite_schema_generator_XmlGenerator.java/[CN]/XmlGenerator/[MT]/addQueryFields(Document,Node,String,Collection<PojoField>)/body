{
  if (!fields.isEmpty()) {
    Element prop=addProperty(doc,parent,name,null);
    Element map=addElement(doc,prop,"map");
    for (    PojoField field : fields)     addElement(doc,map,"entry","key",field.javaName(),"value",field.javaTypeName());
  }
}
