{
  AffinityTopologyVersion topVer=tx != null ? tx.topologyVersionSnapshot() : cctx.mvcc().lastExplicitLockTopologyVersion(threadId);
  if (topVer != null) {
    this.topVer.compareAndSet(null,topVer);
    map(keys);
    markInitialized();
    return;
  }
  mapOnTopology();
}
