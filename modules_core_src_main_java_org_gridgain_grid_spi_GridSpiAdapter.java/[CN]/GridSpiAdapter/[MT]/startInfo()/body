{
  GridSpiInfo ann=getClass().getAnnotation(GridSpiInfo.class);
  if (ann == null)   throw new GridSpiException("@GridSpiInfo annotation is missing for the SPI.");
  return "SPI started ok [startMs=" + getUpTime() + ", spiMBean="+ spiMBean+ ']';
}
