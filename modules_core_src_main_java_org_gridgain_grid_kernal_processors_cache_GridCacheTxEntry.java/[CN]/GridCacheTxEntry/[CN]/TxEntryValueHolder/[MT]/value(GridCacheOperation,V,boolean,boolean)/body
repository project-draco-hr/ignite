{
  if (hasReadVal && this.hasWriteVal)   return;
  boolean clean=this.val != null;
  this.op=op;
  this.val=val;
  if (clean)   valBytes=null;
  this.hasWriteVal=hasWriteVal || op == CREATE || op == UPDATE || op == DELETE;
  this.hasReadVal=hasReadVal || op == READ;
}
