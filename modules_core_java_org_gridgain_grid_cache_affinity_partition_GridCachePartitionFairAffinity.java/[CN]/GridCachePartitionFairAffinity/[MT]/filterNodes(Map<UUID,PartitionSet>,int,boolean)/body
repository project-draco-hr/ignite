{
  PrioritizedPartitionMap res=new PrioritizedPartitionMap(overloaded ? DESC_CMP : ASC_CMP);
  for (  PartitionSet set : mapping.values()) {
    if ((overloaded && set.size() > idealPartCnt + 1) || (set.size() < idealPartCnt - 1))     res.add(set);
  }
  return res;
}
