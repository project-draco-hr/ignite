{
  Session ses=sesFactory.openSession();
  try {
    Criteria crit=ses.createCriteria(cls);
    if (critClo != null)     crit=critClo.apply(crit);
    crit.setCacheable(true);
    return crit.list();
  }
  finally {
    ses.close();
  }
}
