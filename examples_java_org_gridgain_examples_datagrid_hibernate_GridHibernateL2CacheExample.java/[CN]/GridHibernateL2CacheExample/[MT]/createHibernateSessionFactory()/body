{
  ServiceRegistryBuilder builder=new ServiceRegistryBuilder();
  builder.applySetting("hibernate.connection.url",JDBC_URL);
  builder.applySetting("hibernate.show_sql",true);
  return new Configuration().configure(U.resolveGridGainUrl("examples/config/hibernate-L2-cache.xml")).buildSessionFactory(builder.buildServiceRegistry());
}
