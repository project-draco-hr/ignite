{
  Map<JdbcDriverMetadataJob,ClusterNode> map=new HashMap<>();
  for (  ClusterNode n : subgrid)   if (U.hasCache(n,cacheName)) {
    map.put(new JdbcDriverMetadataJob(cacheName),n);
    break;
  }
  return map;
}
