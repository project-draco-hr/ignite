{
  Integer old=e.getValue();
  if (key < 5)   System.err.println(Thread.currentThread().getName() + " <> Transforming value [key=" + key+ ", val="+ old+ ']');
  e.setValue(old == null ? 1 : old + 1);
  return null;
}
