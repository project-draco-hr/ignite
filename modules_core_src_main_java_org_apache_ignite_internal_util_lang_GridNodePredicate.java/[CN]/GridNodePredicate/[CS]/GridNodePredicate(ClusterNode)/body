{
  if (F.isEmpty(nodes))   ids=Collections.emptySet();
 else   if (nodes.length == 1)   ids=Collections.singleton(nodes[0].id());
 else {
    ids=U.newHashSet(nodes.length);
    for (    ClusterNode n : nodes)     ids.add(n.id());
  }
}
