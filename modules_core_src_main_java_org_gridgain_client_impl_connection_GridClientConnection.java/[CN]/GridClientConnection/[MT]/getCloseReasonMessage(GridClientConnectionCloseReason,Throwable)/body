{
  if (reason == CONN_IDLE)   return "Connection was closed by idle thread";
  if (reason == FAILED)   return cause != null ? "Connection failed, cause: " + cause.getMessage() : "Connection failed";
  if (reason == CLIENT_CLOSED)   return "Client was closed";
  return null;
}
