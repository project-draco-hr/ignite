{
  try {
    GridCacheAtomicStamped<String,String> stamped=GridGain.grid().cache(cacheName).dataStructures().atomicStamped(stampedName,null,null,true);
    print("Atomic stamped [value=" + stamped.value() + ", stamp="+ stamped.stamp()+ ']');
  }
 catch (  GridException e) {
    throw new GridRuntimeException(e);
  }
}
