{
  if (isClosed())   return Collections.emptySet();
  Collection<String> res=new ArrayList<>();
  for (  GridCache<?,?> cache : ignite.caches())   res.add(cache.name());
  return Collections.unmodifiableCollection(res);
}
