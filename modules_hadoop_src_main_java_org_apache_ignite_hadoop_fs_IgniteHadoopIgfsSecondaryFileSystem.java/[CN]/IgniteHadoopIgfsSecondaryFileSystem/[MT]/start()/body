{
  dfltUsrName=IgfsUtils.fixUserName(dfltUsrName);
  if (fsFactory == null)   fsFactory=new CachingHadoopFileSystemFactory();
  if (fsFactory instanceof LifecycleAware)   ((LifecycleAware)fsFactory).start();
}
