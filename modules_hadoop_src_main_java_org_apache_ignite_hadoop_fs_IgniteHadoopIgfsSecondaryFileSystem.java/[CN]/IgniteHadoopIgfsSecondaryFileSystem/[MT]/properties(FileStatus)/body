{
  FsPermission perm=status.getPermission();
  if (perm == null)   perm=FsPermission.getDefault();
  return F.asMap(IgfsUtils.PROP_PERMISSION,String.format("%04o",perm.toShort()),IgfsUtils.PROP_USER_NAME,status.getOwner(),IgfsUtils.PROP_GROUP_NAME,status.getGroup());
}
