{
  if (syncFuture().isDone())   return new GridFinishedFuture<>(cctx.kernalContext());
  GridReplicatedForceKeysFuture<K,V> fut=new GridReplicatedForceKeysFuture<>(cctx,this,topVer);
  fut.init(keys);
  return fut;
}
