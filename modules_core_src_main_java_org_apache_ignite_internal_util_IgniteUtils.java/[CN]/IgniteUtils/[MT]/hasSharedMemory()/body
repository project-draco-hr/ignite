{
  if (hasShmem == null) {
    if (isWindows())     hasShmem=false;
 else {
      try {
        IpcSharedMemoryNativeLoader.load();
        hasShmem=true;
      }
 catch (      IgniteCheckedException e) {
        hasShmem=false;
      }
    }
  }
  return hasShmem;
}
