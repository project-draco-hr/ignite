{
  if (s == null)   return;
  OutputStream out=null;
  try {
    out=new FileOutputStream(file,append);
    if (s != null)     out.write(s.getBytes(charset));
  }
  finally {
    closeQuiet(out);
  }
}
