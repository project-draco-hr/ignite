{
  Class<?> clazz;
  if (cls == null)   clazz=dflt;
 else   if (includePrimitiveTypes && primitiveMap.containsKey(cls))   clazz=primitiveMap.get(cls);
 else {
    try {
      clazz=Class.forName(cls);
    }
 catch (    ClassNotFoundException ignore) {
      clazz=dflt;
    }
  }
  return clazz;
}
