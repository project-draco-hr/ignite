{
  if (obj instanceof GridPortableEx)   writePortable((GridPortableEx)obj);
 else   if (obj instanceof Map)   writeMap((Map)obj);
 else   if (obj instanceof Collection)   writeCollection((Collection)obj);
}
