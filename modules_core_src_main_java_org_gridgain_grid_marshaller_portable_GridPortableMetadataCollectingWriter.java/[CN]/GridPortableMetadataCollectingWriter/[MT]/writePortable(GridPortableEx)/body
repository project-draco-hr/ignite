{
  if (portable == null || handles.lookup(portable) >= 0)   return;
  List<String> curFields=new ArrayList<>(fields);
  fields=new ArrayList<>();
  portable.writePortable(this);
  fieldsMap.put(portable.typeId(),fields);
  fields=curFields;
}
