{
  while (log != null) {
    for (    Handler hnd : log.getHandlers()) {
      if (hnd instanceof ConsoleHandler)       return (ConsoleHandler)hnd;
    }
    log=log.getParent();
  }
  return null;
}
