{
  if (oldInfo.type != info.type) {
    return new IgniteException("Another data structure with the same name already created " + "[name= " + info.name + ", new= "+ info.type.className()+ ", existing="+ oldInfo.type.className()+ ']');
  }
  return null;
}
