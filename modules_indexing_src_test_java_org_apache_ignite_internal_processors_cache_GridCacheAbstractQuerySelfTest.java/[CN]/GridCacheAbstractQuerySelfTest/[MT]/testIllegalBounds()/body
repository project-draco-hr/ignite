{
  GridCache<Integer,Integer> cache=((IgniteKernal)ignite).cache(null);
  cache.put(1,1);
  cache.put(2,2);
  CacheQuery<Map.Entry<Integer,Integer>> qry=cache.queries().createSqlQuery(Integer.class,"_key between 2 and 1");
  assertTrue(qry.execute().get().isEmpty());
}
