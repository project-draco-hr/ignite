{
  try (Grid g=GridGain.start("examples/config/example-master-leave-aware.xml")){
    System.out.println(">>> Waiting for master node to join topology and execute GridMasterLeaveJob ...");
    JOptionPane.showMessageDialog(null,new JComponent[]{new JLabel("Worker node started."),new JLabel("Press OK to stop it.")},"GridGain Worker Node",JOptionPane.INFORMATION_MESSAGE);
  }
 }
