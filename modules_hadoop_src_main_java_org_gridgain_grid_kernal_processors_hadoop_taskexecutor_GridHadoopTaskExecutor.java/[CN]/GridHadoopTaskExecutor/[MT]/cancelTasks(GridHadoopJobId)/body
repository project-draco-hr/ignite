{
  Collection<GridFuture<?>> futures=jobs.get(jobId);
  if (futures != null)   for (  GridFuture<?> f : futures) {
    try {
      f.cancel();
    }
 catch (    GridException e) {
      log.error("Future cancelling failed",e);
    }
  }
}
