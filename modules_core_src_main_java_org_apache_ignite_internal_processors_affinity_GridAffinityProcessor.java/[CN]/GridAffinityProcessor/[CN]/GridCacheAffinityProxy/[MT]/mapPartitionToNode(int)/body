{
  try {
    return F.first(GridAffinityProcessor.this.affinityCache(cacheName,topologyVersion()).assignment.get(part));
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException(e);
  }
}
