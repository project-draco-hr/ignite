{
  try {
    Set<Integer> parts=GridAffinityProcessor.this.affinityCache(cacheName,topologyVersion()).assignment.backupPartitions(n.id());
    return U.toIntArray(parts);
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException(e);
  }
}
