{
  ctx.gateway().readLock();
  try {
    return affinityCache(cacheName,topologyVersion()).assignment.primaryPartitions(n.id()).contains(partition(key));
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException(e);
  }
 finally {
    ctx.gateway().readUnlock();
  }
}
