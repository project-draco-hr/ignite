{
  if (!(e.key() instanceof GridCacheQueueItemKey))   return false;
  if (cctx.isLocal())   return e.key().hashCode() % workersCnt == idx;
 else   return cctx.affinity().primary(cctx.localNode(),e.partition()) && e.partition() % workersCnt == idx;
}
