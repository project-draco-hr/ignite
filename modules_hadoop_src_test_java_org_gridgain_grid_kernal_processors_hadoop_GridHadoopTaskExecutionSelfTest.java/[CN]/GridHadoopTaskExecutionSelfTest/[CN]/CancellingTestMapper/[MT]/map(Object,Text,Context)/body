{
  try {
    while (true) {
      if (mapperId == failMapperId)       throw new IOException();
      Thread.sleep(100);
    }
  }
 catch (  InterruptedException e) {
    cancelledTasks.incrementAndGet();
    Thread.currentThread().interrupt();
    throw e;
  }
}
