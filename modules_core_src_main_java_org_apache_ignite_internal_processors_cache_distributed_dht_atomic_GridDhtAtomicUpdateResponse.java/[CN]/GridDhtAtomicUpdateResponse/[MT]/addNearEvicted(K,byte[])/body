{
  if (nearEvicted == null)   nearEvicted=new ArrayList<>();
  nearEvicted.add(key);
  if (bytes != null) {
    if (nearEvictedBytes == null)     nearEvictedBytes=new ArrayList<>();
    nearEvictedBytes.add(bytes);
  }
}
