{
  if (!getSpiContext().isStopping() && sockWriter.isOnline()) {
    TcpDiscoveryClientHeartbeatMessage msg=new TcpDiscoveryClientHeartbeatMessage(getLocalNodeId(),metricsProvider.metrics());
    msg.client(true);
    sockWriter.sendMessage(msg);
  }
}
