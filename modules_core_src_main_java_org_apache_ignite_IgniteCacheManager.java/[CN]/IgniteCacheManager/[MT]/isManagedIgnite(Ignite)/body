{
synchronized (igniteMap) {
    for (    IgniteBiTuple<Ignite,CacheConfigurationMxBeanImpl> tuple : igniteMap.values()) {
      if (ignite.equals(tuple.get1()))       return true;
    }
  }
  return false;
}
