{
  validTxState(true);
  if (!id2InfoPrj.putIfAbsent(info.id(),info))   throw fsException("Failed to create new metadata entry due to ID conflict: " + info.id());
  if (parentId != null)   id2InfoPrj.invoke(parentId,new ListingAddProcessor(name,new IgfsListingEntry(info)));
}
