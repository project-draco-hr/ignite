{
  IgfsFileInfo info=new IgfsFileInfo(true,props,createTime,createTime);
  if (childName != null)   info=new IgfsFileInfo(Collections.singletonMap(childName,childEntry),info);
  info.id(entry.getKey());
  entry.setValue(info);
  return info;
}
