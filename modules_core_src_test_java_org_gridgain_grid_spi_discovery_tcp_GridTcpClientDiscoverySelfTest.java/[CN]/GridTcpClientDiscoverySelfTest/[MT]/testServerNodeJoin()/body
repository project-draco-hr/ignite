{
  startServerNodes(3);
  startClientNodes(3);
  checkNodes(3,3);
  srvJoinLatch=new CountDownLatch(3);
  clientJoinLatch=new CountDownLatch(3);
  attachListeners(3,3);
  startServerNodes(1);
  await(srvJoinLatch);
  await(clientJoinLatch);
  checkNodes(4,3);
}
