{
  failedLatch=new CountDownLatch(3);
  startServerNodes(3);
  startClientNodes(1);
  forceClientFail();
  stopGrid("client-0");
  assertTrue(failedLatch.await(5000,MILLISECONDS));
  checkNodes(3,0);
}
