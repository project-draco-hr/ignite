{
  int i=testsCfg.gridCount();
  if (i < 5)   return super.expectedClient(testGridName);
  return getTestGridName(CLIENT_NODE_IDX).equals(testGridName) || getTestGridName(CLIENT_NODE_IDX_2).equals(testGridName);
}
