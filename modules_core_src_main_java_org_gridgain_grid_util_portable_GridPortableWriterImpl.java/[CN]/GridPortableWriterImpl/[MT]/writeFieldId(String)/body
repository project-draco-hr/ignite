{
  A.notNull(fieldName,"fieldName");
  if (!allowFields)   throw new GridPortableException("Individual field can't be written after raw writer is acquired " + "via rawWriter() method. Consider fixing serialization logic for class: " + cls.getName());
  doWriteInt(ctx.fieldId(typeId,fieldName));
}
