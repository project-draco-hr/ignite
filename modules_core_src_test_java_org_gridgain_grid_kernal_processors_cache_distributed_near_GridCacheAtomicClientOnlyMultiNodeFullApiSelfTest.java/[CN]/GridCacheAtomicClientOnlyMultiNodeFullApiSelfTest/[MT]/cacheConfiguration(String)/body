{
  CacheConfiguration cfg=super.cacheConfiguration(gridName);
  if (cfg.getDistributionMode() == NEAR_ONLY)   cfg.setDistributionMode(CLIENT_ONLY);
 else   cfg.setDistributionMode(PARTITIONED_ONLY);
  return cfg;
}
