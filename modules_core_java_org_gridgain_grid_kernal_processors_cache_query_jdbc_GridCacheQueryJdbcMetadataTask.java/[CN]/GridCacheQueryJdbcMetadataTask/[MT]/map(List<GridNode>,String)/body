{
  Map<JdbcDriverMetadataJob,GridNode> map=new HashMap<>();
  for (  GridNode n : subgrid)   if (U.hasCache(n,cacheName)) {
    map.put(new JdbcDriverMetadataJob(cacheName),n);
    break;
  }
  return map;
}
