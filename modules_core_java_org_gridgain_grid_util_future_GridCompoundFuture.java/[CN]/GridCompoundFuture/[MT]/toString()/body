{
  return S.toString(GridCompoundFuture.class,this,"done",isDone(),"cancelled",isCancelled(),"err",error(),"futs",F.viewReadOnly(futs,new C1<GridFuture<T>,String>(){
    @Override public String apply(    GridFuture<T> f){
      return Boolean.toString(f.isDone());
    }
  }
));
}
