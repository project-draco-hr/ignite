{
  if (ctx.tm().inUserTx())   return ctx.closures().callLocalSafe(cmd,true).get();
 else {
    try {
      return cmd.call();
    }
 catch (    GridException e) {
      throw e;
    }
catch (    Exception e) {
      throw new GridException(e);
    }
  }
}
