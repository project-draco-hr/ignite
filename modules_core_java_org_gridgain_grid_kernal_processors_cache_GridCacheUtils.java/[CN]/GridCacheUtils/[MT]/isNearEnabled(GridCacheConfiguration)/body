{
  if (cfg.getCacheMode() != PARTITIONED)   return false;
  return cfg.getPartitionedDistributionMode() == NEAR_PARTITIONED || cfg.getPartitionedDistributionMode() == GridCachePartitionedDistributionMode.NEAR_ONLY;
}
