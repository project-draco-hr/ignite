{
  for (int i=0; i < 4; i++)   startGrid(i);
  client=true;
  for (int i=4; i < 6; i++)   startGrid(i);
  ignite(4).services().deploy(serviceConfig(true));
  ignite(5).services().deploy(serviceConfig(false));
}
