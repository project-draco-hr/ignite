{
  this.assignments=assignments;
  this.neighborhoodMap=neighborhoodMap;
  this.tierMaps=new Map[tiers];
  for (int tier=0; tier < tiers; tier++)   tierMaps[tier]=assignments(tier,topSnapshot);
  fullMap=assignments(-1,topSnapshot);
}
