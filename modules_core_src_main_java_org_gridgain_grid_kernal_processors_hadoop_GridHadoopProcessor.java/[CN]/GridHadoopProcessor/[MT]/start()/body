{
  super.start();
  if (ctx.isDaemon() || ctx.config().getHadoopConfiguration() == null)   return;
  hctx=new GridHadoopContext(ctx,new GridHadoopJobTrackerManager());
  for (  GridHadoopManager mgr : hctx.managers())   mgr.start(hctx);
}
