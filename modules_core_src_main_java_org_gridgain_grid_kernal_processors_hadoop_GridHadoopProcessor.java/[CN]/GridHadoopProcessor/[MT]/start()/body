{
  super.start();
  if (ctx.isDaemon() || ctx.config().getHadoopConfiguration() == null)   return;
  hctx=new GridHadoopContext(ctx,new GridHadoopJobTracker(),new GridHadoopTaskExecutor(),new GridHadoopShuffle());
  for (  GridHadoopComponent c : hctx.components())   c.start(hctx);
}
