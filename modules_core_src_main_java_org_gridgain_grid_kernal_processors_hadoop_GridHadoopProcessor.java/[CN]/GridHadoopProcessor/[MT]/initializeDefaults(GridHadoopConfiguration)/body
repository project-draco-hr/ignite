{
  if (cfg.getMapReducePlanner() == null)   cfg.setMapReducePlanner(new GridHadoopDefaultMapReducePlanner());
  if (cfg.getJobFactory() == null)   cfg.setJobFactory(new GridHadoopDefaultJobFactory());
}
