{
  if (name.startsWith(GRIDGAIN_KERNAL_PKG)) {
    Boolean res=cache.get(name);
    if (res == Boolean.TRUE || (res == null && hasExternalDependencies(name,new HashSet<String>())))     return loadClassDirect0(name,resolve);
  }
  return super.loadClass(name,resolve);
}
