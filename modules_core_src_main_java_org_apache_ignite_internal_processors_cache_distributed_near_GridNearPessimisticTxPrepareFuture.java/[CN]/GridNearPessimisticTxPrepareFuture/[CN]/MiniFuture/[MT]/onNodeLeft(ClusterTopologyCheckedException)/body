{
  if (msgLog.isDebugEnabled()) {
    msgLog.debug("Near pessimistic prepare, mini future node left [txId=" + tx.nearXidVersion() + ", nodeId="+ m.node().id()+ ']');
  }
  if (tx.onePhaseCommit()) {
    tx.markForBackupCheck();
    onDone((GridNearTxPrepareResponse)null);
  }
  onError(e);
}
