{
  if (err != null)   this.err.compareAndSet(null,err);
  err=this.err.get();
  if (err == null || tx.needCheckBackup())   tx.state(PREPARED);
  if (super.onDone(tx,err)) {
    cctx.mvcc().removeMvccFuture(this);
    return true;
  }
  return false;
}
