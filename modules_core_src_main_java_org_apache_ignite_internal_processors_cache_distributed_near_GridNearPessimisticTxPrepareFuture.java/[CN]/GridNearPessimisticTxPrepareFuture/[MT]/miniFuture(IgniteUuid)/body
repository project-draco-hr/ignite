{
synchronized (sync) {
    for (int i=0; i < futuresCount(); i++) {
      MiniFuture mini=(MiniFuture)future(i);
      if (mini.futureId().equals(miniId)) {
        if (!mini.isDone())         return mini;
 else         return null;
      }
    }
  }
  return null;
}
