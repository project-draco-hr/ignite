{
  for (  GridNode n : nodes) {
    if (n.id().equals(nodeId)) {
      onResult(nodeId,new GridDistributedTxPrepareResponse<K,V>(tx.xidVersion(),new GridTopologyException("Valid exception to signal node departure: " + n)));
      return true;
    }
  }
  return false;
}
