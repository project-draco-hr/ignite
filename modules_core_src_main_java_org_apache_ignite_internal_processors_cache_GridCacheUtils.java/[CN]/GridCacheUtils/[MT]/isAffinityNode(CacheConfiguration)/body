{
  if (cfg.getCacheMode() == LOCAL)   return true;
  CacheDistributionMode partTax=cfg.getDistributionMode();
  if (partTax == null)   partTax=distributionMode(cfg);
  return partTax == CacheDistributionMode.PARTITIONED_ONLY || partTax == CacheDistributionMode.NEAR_PARTITIONED;
}
