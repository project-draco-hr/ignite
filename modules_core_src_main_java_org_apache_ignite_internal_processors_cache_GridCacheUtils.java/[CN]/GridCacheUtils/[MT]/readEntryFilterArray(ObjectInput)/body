{
  int len=in.readInt();
  IgnitePredicate<Entry<K,V>>[] arr=null;
  if (len > 0) {
    arr=new IgnitePredicate[len];
    for (int i=0; i < len; i++)     arr[i]=(IgnitePredicate<Entry<K,V>>)in.readObject();
  }
  return arr;
}
