{
  for (  ClientMap map : cliMap.values()) {
    Set<String> keys=new HashSet<>(map.map.keySet());
    for (    String key : keys) {
      Client cli=map.map.get(key);
      while (!cli.release()) {
      }
      map.map.remove(key);
    }
  }
}
