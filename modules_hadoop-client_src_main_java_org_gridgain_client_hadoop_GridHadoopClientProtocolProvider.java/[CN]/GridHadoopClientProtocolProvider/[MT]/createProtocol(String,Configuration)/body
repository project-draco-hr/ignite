{
  if (addr == null || addr.isEmpty())   throw new IOException("Failed to create client protocol because server address is not specified (is " + PROP_SRV_ADDR + " property set?).");
  return new GridHadoopClientProtocol(conf,addr,acquire(addr));
}
