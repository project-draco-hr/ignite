{
  byte[] newData=new byte[size];
  if (this.data == data)   this.data=newData;
  GridUnsafe.copyMemory(data,GridUnsafe.BYTE_ARR_OFF,newData,GridUnsafe.BYTE_ARR_OFF,data.length);
  return newData;
}
