{
  if (maxHeapSize == null) {
    maxHeapSize=Long.MIN_VALUE;
    for (    VisorCacheMetrics metric : metrics.values())     maxHeapSize=Math.max(maxHeapSize,metric.keySize());
  }
  return maxHeapSize;
}
