{
  if (maxOffHeapSize == null) {
    maxOffHeapSize=Long.MIN_VALUE;
    for (    VisorCacheMetrics metric : metrics.values())     maxOffHeapSize=Math.max(maxOffHeapSize,offHeapEntriesCount(metric));
  }
  return maxOffHeapSize;
}
