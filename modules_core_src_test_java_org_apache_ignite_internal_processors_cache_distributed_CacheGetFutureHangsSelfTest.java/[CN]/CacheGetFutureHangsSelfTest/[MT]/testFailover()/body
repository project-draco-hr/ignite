{
  int cnt=10;
  for (int i=0; i < cnt; i++) {
    try {
      U.debug("*** Iteration " + (i + 1) + '/'+ cnt);
      init();
      doTestFailover();
    }
  finally {
      stopAllGrids();
    }
  }
}
