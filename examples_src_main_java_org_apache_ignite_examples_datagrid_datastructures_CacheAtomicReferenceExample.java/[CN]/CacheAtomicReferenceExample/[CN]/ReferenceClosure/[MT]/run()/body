{
  try {
    CacheAtomicReference<String> ref=Ignition.ignite().cache(cacheName).dataStructures().atomicReference(refName,null,true);
    System.out.println("Atomic reference value is " + ref.get() + '.');
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException(e);
  }
}
