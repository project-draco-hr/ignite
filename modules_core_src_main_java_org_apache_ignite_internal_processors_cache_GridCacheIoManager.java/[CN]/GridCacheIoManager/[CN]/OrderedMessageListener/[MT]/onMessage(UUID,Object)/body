{
  if (log.isDebugEnabled())   log.debug("Received cache ordered message [nodeId=" + nodeId + ", msg="+ msg+ ']');
  final GridCacheMessage<K,V> cacheMsg=(GridCacheMessage<K,V>)msg;
  onMessage0(nodeId,cacheMsg,c);
}
