{
  writer.setBuffer(buf);
  if (!typeWritten) {
    if (!writer.writeByte(null,directType()))     return false;
    typeWritten=true;
  }
switch (state) {
case 0:
    if (!writer.writeIgniteUuid("taskId",taskId))     return false;
  state++;
case 1:
if (!writer.writeByteArray("topicBytes",topicBytes)) return false;
state++;
}
return true;
}
