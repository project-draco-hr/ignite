{
  Collection c=futs.get(ver);
  if (c == null)   return Collections.<IgniteInternalFuture<T>>emptyList();
 else {
synchronized (c) {
      return new ArrayList<>((Collection<IgniteInternalFuture<T>>)c);
    }
  }
}
