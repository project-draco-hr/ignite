{
  GridCacheMvccCandidate next=cand.next();
  if (next != null) {
    GridCacheMvccCandidate prev=cand.previous();
    next.previous(prev);
    if (prev != null)     prev.next(next);
  }
  if (log.isDebugEnabled())   log.debug("Unlinked lock candidate: " + cand);
}
