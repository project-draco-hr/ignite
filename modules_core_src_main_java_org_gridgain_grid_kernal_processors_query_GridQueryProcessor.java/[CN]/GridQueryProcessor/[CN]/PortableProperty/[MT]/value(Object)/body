{
  if (parent != null)   obj=parent.value(obj);
  if (obj == null)   return null;
  if (!(obj instanceof PortableObject))   throw new GridException("Non-portable object received as a result of property extraction " + "[parent=" + parent + ", propName="+ propName+ ", obj="+ obj+ ']');
  return ((PortableObject)obj).field(propName);
}
