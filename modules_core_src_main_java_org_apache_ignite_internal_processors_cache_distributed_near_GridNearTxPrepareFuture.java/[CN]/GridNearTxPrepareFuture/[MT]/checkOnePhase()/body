{
  if (cctx.isStoreEnabled())   return;
  Map<UUID,Collection<UUID>> map=txMapping.transactionNodes();
  if (map.size() == 1) {
    Collection<UUID> backups=F.firstEntry(map).getValue();
    if (backups.size() <= 1)     tx.onePhaseCommit(true);
  }
}
