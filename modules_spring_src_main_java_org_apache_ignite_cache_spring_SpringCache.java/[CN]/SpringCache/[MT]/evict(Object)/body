{
  try {
    cache.removex(keyFactory.apply(key));
  }
 catch (  IgniteCheckedException e) {
    throw new IgniteException("Failed to remove value from cache [cacheName=" + cache.name() + ", key="+ key+ ']',e);
  }
}
