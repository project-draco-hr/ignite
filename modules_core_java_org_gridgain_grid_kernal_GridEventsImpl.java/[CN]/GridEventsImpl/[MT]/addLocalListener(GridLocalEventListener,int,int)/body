{
  A.notNull(lsnr,"lsnr");
  guard();
  try {
    addLocalListener(lsnr,new int[]{type});
    if (types != null && types.length > 0)     addLocalListener(lsnr,types);
  }
  finally {
    unguard();
  }
}
