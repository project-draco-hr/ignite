{
  A.notNull(lsnr,"lsnr");
  A.notNull(types,"types");
  guard();
  try {
    if (types.length == 0)     throw new GridRuntimeException("Array of event types cannot be empty.");
    ctx.event().addLocalEventListener(lsnr,types);
  }
  finally {
    unguard();
  }
}
