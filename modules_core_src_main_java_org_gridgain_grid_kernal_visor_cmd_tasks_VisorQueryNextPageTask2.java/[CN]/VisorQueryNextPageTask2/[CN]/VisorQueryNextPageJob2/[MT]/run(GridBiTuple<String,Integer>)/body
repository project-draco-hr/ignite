{
  long start=U.currentTimeMillis();
  VisorQueryResult res=arg.get1().startsWith(SCAN_QRY_NAME) ? nextScanPage(arg) : nextSqlPage(arg);
  return new VisorQueryResult2(res,U.currentTimeMillis() - start);
}
