{
  A.notNull(jobId,"jobId");
  Collection<GridComputeJobSibling> tmp=getJobSiblings();
  for (  GridComputeJobSibling sibling : tmp)   if (sibling.getJobId().equals(jobId))   return sibling;
  return null;
}
