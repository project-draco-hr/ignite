{
  CacheOperationContext prev=gate.enter(prj);
  try {
    if (prj.skipStore() == skipStore)     return this;
    return new GridCacheProxyImpl<>(ctx,delegate,prj.setSkipStore(skipStore));
  }
  finally {
    gate.leave(prev);
  }
}
