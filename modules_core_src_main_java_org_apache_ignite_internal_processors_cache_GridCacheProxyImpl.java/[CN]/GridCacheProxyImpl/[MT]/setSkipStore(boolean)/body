{
  CacheOperationContext prev=gate.enter(opCtx);
  try {
    if (opCtx.skipStore() == skipStore)     return this;
    return new GridCacheProxyImpl<>(ctx,delegate,opCtx.setSkipStore(skipStore));
  }
  finally {
    gate.leave(prev);
  }
}
