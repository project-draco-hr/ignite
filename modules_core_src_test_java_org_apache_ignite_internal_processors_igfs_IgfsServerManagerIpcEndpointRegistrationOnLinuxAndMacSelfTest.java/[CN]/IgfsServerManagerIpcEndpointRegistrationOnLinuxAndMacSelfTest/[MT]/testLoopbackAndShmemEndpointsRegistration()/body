{
  IgniteConfiguration cfg=gridConfiguration();
  cfg.setIgfsConfiguration(igniteFsConfiguration(null,null,null),igniteFsConfiguration("tcp",DFLT_IPC_PORT + 1,null),igniteFsConfiguration("shmem",DFLT_IPC_PORT + 2,null));
  G.start(cfg);
  T2<Integer,Integer> res=checkRegisteredIpcEndpoints();
  assertEquals(4,res.get1().intValue());
  assertEquals(2,res.get2().intValue());
}
