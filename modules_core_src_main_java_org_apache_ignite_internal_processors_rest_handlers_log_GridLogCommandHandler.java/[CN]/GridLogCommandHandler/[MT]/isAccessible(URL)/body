{
  if (accessibleFolders == null)   return true;
  File f=new File(url.getFile());
  f=f.getCanonicalFile();
  do {
    if (F.contains(accessibleFolders,f))     return true;
    f=f.getParentFile();
  }
 while (f != null);
  return false;
}
