{
  GridNearTxPrepareFuture fut=(GridNearTxPrepareFuture)ctx.mvcc().<IgniteInternalTx>future(res.version(),res.futureId());
  if (fut == null) {
    U.warn(log,"Failed to find future for prepare response [sender=" + nodeId + ", res="+ res+ ']');
    return;
  }
  fut.onResult(nodeId,res);
}
