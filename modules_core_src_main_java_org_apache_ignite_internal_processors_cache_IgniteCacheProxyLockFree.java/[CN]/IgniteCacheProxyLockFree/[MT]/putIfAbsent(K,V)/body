{
  try {
    if (isAsync()) {
      setFuture(delegate.putIfAbsentAsync(key,val));
      return false;
    }
 else     return delegate.putIfAbsent(key,val);
  }
 catch (  IgniteCheckedException e) {
    throw cacheException(e);
  }
}
