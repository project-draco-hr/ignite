{
  GridCacheAtomicLong atomicLong=GridGain.grid().cache(cacheName).dataStructures().atomicLong(atomicName,0,true);
  for (int i=0; i < RETRIES; i++)   print("AtomicLong value has been incremented " + atomicLong.incrementAndGet() + '.');
  return null;
}
