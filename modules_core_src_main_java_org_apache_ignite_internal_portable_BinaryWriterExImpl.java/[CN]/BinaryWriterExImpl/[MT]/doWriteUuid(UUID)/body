{
  if (uuid == null)   out.writeByte(NULL);
 else {
    out.unsafeEnsure(1 + 8 + 8);
    out.unsafeWriteByte(UUID);
    out.unsafeWriteLong(uuid.getMostSignificantBits());
    out.unsafeWriteLong(uuid.getLeastSignificantBits());
  }
}
