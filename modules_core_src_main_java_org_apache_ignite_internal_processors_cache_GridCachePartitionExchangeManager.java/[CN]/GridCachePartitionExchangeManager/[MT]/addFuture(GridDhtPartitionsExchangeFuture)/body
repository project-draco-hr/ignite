{
  if (fut.onAdded()) {
    exchWorker.addFuture(fut);
    for (    GridCacheContext cacheCtx : cctx.cacheContexts())     cacheCtx.preloader().onExchangeFutureAdded();
    return true;
  }
  return false;
}
