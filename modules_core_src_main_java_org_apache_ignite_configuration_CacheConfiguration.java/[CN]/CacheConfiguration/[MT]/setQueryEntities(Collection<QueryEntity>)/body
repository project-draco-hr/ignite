{
  if (this.qryEntities == null)   this.qryEntities=new ArrayList<>(qryEntities);
 else   if (indexedTypes != null)   this.qryEntities.addAll(qryEntities);
 else   throw new CacheException("Query entities can be set only once.");
  return this;
}
