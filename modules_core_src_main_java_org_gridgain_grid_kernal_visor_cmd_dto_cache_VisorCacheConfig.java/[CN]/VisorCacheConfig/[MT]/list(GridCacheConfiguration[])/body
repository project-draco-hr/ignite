{
  if (caches == null)   return Collections.emptyList();
  final Collection<VisorCacheConfig> cfgs=new ArrayList<>(caches.length);
  for (  GridCacheConfiguration cache : caches)   cfgs.add(from(cache));
  return cfgs;
}
