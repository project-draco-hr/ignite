{
  if (store instanceof GridLifecycleAware) {
    if (cctx.config().isWriteBehindEnabled()) {
      if (!cctx.isNear())       ((GridLifecycleAware)store).start();
    }
 else {
      if (cctx.isNear() || !CU.isNearEnabled(cctx))       ((GridLifecycleAware)store).start();
    }
  }
}
