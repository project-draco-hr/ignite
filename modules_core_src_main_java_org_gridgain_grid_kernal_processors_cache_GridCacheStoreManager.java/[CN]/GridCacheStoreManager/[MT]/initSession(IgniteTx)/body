{
  if (!sesEnabled || tx == null)   return false;
  SessionData ses=((GridMetadataAware)tx).meta(SES_ATTR);
  if (ses == null) {
    ses=new SessionData(tx);
    ((GridMetadataAware)tx).addMeta(SES_ATTR,ses);
  }
  sesHolder.set(ses);
  return true;
}
