{
  SessionData ses;
  if (tx != null) {
    ses=((IgniteMetadataAware)tx).meta(SES_ATTR);
    if (ses == null) {
      ses=new SessionData(tx,cctx.name());
      ((IgniteMetadataAware)tx).addMeta(SES_ATTR,ses);
    }
  }
 else   ses=new SessionData(null,cctx.name());
  sesHolder.set(ses);
}
