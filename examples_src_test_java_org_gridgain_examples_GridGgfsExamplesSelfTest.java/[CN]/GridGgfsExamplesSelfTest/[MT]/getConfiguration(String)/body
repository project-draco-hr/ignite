{
  String cfgPath=CLIENT_LIGHT_GRID_NAME.equals(gridName) ? (U.isWindows() ? GGFS_LOOPBACK_CFG : GGFS_SHMEM_CFG) : GGFS_NO_ENDPOINT_CFG;
  GridConfiguration cfg=loadConfiguration(cfgPath);
  cfg.setGridName(gridName);
  return cfg;
}
