{
  GridDr dr=g.dr();
  String cacheName=arg.get1();
  boolean resume=arg.get2() != null ? arg.get2() : false;
  if (resume)   dr.senderCacheDrResume(cacheName);
 else   dr.senderCacheDrPause(cacheName);
  return dr.senderCacheMetrics(cacheName).status();
}
