{
  Value v=peekValue(VAL_COL);
  if (v == null && attempt != 0) {
    try {
      wait(attempt);
    }
 catch (    InterruptedException e) {
      throw new IgniteInterruptedException(e);
    }
    v=peekValue(VAL_COL);
  }
  return v;
}
