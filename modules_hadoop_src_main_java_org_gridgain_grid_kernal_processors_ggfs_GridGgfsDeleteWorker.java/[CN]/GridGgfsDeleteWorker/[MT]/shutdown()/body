{
  cancelled=true;
  if (log.isDebugEnabled())   log.debug("Delete worker cancelled.");
  signal();
  try {
    try {
      join(FREQUENCY);
    }
  finally {
      interrupt();
      join();
    }
  }
 catch (  InterruptedException ignored) {
    currentThread().interrupt();
  }
}
