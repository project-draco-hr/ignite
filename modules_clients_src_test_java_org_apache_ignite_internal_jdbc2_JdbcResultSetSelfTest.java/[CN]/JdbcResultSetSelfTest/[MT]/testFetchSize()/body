{
  stmt.setFetchSize(1);
  ResultSet rs=stmt.executeQuery("select * from TestObject where id > 0");
  assertTrue(rs.next());
  assertTrue(rs.next());
  assertTrue(rs.next());
  stmt.setFetchSize(0);
}
