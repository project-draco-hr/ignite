{
  for (  PojoField field : fields) {
    String javaTypeName=field.javaTypeName();
    if (javaTypeName.startsWith("java.lang."))     javaTypeName=javaTypeName.substring(10);
    src.add(owner + ".add(new CacheTypeFieldMetadata(\"" + field.dbName()+ "\", "+ "java.sql.Types."+ field.dbTypeName()+ ",\""+ field.javaName()+ "\", "+ javaTypeName+ ".class));");
  }
}
