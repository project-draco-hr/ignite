{
  for (int i=0; i < 1000000; i++)   doIteration();
  try {
    barrier.await();
  }
 catch (  InterruptedException ignored) {
    return;
  }
catch (  BrokenBarrierException e) {
    e.printStackTrace();
    return;
  }
  while (!interrupted()) {
    doIteration();
    iterations++;
  }
}
