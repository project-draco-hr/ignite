{
  try {
    if (t.nextToken().equals(str))     return true;
 else     throw new GridException("Invalid date format: " + date);
  }
 catch (  NoSuchElementException ignored) {
    return false;
  }
}
