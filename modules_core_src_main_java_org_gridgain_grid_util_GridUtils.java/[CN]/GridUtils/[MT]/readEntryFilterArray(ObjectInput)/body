{
  int len=in.readInt();
  GridPredicate<GridCacheEntry<K,V>>[] arr=null;
  if (len > 0) {
    arr=new GridPredicate[len];
    for (int i=0; i < len; i++)     arr[i]=(GridPredicate<GridCacheEntry<K,V>>)in.readObject();
  }
  return arr;
}
