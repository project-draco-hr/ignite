{
  int len=in.readInt();
  IgnitePredicate<GridCacheEntry<K,V>>[] arr=null;
  if (len > 0) {
    arr=new IgnitePredicate[len];
    for (int i=0; i < len; i++)     arr[i]=(IgnitePredicate<GridCacheEntry<K,V>>)in.readObject();
  }
  return arr;
}
