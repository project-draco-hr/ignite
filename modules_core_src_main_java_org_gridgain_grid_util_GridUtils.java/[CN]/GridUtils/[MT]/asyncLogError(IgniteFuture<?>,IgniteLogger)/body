{
  if (f != null)   f.listenAsync(new CI1<IgniteFuture<?>>(){
    @Override public void apply(    IgniteFuture<?> f){
      try {
        f.get();
      }
 catch (      GridException e) {
        U.error(log,"Failed to execute future: " + f,e);
      }
    }
  }
);
}
