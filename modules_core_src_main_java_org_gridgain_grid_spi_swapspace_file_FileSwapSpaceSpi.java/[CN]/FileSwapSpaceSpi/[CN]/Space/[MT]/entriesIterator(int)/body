{
  Map<SwapKey,SwapValue> partMap=partition(part,false);
  if (partMap == null)   return Collections.<Map.Entry<SwapKey,byte[]>>emptySet().iterator();
  return transform(partMap.entrySet().iterator());
}
