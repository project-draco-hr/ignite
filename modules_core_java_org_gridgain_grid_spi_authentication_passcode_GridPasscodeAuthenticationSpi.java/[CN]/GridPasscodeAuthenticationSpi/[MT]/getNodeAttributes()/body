{
  String passcode=passcodes.get(REMOTE_NODE);
  if (passcode != null)   return F.<String,Object>asMap(createSpiAttributeName(NODE_PASSCODE_ATTRIBUTE_KEY),passcode);
  return Collections.emptyMap();
}
