{
  off=fieldOffset(fieldName);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != LONG_ARR)     throw new GridPortableException("Invalid flag value: " + flag);
    return doReadLongArray(false);
  }
 else   return null;
}
