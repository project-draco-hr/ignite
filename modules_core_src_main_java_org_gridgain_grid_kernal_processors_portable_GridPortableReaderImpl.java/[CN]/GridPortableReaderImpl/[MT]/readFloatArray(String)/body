{
  int fieldOff=fieldOffset(fieldName);
  if (fieldOff >= 0) {
    byte flag=readByte(fieldOff++);
    if (flag != FLOAT_ARR)     throw new GridPortableException("Invalid flag value: " + flag);
    return readFloatArray(fieldOff);
  }
 else   return null;
}
