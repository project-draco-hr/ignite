{
  int size=readInt();
  if (size >= 0) {
    Map<K,V> map=new HashMap<>(size);
    for (int i=0; i < size; i++)     map.put((K)readObject(),(V)readObject());
    return map;
  }
 else   return null;
}
