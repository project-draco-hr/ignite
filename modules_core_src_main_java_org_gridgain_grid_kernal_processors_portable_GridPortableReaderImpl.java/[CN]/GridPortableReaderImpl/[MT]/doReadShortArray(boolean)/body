{
  int len=doReadInt(raw);
  if (len >= 0) {
    short[] arr=PRIM.readShortArray(this.arr,raw ? rawOff : off,len);
    int bytes=len << 1;
    if (raw)     rawOff+=bytes;
 else     off+=bytes;
    return arr;
  }
 else   return null;
}
