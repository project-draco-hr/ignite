{
  off=fieldOffset(fieldName);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != LONG)     throw new GridPortableException("Invalid flag value: " + flag);
    return doReadLong(false);
  }
 else   return 0;
}
