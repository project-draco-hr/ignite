{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != FLOAT)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readFloat(fieldOff) : 0;
}
