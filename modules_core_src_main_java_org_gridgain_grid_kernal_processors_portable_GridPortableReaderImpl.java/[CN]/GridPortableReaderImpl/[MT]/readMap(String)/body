{
  off=fieldOffset(fieldName);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != BOOLEAN_ARR)     throw new GridPortableException("Invalid flag value: " + flag);
    return (Map<K,V>)doReadMap(false);
  }
 else   return null;
}
