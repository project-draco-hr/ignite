{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != INT)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readInt(fieldOff) : 0;
}
