{
  int fieldOff=fieldOffset(fieldName);
  if (fieldOff >= 0) {
    byte flag=readByte(fieldOff++);
    if (flag != INT_ARR)     throw new GridPortableException("Invalid flag value: " + flag);
    return readIntArray(fieldOff);
  }
 else   return null;
}
