{
  int res=dfltVal;
  if (obj instanceof Number)   res=((Number)obj).intValue();
 else   if (obj instanceof String) {
    try {
      res=Integer.parseInt((String)obj);
    }
 catch (    RuntimeException ignored) {
    }
  }
  return res;
}
