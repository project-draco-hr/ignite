{
  if (cfg.getCacheMode() == LOCAL)   return true;
  GridCacheDistributionMode partTax=cfg.getDistributionMode();
  if (partTax == null)   partTax=distributionMode(cfg);
  return partTax == GridCacheDistributionMode.PARTITIONED_ONLY || partTax == GridCacheDistributionMode.NEAR_PARTITIONED;
}
