{
  if (log.isDebugEnabled()) {
    if (e == null)     log.debug("ODBC client disconnected: " + ses.remoteAddress());
 else     log.debug("ODBC client disconnected due to an error [addr=" + ses.remoteAddress() + ", err="+ e+ ']');
  }
}
