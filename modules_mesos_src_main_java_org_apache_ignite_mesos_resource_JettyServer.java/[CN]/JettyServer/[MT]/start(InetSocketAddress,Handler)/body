{
  if (server == null) {
    server=new Server(address);
    server.setHandler(handler);
    server.start();
  }
 else   throw new IllegalStateException("Failed. Jetty server has been started already.");
}
