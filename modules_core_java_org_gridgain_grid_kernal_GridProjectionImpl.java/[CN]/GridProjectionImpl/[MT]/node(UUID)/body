{
  if (dynamic)   return super.node(nodeId);
  A.notNull(nodeId,"nodeId");
  guard();
  try {
    return ids.contains(nodeId) ? ctx.discovery().node(nodeId) : null;
  }
  finally {
    unguard();
  }
}
