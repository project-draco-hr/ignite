{
  start=U.currentTimeMillis();
  A arg=argument(0);
  try {
    if (debug)     logStart(g.log(),getClass(),start);
    return run(arg);
  }
  finally {
    if (debug)     logFinish(g.log(),getClass(),start);
  }
}
