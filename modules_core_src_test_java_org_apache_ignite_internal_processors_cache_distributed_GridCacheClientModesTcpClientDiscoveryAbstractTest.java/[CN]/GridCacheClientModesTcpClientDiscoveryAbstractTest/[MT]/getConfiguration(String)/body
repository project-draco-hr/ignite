{
  IgniteConfiguration cfg=super.getConfiguration(gridName);
  if (cfg.isClientMode() != null && cfg.isClientMode()) {
    cfg.setDiscoverySpi(new TcpDiscoverySpi().setClientMode(true).setIpFinder(ipFinder));
  }
  return cfg;
}
