{
  IgniteConfiguration cfg=super.getConfiguration(gridName);
  if (cfg.isClientMode() != null && cfg.isClientMode())   cfg.setDiscoverySpi(createClientDiscovery(ipFinder));
  cfg.setLocalHost("127.0.0.1");
  return cfg;
}
