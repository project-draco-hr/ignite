{
  List<String> cols=new LinkedList<>();
  if (!PersistenceStrategy.POJO.equals(settings.getStrategy())) {
    cols.add(settings.getColumn());
    return cols;
  }
  for (  PojoField field : settings.getFields())   cols.add(field.getColumn());
  return cols;
}
