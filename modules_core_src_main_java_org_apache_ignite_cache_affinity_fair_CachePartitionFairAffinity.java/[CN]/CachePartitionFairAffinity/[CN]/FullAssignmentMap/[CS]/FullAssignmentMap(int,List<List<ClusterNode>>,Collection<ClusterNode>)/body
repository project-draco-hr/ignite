{
  this.assignments=assignments;
  tierMaps=new Map[tiers];
  for (int tier=0; tier < tiers; tier++)   tierMaps[tier]=assignments(tier,assignments,topSnapshot);
  fullMap=assignments(-1,assignments,topSnapshot);
}
