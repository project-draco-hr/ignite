{
  GridCache<Integer,String> near=cache();
  near.lock(1,0);
  try {
    near.put(1,"1");
    near.put(2,"2");
    String one=near.put(1,"3");
    assertNotNull(one);
    assertEquals("1",one);
  }
  finally {
    near.unlock(1);
  }
}
