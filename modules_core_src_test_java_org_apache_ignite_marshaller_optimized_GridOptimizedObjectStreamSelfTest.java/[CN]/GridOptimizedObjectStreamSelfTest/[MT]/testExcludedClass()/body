{
  Class<?>[] exclClasses=U.staticField(GridMarshallerExclusions.class,"EXCL_CLASSES");
  assertFalse(F.isEmpty(exclClasses));
  for (  Class<?> cls : exclClasses)   assertEquals(cls,marshalUnmarshal(cls));
}
