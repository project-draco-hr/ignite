{
  rwLock.readLock().lock();
  try {
    Collection<GridTcpDiscoveryNode> filtered=F.isEmpty(excluded) ? nodes : F.view(nodes,F.notContains(excluded));
    if (F.isEmpty(filtered))     return null;
    return Collections.min(filtered);
  }
  finally {
    rwLock.readLock().unlock();
  }
}
