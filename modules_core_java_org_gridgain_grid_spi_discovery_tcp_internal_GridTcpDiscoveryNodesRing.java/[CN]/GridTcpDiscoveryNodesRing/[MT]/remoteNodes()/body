{
  rwLock.readLock().lock();
  try {
    return Collections.unmodifiableCollection(F.view(nodes,F.<GridTcpDiscoveryNode>remoteNodes(locNode.id())));
  }
  finally {
    rwLock.readLock().unlock();
  }
}
