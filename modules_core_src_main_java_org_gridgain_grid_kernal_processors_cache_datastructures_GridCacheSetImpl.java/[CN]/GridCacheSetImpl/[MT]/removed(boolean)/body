{
  if (this.rmvd)   return;
  this.rmvd=rmvd;
  if (rmvd) {
    for (    GridCacheQueryFuture<?> fut : itFuts.values()) {
      try {
        fut.cancel();
      }
 catch (      GridException e) {
        log.error("Failed to close iterator.",e);
      }
    }
    itFuts.clear();
  }
}
