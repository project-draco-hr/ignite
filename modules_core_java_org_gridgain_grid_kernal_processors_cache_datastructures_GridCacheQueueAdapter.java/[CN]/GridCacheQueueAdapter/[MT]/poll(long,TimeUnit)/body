{
  long end=U.currentTimeMillis() + MILLISECONDS.convert(timeout,unit);
  while (U.currentTimeMillis() < end) {
    T e=poll();
    if (e != null)     return e;
    if (Thread.interrupted())     throw new GridRuntimeException("Queue poll interrupted.");
  }
  return null;
}
