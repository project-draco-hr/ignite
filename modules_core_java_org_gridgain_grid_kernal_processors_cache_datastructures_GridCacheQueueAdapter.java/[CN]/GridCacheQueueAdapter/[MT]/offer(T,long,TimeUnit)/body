{
  long end=U.currentTimeMillis() + MILLISECONDS.convert(timeout,unit);
  while (U.currentTimeMillis() < end) {
    if (offer(item))     return true;
    if (Thread.interrupted())     throw new GridRuntimeException("Queue offer interrupted.");
  }
  return false;
}
