{
  if (queueRemoved(header,uuid) || !spaceAvailable(header,size)) {
    return header;
  }
  GridCacheQueueHeader ret=new GridCacheQueueHeader(header.uuid(),header.capacity(),header.collocated(),header.head(),header.tail() + size,header.removedIndexes());
  return ret;
}
