{
  if (queueRemoved(hdr,uuid) || !spaceAvailable(hdr,size))   return hdr;
  return new GridCacheQueueHeader(hdr.uuid(),hdr.capacity(),hdr.collocated(),hdr.head(),hdr.tail() + size,hdr.removedIndexes());
}
