{
  if (queueRemoved(header,uuid) || !spaceAvailable(header,size))   return header;
  return new GridCacheQueueHeader2(header.uuid(),header.capacity(),header.collocated(),header.head(),header.tail() + size);
}
