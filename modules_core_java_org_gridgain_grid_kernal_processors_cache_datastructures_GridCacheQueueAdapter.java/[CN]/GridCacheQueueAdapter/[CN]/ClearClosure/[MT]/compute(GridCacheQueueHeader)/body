{
  if (queueRemoved(header,uuid))   return new GridBiTuple<>(QUEUE_REMOVED_IDX,QUEUE_REMOVED_IDX);
  if (header.empty())   return null;
  return header.empty() ? null : new GridBiTuple<>(header.head(),header.tail());
}
