{
  if (fetched == null)   throw new IgniteException("Fetched result set was too large.");
  if (fetched.size() == cnt.get())   return findAllFetched(fetched,first,last);
  return findInStream(first,last);
}
