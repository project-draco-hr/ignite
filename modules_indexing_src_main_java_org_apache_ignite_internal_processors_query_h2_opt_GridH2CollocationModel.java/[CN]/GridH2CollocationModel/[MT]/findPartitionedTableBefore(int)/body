{
  for (int i=0; i < f; i++) {
    GridH2CollocationModel child=child(i,true);
    if (child != null && child.type(true).isPartitioned())     return true;
  }
  return upper != null && upper.findPartitionedTableBefore(filter);
}
