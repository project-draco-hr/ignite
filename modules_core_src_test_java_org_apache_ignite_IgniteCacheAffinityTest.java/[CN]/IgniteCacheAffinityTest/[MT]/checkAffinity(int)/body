{
  for (int i=0; i < n; ++i) {
    if (grid(i).cachex(null) != null)     checkAffinity(i,grid(i).<String,Integer>cache(null));
    if (grid(i).cachex(CACHE1) != null)     checkAffinity(i,grid(i).<String,Integer>cache(null));
    if (grid(i).cachex(CACHE2) != null)     checkAffinity(i,grid(i).<String,Integer>cache(null));
  }
}
