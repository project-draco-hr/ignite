{
  checkObsolete();
  if (dhtVer == null)   return null;
 else {
    V val0=null;
    byte[] valBytes0=null;
    GridCacheValueBytes valBytesTuple=valueBytes();
    if (!valBytesTuple.isNull()) {
      if (valBytesTuple.isPlain())       val0=(V)valBytesTuple.get();
 else       valBytes0=valBytesTuple.get();
    }
 else     val0=val;
    return F.t(dhtVer,val0,valBytes0);
  }
}
