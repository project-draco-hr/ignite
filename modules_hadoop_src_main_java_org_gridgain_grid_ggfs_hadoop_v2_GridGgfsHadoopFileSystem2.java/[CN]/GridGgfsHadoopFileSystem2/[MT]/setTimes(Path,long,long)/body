{
  if (mode(f) == PROXY)   secondaryFs.setTimes(f,mtime,atime);
 else {
    if (mtime == -1 && atime == -1)     return;
    try {
      rmtClient.setTimes(convert(f),atime,mtime);
    }
 catch (    GridException e) {
      throw cast(e);
    }
  }
}
