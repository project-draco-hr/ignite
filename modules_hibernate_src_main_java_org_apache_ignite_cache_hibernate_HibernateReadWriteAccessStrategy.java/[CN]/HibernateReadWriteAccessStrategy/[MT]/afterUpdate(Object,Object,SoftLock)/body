{
  try {
    TxContext ctx=txCtx.get();
    if (ctx != null) {
      cache.putx(key,val);
      unlock(ctx,key);
      return true;
    }
    return false;
  }
 catch (  Exception e) {
    rollbackCurrentTx();
    throw new CacheException(e);
  }
}
