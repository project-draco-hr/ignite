{
  boolean success=false;
  try {
    TxContext ctx=txCtx.get();
    if (ctx != null)     cache.remove(key);
    success=true;
  }
 catch (  IgniteCheckedException e) {
    throw new CacheException(e);
  }
 finally {
    if (!success)     rollbackCurrentTx();
  }
}
