{
  if (joiningNodeId.equals(ctx.localNodeId())) {
    Map<String,Object> map=(Map<String,Object>)data;
    if (map != null && map.containsKey(ATTR_UPDATE_NOTIFIER_STATUS))     notifyEnabled.set((Boolean)map.get(ATTR_UPDATE_NOTIFIER_STATUS));
  }
}
