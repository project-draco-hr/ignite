{
  try (InputStream targetIS=new FileInputStream(target);InputStream srcIS=src.openStream()){
    String targetMD5=calculateMD5(targetIS);
    String srcMD5=calculateMD5(srcIS);
    return targetMD5.equals(srcMD5);
  }
 }
