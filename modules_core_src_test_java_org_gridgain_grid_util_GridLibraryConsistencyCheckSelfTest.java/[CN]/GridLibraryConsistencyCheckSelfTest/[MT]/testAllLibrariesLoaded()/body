{
  ArrayList<String> libs=libraries();
  int idx=libs.indexOf(NOT_FOUND_MESSAGE);
  boolean testPassed=idx == -1;
  String msg=testPassed ? "" : "Library with class " + CLASS_LIST[idx] + " is removed, if it is made on purpose then remove this lib from "+ GridLibraryConsistencyCheck.class.getSimpleName()+ " class.";
  assertTrue(msg,testPassed);
}
