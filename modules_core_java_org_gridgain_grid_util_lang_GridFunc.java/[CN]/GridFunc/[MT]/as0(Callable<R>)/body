{
  A.notNull(c,"c");
  return new CO<R>(){
{
      peerDeployLike(U.peerDeployAware(c));
    }
    @Override public R apply(){
      try {
        return c.call();
      }
 catch (      Exception e) {
        throw wrap(e);
      }
    }
  }
;
}
