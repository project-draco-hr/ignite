{
  A.notNull(m,"m");
  return new C1<K,V>(){
{
      peerDeployLike(U.peerDeployAware0(m,c));
    }
    @Nullable @Override public V apply(    K k){
      return returnIfAbsent(m,k,c);
    }
  }
;
}
