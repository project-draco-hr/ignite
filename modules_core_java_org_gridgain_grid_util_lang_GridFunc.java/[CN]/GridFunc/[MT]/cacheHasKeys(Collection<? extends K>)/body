{
  return isEmpty(keys) ? F.<GridCacheEntry<K,V>>alwaysFalse() : new GridPredicate<GridCacheEntry<K,V>>(){
    @Override public boolean apply(    GridCacheEntry<K,V> e){
      return keys != null && keys.contains(e.getKey());
    }
  }
;
}
