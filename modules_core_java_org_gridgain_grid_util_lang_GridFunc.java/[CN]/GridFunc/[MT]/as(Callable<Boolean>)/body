{
  A.notNull(c,"c");
  return new GridAbsPredicate(){
{
      peerDeployLike(U.peerDeployAware(c));
    }
    @Override public boolean apply(){
      try {
        return c.call();
      }
 catch (      Exception e) {
        throw wrap(e);
      }
    }
  }
;
}
