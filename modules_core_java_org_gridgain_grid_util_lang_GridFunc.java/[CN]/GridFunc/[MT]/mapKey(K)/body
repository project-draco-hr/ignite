{
  return new P1<Map.Entry<K,V>>(){
{
      if (key != null)       peerDeployLike(U.peerDeployAware(key));
    }
    @Override public boolean apply(    Map.Entry<K,V> e){
      return e.getKey().equals(key);
    }
  }
;
}
