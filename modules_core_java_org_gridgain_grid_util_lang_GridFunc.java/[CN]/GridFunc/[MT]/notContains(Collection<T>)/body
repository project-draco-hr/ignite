{
  return c == null || c.isEmpty() ? GridFunc.<T>alwaysTrue() : new P1<T>(){
{
      if (c != null)       peerDeployLike(U.peerDeployAware0(c));
    }
    @Override public boolean apply(    T t){
      return !c.contains(t);
    }
  }
;
}
