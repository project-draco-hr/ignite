{
  A.notNull(cls,"cls");
  return new P1<T>(){
{
      if (cls != null) {
        peerDeployLike(cls);
      }
    }
    @Override public boolean apply(    T t){
      return t != null && cls.isAssignableFrom(t.getClass());
    }
  }
;
}
