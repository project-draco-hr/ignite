{
  return new P1<Map.Entry<K,V>>(){
{
      if (val != null)       peerDeployLike(U.peerDeployAware(val));
    }
    @Override public boolean apply(    Map.Entry<K,V> e){
      return e.getValue().equals(val);
    }
  }
;
}
