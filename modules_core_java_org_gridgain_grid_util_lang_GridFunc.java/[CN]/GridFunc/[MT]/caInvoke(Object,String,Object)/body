{
  A.notNull(o,"o",mtdName,"mtdName");
  return new CA(){
{
      if (o != null && args != null)       peerDeployLike(U.peerDeployAware0(flat0(o,args)));
 else       if (o != null && args == null)       peerDeployLike(o);
 else       if (o == null && args != null)       peerDeployLike(U.peerDeployAware0(args));
    }
    private Method mtd;
    @SuppressWarnings("unchecked") @Override public void apply(){
      try {
        if (mtd == null) {
          mtd=method(o.getClass(),mtdName,args);
          mtd.setAccessible(true);
        }
        mtd.invoke(o,args);
      }
 catch (      Throwable e) {
        throw wrap(e);
      }
    }
  }
;
}
