{
  A.notNull(fut,"fut");
  return new CO<T>(){
{
      peerDeployLike(U.peerDeployAware(fut));
    }
    @Override public T apply(){
      try {
        return fut.get();
      }
 catch (      Exception e) {
        throw new GridRuntimeException(e);
      }
    }
  }
;
}
