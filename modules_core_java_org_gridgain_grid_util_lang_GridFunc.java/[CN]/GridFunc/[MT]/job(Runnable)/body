{
  A.notNull(r,"job");
  if (r instanceof GridComputeJob)   return (GridComputeJob)r;
  return new GridComputeJobAdapter(){
{
      peerDeployLike(U.peerDeployAware(r));
    }
    @Nullable @Override public Object execute(){
      r.run();
      return null;
    }
  }
;
}
