{
  if (isEmpty(c) || isAlwaysFalse(p))   return Collections.emptyList();
  Collection<T> res=null;
  for (  T t : c)   if (isAll(t,p)) {
    if (res == null)     res=new ArrayList<>();
    res.add(t);
  }
  return res == null ? Collections.<T>emptyList() : res;
}
