{
  try {
    if (clientNode)     return clientMetaDataCache.get(new PortableMetaDataKey(typeId));
    return metaDataCache.localPeek(new PortableMetaDataKey(typeId));
  }
 catch (  CacheException e) {
    throw new PortableException(e);
  }
}
