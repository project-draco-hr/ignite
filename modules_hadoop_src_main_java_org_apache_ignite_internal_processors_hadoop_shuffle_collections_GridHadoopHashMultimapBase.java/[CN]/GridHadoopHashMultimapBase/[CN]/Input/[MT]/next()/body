{
  if (metaPtr != 0) {
    metaPtr=collision(metaPtr);
    if (metaPtr != 0)     return true;
  }
  while (++idx < cap) {
    metaPtr=meta(idx);
    if (metaPtr != 0)     return true;
  }
  return false;
}
