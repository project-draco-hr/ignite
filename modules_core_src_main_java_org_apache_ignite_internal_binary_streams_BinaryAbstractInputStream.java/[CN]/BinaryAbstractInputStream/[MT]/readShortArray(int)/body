{
  int len=cnt << 1;
  ensureEnoughData(len);
  short[] res=new short[cnt];
  copyAndShift(res,GridUnsafe.SHORT_ARR_OFF,len);
  if (BIG_ENDIAN) {
    for (int i=0; i < res.length; i++)     res[i]=Short.reverseBytes(res[i]);
  }
  return res;
}
