{
  GridCacheContext ctx=((GridKernal)g).internalCache().context();
  Field qryItersField=ReflectionUtils.findField(ctx.queries().getClass(),"qryIters");
  qryItersField.setAccessible(true);
  return (ConcurrentMap<UUID,Map<Long,GridFutureAdapter<GridCloseableIterator<GridIndexingKeyValueRow<CacheKey,CacheValue>>>>>)ReflectionUtils.getField(qryItersField,ctx.queries());
}
