{
  Set<String> unsignedTypes=new HashSet<>();
  try (ResultSet typeRs=dbMeta.getTypeInfo()){
    while (typeRs.next()) {
      String typeName=typeRs.getString(TYPE_NAME_IDX);
      if (typeName.contains("UNSIGNED"))       unsignedTypes.add(typeName);
    }
  }
   return unsignedTypes;
}
