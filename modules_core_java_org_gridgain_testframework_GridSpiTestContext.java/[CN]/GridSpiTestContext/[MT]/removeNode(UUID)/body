{
  for (Iterator<GridNode> iter=rmtNodes.iterator(); iter.hasNext(); ) {
    GridNode node=iter.next();
    if (node.id().equals(nodeId)) {
      iter.remove();
      notifyListener(new GridDiscoveryEvent(locNode.id(),"Node left",EVT_NODE_LEFT,node.id()));
    }
  }
}
