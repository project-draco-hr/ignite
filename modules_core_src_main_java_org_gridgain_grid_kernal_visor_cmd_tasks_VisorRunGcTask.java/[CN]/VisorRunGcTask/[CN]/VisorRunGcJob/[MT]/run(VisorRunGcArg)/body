{
  GridNode locNode=g.localNode();
  GridNodeMetrics m=locNode.metrics();
  long before=freeHeap(m);
  System.gc();
  if (arg.dgc())   for (  GridCache<?,?> cache : g.cachesx(null))   cache.dgc();
  return new VisorBeforeAfterResult(before,freeHeap(m));
}
