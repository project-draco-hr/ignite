{
  GridNode locNode=g.localNode();
  long before=freeHeap(locNode);
  System.gc();
  if (arg.dgc)   for (  GridCache<?,?> cache : g.cachesx())   cache.dgc();
  return new T2<>(before,freeHeap(locNode));
}
