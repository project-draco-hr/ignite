{
  Collection<GridNode> nodes=nodes(grid);
  if (qryLog.isDebugEnabled())   qryLog.debug(U.compact("Executing query with visitor on nodes: " + toShortString(nodes)));
  return execute(nodes,false,false,null,vis);
}
