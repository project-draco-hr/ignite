{
  Collection<GridNode> nodes=nodes(grid);
  if (qryLog.isDebugEnabled())   qryLog.debug(U.compact("Executing query on nodes: " + toShortString(nodes)));
  return executeSync(nodes,false,false,null,null);
}
