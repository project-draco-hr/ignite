{
  GridCacheQueryAdapter<T> cp=copy0();
  cp.log=log;
  cp.pageSize=pageSize;
  cp.timeout=timeout;
  cp.keepAll=keepAll;
  cp.incBackups=incBackups;
  cp.dedup=dedup;
  cp.prj=prj;
  cp.filter=filter;
  return cp;
}
