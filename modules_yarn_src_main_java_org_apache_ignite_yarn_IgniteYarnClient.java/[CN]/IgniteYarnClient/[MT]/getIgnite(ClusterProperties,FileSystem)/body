{
  IgniteProvider provider=new IgniteProvider(props,fileSystem);
  if (props.igniteVer() == null || props.igniteVer().equalsIgnoreCase(ClusterProperties.DEFAULT_IGNITE_VERSION))   return provider.getIgnite();
 else   return provider.getIgnite(props.igniteVer());
}
