{
  if (findFieldById(fieldId)) {
    if (checkFlag(PORTABLE_OBJ) == Flag.NULL)     return null;
    return new BinaryObjectImpl(ctx,BinaryUtils.doReadByteArray(in),in.readInt());
  }
 else   return null;
}
