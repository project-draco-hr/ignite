{
  if (ttl >= 0L)   return CU.toExpireTime(ttl);
  while (true) {
    try {
      GridCacheEntryEx<K,V> entry=unwrapNoCreate();
      return entry != null ? entry.expireTime() : 0L;
    }
 catch (    GridCacheEntryRemovedException ignore) {
      reset();
    }
  }
}
