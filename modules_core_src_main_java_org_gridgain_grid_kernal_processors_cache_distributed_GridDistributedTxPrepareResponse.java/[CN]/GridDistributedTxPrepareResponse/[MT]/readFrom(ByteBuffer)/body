{
  commState.setBuffer(buf);
  if (!super.readFrom(buf))   return false;
switch (commState.idx) {
case 8:
    byte[] candsBytes0=commState.getByteArray("candsBytes");
  if (candsBytes0 == BYTE_ARR_NOT_READ)   return false;
candsBytes=candsBytes0;
commState.idx++;
case 9:
byte[] errBytes0=commState.getByteArray("errBytes");
if (errBytes0 == BYTE_ARR_NOT_READ) return false;
errBytes=errBytes0;
commState.idx++;
}
return true;
}
