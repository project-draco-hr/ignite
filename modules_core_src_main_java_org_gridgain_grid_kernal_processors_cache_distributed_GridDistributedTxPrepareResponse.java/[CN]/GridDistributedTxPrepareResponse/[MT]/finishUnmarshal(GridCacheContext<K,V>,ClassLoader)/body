{
  super.finishUnmarshal(ctx,ldr);
  if (candsBytes != null && cands == null)   cands=ctx.marshaller().unmarshal(candsBytes,ldr);
  if (errBytes != null)   err=ctx.marshaller().unmarshal(errBytes,ldr);
}
