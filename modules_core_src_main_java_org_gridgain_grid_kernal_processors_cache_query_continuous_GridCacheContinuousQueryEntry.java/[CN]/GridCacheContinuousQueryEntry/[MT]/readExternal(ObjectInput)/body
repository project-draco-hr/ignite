{
  boolean b=in.readBoolean();
  if (b) {
    keyBytes=U.readByteArray(in);
    if (in.readBoolean())     valBytes=in.readBoolean() ? plain(U.readByteArray(in)) : marshaled(U.readByteArray(in));
    cacheName=U.readString(in);
    depInfo=(GridDeploymentInfo)in.readObject();
  }
 else {
    key=(K)in.readObject();
    val=(V)in.readObject();
  }
}
