{
  A.notNull(job,"job");
  A.notNull(args,"args");
  guard();
  try {
    return (Collection<R>)asyncSup.saveOrGet(ctx.closure().callAsync(job,args,prj.nodes()));
  }
  finally {
    unguard();
  }
}
