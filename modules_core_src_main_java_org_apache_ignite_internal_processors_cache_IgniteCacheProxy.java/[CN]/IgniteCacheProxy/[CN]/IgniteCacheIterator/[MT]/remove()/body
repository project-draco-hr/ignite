{
  if (curIter == null)   throw new IllegalStateException();
  try {
    delegate.removex(curIter.getKey());
  }
 catch (  IgniteCheckedException e) {
    throw cacheException(e);
  }
  curIter=null;
}
