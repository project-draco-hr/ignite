{
  if (curEntry == null)   throw new IllegalStateException();
  try {
    delegate.removex(curEntry.getKey());
  }
 catch (  IgniteCheckedException e) {
    throw cacheException(e);
  }
  curEntry=null;
}
