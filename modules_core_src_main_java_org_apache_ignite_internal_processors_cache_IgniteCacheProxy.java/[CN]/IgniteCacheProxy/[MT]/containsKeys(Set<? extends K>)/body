{
  CacheProjectionContext prev=gate.enter(prjCtx);
  try {
    if (isAsync()) {
      setFuture(delegate.containsKeysAsync(keys));
      return false;
    }
 else     return delegate.containsKeys(keys);
  }
  finally {
    gate.leave(prev);
  }
}
