{
  try {
    CacheOperationContext prev=gate.enter(prjCtx);
    try {
      if (isAsync())       setFuture(delegate.putAsync(key,val));
 else       delegate.put(key,val);
    }
  finally {
      gate.leave(prev);
    }
  }
 catch (  IgniteCheckedException e) {
    throw cacheException(e);
  }
}
