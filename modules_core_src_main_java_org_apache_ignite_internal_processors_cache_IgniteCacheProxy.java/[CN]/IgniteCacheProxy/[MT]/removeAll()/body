{
  GridCacheProjectionImpl<K,V> prev=gate.enter(prj);
  try {
    delegate.removeAll();
  }
 catch (  IgniteCheckedException e) {
    throw cacheException(e);
  }
 finally {
    gate.leave(prev);
  }
}
