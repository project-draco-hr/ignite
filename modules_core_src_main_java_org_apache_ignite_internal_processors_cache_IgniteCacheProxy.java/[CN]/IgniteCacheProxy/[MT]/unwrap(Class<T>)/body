{
  if (clazz.isAssignableFrom(IgniteCache.class))   return (T)this;
 else   if (clazz.isAssignableFrom(Ignite.class))   return (T)ctx.grid();
  throw new IllegalArgumentException("Unwrapping to class is not supported: " + clazz);
}
