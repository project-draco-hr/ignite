{
  try {
    GridCacheProjectionImpl<K,V> prev=onEnter(prj);
    try {
      if (isAsync()) {
        setFuture(delegate.getAndRemoveAsync(key));
        return null;
      }
 else       return delegate.getAndRemove(key);
    }
  finally {
      onLeave(prev);
    }
  }
 catch (  IgniteCheckedException e) {
    throw cacheException(e);
  }
}
