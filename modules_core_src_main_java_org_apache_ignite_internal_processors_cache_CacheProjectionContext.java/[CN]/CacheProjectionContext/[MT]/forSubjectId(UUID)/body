{
  A.notNull(subjId,"subjId");
  CacheProjectionContext<K,V> prj=new CacheProjectionContext<>(this,cctx,skipStore,subjId,keepPortable,expiryPlc);
  return new GridCacheProxyImpl<>(cctx,cache,prj);
}
