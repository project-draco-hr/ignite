{
  metricsCache=cfg.isEnableMetricsCache();
  attrCache=cfg.isEnableAttributesCache();
  prot=cfg.getProtocol();
  routerAddrs=(!cfg.getRouters().isEmpty() && cfg.getServers().isEmpty()) ? new HashSet<>(cfg.getRouters()) : null;
}
