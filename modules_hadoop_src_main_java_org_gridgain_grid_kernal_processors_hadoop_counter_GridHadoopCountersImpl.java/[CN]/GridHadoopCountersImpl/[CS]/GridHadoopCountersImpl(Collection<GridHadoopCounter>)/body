{
  this.cntrsMap=new HashMap<>(Math.max(MINIMAL_CAPACITY,cntrs.size()));
  List<GridHadoopCounter> copy=new ArrayList<>(cntrs.size());
  for (  GridHadoopCounter cntr : cntrs)   copy.add(new GridHadoopCounterImpl(cntr.group(),cntr.name(),cntr.value()));
  addCounters(copy);
}
