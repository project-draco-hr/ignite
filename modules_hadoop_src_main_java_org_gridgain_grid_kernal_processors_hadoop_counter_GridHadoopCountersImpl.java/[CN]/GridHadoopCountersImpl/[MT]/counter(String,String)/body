{
  GridHadoopCounter counter=new GridHadoopCounterImpl(group,name);
  GridHadoopCounter oldCounter=countersMap.putIfAbsent(new T2<>(group,name),counter);
  if (oldCounter != null)   counter=oldCounter;
  return counter;
}
