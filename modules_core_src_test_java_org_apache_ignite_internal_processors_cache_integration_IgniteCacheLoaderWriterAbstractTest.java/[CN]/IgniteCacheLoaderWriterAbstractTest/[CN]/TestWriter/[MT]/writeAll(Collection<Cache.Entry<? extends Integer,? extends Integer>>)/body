{
  assertTrue(startCalled);
  assertNotNull(ignite);
  log.info("WriteAll: " + entries);
  writerCallCnt.incrementAndGet();
  for (  Cache.Entry<? extends Integer,? extends Integer> e : entries)   storeMap.put(e.getKey(),e.getValue());
}
