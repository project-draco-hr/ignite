{
  if (qry instanceof Select)   return parse((Select)qry);
  if (qry instanceof SelectUnion)   return parse((SelectUnion)qry);
  if (qry instanceof Explain)   return parse(EXPLAIN_COMMAND.get((Explain)qry)).explain(true);
  throw new UnsupportedOperationException("Unknown query type: " + qry);
}
