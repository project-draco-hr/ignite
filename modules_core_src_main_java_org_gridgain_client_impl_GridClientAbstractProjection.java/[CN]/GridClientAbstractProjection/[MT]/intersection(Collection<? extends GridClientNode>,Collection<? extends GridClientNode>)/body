{
  if (first == null && second == null)   return null;
  if (first != null && second != null) {
    Collection<GridClientNode> res=new LinkedList<>(first);
    res.retainAll(second);
    return res;
  }
 else   return new ArrayList<>(first != null ? first : second);
}
