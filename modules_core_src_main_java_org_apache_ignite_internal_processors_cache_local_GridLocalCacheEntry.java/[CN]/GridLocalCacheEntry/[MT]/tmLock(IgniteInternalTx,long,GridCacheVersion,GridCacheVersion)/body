{
  GridCacheMvccCandidate cand=addLocal(tx.threadId(),tx.xidVersion(),serOrder,serReadVer,timeout,false,true,tx.implicitSingle());
  if (cand != null) {
    readyLocal(cand);
    return true;
  }
  return false;
}
