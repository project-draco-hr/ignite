{
  IgniteCache<Integer,String> cache=jcache();
  for (int i=0; i < 10; i++)   cache.put(i,String.valueOf(i));
  for (int i=0; i < 10; i++)   assertEquals(String.valueOf(i),cache.get(i));
  for (int i=0; i < 10; i++)   cache.remove(i);
  for (int i=0; i < 10; i++)   assertNull(cache.get(i));
}
