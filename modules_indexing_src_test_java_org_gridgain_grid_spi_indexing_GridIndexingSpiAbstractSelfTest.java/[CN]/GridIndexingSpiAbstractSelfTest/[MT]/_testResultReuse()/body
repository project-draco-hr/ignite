{
  final X spi=getSpi();
  multithreaded(new Callable<Object>(){
    @Override public Object call() throws Exception {
      return spi.queryFields(null,"SELECT sum(x) + sum(x) + sum(x) + sum(x) FROM SYSTEM_RANGE(?, ?)",F.<Object>asList(0,7000000));
    }
  }
,5);
}
