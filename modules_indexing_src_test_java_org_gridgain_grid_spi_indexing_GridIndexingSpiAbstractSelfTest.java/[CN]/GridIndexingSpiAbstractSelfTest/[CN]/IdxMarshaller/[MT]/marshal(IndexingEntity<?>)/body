{
  if (entity.bytes() != null)   return entity.bytes();
  ByteArrayOutputStream out=new ByteArrayOutputStream();
  try {
    marshaller.marshal(entity.value(),out);
  }
 catch (  GridException e) {
    throw new IgniteSpiException(e);
  }
  return out.toByteArray();
}
