{
  try {
    IgniteStreamer streamer=g.streamer(streamerName);
    streamer.resetMetrics();
    return null;
  }
 catch (  IllegalArgumentException iae) {
    throw new IgniteCheckedException("Failed to reset metrics for streamer: " + escapeName(streamerName) + " on node: "+ g.localNode().id(),iae);
  }
}
