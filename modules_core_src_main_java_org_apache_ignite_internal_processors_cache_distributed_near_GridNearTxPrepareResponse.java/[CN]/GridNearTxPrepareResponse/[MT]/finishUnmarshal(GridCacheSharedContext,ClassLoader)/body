{
  super.finishUnmarshal(ctx,ldr);
  if (ownedValsBytes != null && ownedVals == null) {
  }
  if (retVal == null && retValBytes != null)   retVal=ctx.marshaller().unmarshal(retValBytes,ldr);
  if (filterFailedKeys == null && filterFailedKeyBytes != null)   filterFailedKeys=ctx.marshaller().unmarshal(filterFailedKeyBytes,ldr);
}
