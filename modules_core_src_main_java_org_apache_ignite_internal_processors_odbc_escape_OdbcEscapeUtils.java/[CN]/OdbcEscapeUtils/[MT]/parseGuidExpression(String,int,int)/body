{
  String val=substring(text,startPos,len).trim();
  if (!GUID_PATTERN.matcher(val).matches())   throw new IgniteException("Invalid GUID escape sequence: " + substring(text,startPos,len));
  return val;
}
