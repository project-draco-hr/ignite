{
  Collection<IgniteCacheManager> mgrs=new ArrayList<>();
synchronized (cacheManagers) {
    for (    Map<URI,IgniteCacheManager> uriMap : cacheManagers.values())     mgrs.addAll(uriMap.values());
    cacheManagers.clear();
  }
  for (  IgniteCacheManager mgr : mgrs)   mgr.close();
}
