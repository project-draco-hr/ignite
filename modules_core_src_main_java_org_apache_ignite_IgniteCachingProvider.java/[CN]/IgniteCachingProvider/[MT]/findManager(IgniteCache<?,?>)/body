{
  Ignite ignite=cache.ignite();
synchronized (cacheManagers) {
    for (    Map<URI,IgniteCacheManager> map : cacheManagers.values()) {
      for (      IgniteCacheManager manager : map.values()) {
        if (manager.isManagedIgnite(ignite))         return manager;
      }
    }
  }
  return null;
}
