{
  CountDownLatch latch=new CountDownLatch(1);
  for (; ; ) {
    CountDownLatch l=keyLocks.putIfAbsent(k,latch);
    if (l == null)     return latch;
    try {
      l.await();
    }
 catch (    InterruptedException e) {
      throw new RuntimeException(e);
    }
  }
}
