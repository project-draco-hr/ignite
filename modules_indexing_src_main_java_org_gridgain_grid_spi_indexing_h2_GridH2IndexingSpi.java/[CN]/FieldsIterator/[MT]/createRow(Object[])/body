{
  List<GridIndexingEntity<?>> row=new ArrayList<>(arr.length);
  for (  Object val : arr) {
    row.add(val instanceof GridIndexingEntity ? (GridIndexingEntity<?>)val : new GridIndexingEntityAdapter<>(val,null));
  }
  return row;
}
