{
  List<IndexingEntity<?>> res=new ArrayList<>(row.length);
  for (  Object val : row) {
    res.add(val instanceof IndexingEntity ? (IndexingEntity<?>)val : new IndexingEntityAdapter<>(val,null));
  }
  return res;
}
