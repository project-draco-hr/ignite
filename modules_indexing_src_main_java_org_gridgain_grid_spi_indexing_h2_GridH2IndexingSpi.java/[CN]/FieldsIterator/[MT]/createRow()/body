{
  List<GridIndexingEntity<?>> res=new ArrayList<>(row.length);
  for (  Object val : row) {
    res.add(val instanceof GridIndexingEntity ? (GridIndexingEntity<?>)val : new GridIndexingEntityAdapter<>(val,null));
  }
  return res;
}
