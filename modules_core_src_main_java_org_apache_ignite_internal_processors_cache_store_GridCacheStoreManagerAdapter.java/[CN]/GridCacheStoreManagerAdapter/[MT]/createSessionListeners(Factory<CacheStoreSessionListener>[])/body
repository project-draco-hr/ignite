{
  if (factories == null)   return null;
  CacheStoreSessionListener[] lsnrs=new CacheStoreSessionListener[factories.length];
  for (int i=0; i < factories.length; i++)   lsnrs[i]=factories[i].create();
  return lsnrs;
}
