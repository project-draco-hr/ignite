{
  U.warn(log,"Next node will be forcibly failed (if any).");
  GridTcpDiscoveryNode next;
synchronized (mux) {
    next=ring.nextNode(failedNodes);
  }
  if (next != null)   msgWorker.addMessage(new GridTcpDiscoveryNodeFailedMessage(locNodeId,next.id(),next.internalOrder()));
}
