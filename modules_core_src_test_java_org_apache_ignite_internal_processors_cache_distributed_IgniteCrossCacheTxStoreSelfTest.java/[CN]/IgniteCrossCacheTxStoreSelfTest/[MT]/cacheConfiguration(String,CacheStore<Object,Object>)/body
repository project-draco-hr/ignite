{
  CacheConfiguration cfg=defaultCacheConfiguration();
  cfg.setNearConfiguration(null);
  cfg.setName(cacheName);
  cfg.setBackups(1);
  if (store != null) {
    cfg.setCacheStoreFactory(new FactoryBuilder.SingletonFactory<CacheStore<? super Object,? super Object>>(store));
    cfg.setWriteThrough(true);
  }
  return cfg;
}
