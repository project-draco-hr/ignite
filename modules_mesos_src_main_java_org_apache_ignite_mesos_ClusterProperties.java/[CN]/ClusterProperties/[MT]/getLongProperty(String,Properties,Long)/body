{
  if (fileProps != null && fileProps.containsKey(name))   return Long.valueOf(fileProps.getProperty(name));
  String prop=System.getProperty(name);
  if (prop == null)   prop=System.getenv(name);
  return prop == null ? dfltVal : Long.valueOf(prop);
}
