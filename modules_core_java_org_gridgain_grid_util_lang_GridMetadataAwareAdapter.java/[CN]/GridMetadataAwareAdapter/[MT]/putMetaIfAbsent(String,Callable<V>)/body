{
  A.notNull(name,"name",c,"c");
synchronized (mux) {
    V v=(V)meta(name);
    if (v == null)     try {
      return addMeta(name,c.call());
    }
 catch (    Exception e) {
      throw F.wrap(e);
    }
    return v;
  }
}
