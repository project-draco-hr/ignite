{
  final Collection<Entry<K,V>> col=(Collection<Entry<K,V>>)compute.call(new IgniteCallable<Object>(){
    @Override public Object call() throws Exception {
      Collection res=new ArrayList();
      for (      Object o : cache())       res.add(o);
      return res;
    }
  }
);
  return col.iterator();
}
