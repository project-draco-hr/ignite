{
  compute.run(new IgniteRunnable(){
    @Override public void run(){
      IgniteCache<Object,Object> cache=cache();
      cache.removeAll();
      if (isAsync)       cache.future().get();
    }
  }
);
}
