{
  A.notNull(r,"job");
  if (r instanceof ComputeJobMasterLeaveAware)   return new C4(r);
 else {
    return new ComputeJobAdapter(){
      @Nullable @Override public Object execute(){
        r.run();
        return null;
      }
    }
;
  }
}
