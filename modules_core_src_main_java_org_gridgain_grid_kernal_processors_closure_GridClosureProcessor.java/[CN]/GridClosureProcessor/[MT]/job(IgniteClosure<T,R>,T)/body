{
  A.notNull(job,"job");
  if (job instanceof ComputeJobMasterLeaveAware)   return new C1<>(job,arg);
 else {
    return new ComputeJobAdapter(){
      @Nullable @Override public Object execute(){
        return job.apply(arg);
      }
    }
;
  }
}
