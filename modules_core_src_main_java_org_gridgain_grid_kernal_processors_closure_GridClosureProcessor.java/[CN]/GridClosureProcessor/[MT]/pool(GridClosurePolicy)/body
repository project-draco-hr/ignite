{
switch (plc) {
case PUBLIC_POOL:
    return pubPool;
case SYSTEM_POOL:
  return sysPool;
case GGFS_POOL:
return ggfsPool;
default :
throw new IllegalArgumentException("Invalid closure execution policy: " + plc);
}
}
