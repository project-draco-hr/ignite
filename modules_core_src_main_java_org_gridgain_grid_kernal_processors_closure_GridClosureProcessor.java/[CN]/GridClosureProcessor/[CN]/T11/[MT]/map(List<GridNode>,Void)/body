{
  if (F.isEmpty(subgrid))   return Collections.emptyMap();
  Map<GridComputeJob,GridNode> map=new HashMap<>(subgrid.size(),1);
  JobMapper mapper=new JobMapper(map);
  for (  GridNode n : subgrid)   mapper.map(job(job,this.arg),n);
  return map;
}
