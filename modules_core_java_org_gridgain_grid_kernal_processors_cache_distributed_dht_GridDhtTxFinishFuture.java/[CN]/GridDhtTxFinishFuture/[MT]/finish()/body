{
  if (!F.isEmpty(dhtMap) || !F.isEmpty(nearMap)) {
    boolean sync=finish(dhtMap,nearMap);
    markInitialized();
    if (!sync)     onComplete();
  }
 else {
    markInitialized();
    onComplete();
  }
}
