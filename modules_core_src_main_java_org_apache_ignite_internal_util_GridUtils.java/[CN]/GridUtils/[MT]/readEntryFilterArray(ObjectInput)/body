{
  int len=in.readInt();
  IgnitePredicate<CacheEntry<K,V>>[] arr=null;
  if (len > 0) {
    arr=new IgnitePredicate[len];
    for (int i=0; i < len; i++)     arr[i]=(IgnitePredicate<CacheEntry<K,V>>)in.readObject();
  }
  return arr;
}
