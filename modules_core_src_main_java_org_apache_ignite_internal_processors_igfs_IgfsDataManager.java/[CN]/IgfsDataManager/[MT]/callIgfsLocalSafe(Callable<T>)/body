{
  try {
    igfsSvc.submit(c);
  }
 catch (  RejectedExecutionException ignored) {
    try {
      c.call();
    }
 catch (    Exception e) {
      log.warning("Failed to execute IGFS callable: " + c,e);
    }
  }
}
