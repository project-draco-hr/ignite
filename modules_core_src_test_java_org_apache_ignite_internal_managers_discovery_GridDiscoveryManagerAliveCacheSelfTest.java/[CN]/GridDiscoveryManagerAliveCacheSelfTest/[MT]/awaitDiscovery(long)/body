{
  for (  Ignite g : alive) {
    if (g.configuration().getDiscoverySpi() instanceof TcpClientDiscoverySpi)     ((TcpClientDiscoverySpi)g.configuration().getDiscoverySpi()).waitForMessagePrecessed();
    while (g.cluster().nodes().size() != nodesCnt)     Thread.sleep(10);
  }
}
