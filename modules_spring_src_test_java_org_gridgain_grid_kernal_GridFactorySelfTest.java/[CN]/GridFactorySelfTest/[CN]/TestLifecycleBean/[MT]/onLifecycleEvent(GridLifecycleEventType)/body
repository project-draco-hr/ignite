{
  evts.add(evt);
  gridNames.add(ignite.name());
  try {
    checkState(ignite.name(),evt == GridLifecycleEventType.AFTER_GRID_START || evt == GridLifecycleEventType.BEFORE_GRID_STOP);
  }
 catch (  Throwable e) {
    log.error("Lifecycle bean failed state check: " + this,e);
    err.compareAndSet(null,e);
  }
}
