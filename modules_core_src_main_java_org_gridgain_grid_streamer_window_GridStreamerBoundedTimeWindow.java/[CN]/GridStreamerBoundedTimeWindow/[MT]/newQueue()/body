{
  return new GridConcurrentSkipListSet<>(new Comparator<Holder>(){
    @Override public int compare(    Holder h1,    Holder h2){
      if (h1 == h2)       return 0;
      if (h1.ts != h2.ts)       return h1.ts < h2.ts ? -1 : 1;
      return h1.order < h2.order ? -1 : 1;
    }
  }
);
}
