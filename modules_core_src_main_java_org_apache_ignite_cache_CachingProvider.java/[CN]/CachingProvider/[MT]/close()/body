{
  Collection<CacheManager> mgrs=new ArrayList<>();
synchronized (cacheManagers) {
    for (    Map<URI,CacheManager> uriMap : cacheManagers.values())     mgrs.addAll(uriMap.values());
    cacheManagers.clear();
  }
  for (  CacheManager mgr : mgrs)   mgr.close();
}
