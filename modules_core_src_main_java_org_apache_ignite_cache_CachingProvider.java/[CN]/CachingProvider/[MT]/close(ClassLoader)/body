{
  Collection<CacheManager> mgrs;
synchronized (cacheManagers) {
    Map<URI,CacheManager> uriMap=cacheManagers.remove(clsLdr);
    if (uriMap == null)     return;
    mgrs=uriMap.values();
  }
  for (  CacheManager mgr : mgrs)   mgr.close();
}
