{
  GridPredicate<GridCacheEntry<K,V>> entryFilter=entryFilter(nonNulls);
  if (F.isEmpty(f1))   return entryFilter;
  GridPredicate<GridCacheEntry<K,V>>[] entryFilter0=(GridPredicate<GridCacheEntry<K,V>>[])Array.newInstance(entryFilter.getClass(),1);
  entryFilter0[0]=entryFilter;
  return F0.and(entryFilter0,f1);
}
