{
  GridCacheFlag[] forced=cctx.forcedFlags();
  if (F.isEmpty(forced))   return flags;
  Set<GridCacheFlag> ret=new HashSet<>();
  ret.addAll(flags);
  ret.addAll(F.asList(forced));
  return Collections.unmodifiableSet(ret);
}
