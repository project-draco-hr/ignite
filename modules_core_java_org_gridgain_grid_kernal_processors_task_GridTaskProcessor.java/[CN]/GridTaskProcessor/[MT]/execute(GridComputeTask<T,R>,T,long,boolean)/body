{
  lock.readLock();
  try {
    if (stopping)     throw new IllegalStateException("Failed to execute task due to grid shutdown: " + task);
    return startTask(null,null,task,GridUuid.fromUuid(ctx.localNodeId()),timeout,arg,sys);
  }
  finally {
    lock.readUnlock();
  }
}
