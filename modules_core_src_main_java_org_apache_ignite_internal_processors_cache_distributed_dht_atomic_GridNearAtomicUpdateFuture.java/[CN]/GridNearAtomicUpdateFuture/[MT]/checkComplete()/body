{
  if ((syncMode == FULL_ASYNC && cctx.config().getAtomicWriteOrderMode() == PRIMARY) || mappings.isEmpty()) {
    GridCachePartialUpdateException err0=err;
    if (err0 != null)     onDone(err0);
 else     onDone(opRes);
  }
}
