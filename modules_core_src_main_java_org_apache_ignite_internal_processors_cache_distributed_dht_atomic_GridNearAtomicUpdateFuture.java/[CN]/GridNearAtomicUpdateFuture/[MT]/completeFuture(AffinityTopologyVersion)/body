{
  if (waitForPartitionExchange()) {
    GridFutureAdapter<Void> fut=completeFuture0(topVer);
    if (fut != null && isDone()) {
      fut.onDone();
      return null;
    }
    return fut;
  }
  return null;
}
