{
  if (waitForPartitionExchange()) {
    GridFutureAdapter<Void> fut=state.completeFuture(topVer);
    if (fut != null && isDone()) {
      fut.onDone();
      return null;
    }
    return fut;
  }
  return null;
}
