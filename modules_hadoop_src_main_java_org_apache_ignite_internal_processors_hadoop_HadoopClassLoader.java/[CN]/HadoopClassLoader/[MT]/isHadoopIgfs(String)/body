{
  String ignitePkgPrefix="org.apache.ignite";
  int len=ignitePkgPrefix.length();
  return cls.startsWith(ignitePkgPrefix) && (cls.indexOf("igfs.",len) != -1 || cls.indexOf(".fs.",len) != -1 || cls.indexOf("hadoop.",len) != -1);
}
