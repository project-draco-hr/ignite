{
  if (ctx.found)   return;
  ctx.onInternalTypeName(superName);
  if (ctx.found)   return;
  if (ifaces != null) {
    for (    String iface : ifaces) {
      ctx.onInternalTypeName(iface);
      if (ctx.found)       return;
    }
  }
}
