{
  GridCache<?,?> cache=g.cache(CACHE_NAME);
  GridCacheQuery<List<?>> qry=cache.queries().createSqlFieldsQuery("select concat(firstName, ' ', " + "lastName) from AffinityPerson").projection(p);
  Collection<List<?>> res=qry.execute().get();
  print("Names of all employees:",res);
}
