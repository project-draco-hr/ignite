{
  Map<GridSwapKey,byte[]> batch=new LinkedHashMap<>();
  for (  GridCacheBatchSwapEntry entry : swapped)   batch.put(new GridSwapKey(entry.key(),entry.partition(),entry.keyBytes()),marshal(entry));
  getSpi().storeAll(spaceName,batch,context(ldr));
}
