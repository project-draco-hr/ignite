{
  removeStale();
  if (!store.isEmpty() && o != null) {
    for (    PhantomReferenceElement ref : store) {
      Object reft=ref.get();
      if (reft != null && reft.equals(o))       return true;
    }
  }
  return false;
}
