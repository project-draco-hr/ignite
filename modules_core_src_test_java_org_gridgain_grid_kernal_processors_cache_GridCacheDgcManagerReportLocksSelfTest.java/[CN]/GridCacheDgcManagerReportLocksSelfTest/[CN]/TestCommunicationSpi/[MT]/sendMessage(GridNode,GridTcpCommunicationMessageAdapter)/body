{
  if (isTxFinish((GridIoMessage)msg)) {
    if (log.isDebugEnabled())     log.debug("Delayed message send: " + msg);
    delayedMsgs.add(F.<GridNode,GridTcpCommunicationMessageAdapter>t(node,msg));
    return;
  }
  super.sendMessage(node,msg);
}
