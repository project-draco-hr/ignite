{
  GridTestUtils.assertThrows(log,new Callable<Object>(){
    @Override public Object call() throws Exception {
      return GridIpcServerEndpointDeserializer.deserialize(GridHadoopTestUtils.jsonToMap(JSONSerializer.toJSON(tcpSrvEndpoint).toString()));
    }
  }
,GridException.class,null);
}
