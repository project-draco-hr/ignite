{
  ListMap<K,V> updmap=listmap.remove(k);
  if (updmap.size() > 1)   return new LNode<K,V>(updmap);
 else {
    Entry<K,V> kv=updmap.iterator().next();
    return new TNode<K,V>(kv.getKey(),kv.getValue(),ct.computeHash(kv.getKey()));
  }
}
