{
  int i=0;
  int sz=0;
  final int offset=0;
  while (i < array.length) {
    int pos=(i + offset) % array.length;
    BasicNode elem=array[pos];
    if (elem instanceof SNode)     sz+=1;
 else     if (elem instanceof INode)     sz+=((INode<K,V>)elem).cachedSize(ct);
    i+=1;
  }
  return sz;
}
