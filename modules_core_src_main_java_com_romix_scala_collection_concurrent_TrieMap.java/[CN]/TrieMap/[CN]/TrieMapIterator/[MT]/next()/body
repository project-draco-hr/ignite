{
  if (hasNext()) {
    Entry<K,V> r=null;
    if (subiter != null) {
      r=subiter.next();
      checkSubiter();
    }
 else {
      r=current.kvPair();
      advance();
    }
    lastReturned=r;
    if (r instanceof Entry) {
      final Entry<K,V> rr=(Entry<K,V>)r;
      return nextEntry(rr);
    }
    return r;
  }
 else {
    return null;
  }
}
