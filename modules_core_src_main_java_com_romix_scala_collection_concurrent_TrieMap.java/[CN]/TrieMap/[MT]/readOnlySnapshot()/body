{
  if (!nonReadOnly())   return this;
  while (true) {
    INode<K,V> r=RDCSS_READ_ROOT();
    MainNode<K,V> expmain=r.gcasRead(this);
    if (RDCSS_ROOT(r,expmain,r.copyToGen(new Gen(),this)))     return new TrieMap<K,V>(r,null,hashing(),equality());
 else {
      continue;
    }
  }
}
