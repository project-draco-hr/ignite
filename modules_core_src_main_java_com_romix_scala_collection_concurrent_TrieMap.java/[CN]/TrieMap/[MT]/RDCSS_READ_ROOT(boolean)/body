{
  Object r=root;
  if (r instanceof INode)   return (INode<K,V>)r;
 else   if (r instanceof RDCSS_Descriptor) {
    return RDCSS_Complete(abort);
  }
  throw new RuntimeException("Should not happen");
}
