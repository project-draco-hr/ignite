{
  if (this.skipStore == skipStore)   return new GridCacheProxyImpl<>(cctx,this,this);
  GridCacheProjectionImpl<K,V> prj=new GridCacheProjectionImpl<>(this,cctx,filter,skipStore,subjId,keepPortable,expiryPlc);
  return new GridCacheProxyImpl<>(cctx,prj,prj);
}
