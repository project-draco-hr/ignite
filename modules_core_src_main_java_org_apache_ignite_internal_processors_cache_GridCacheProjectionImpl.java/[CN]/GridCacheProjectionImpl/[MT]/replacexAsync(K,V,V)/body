{
  A.notNull(key,"key",oldVal,"oldVal",newVal,"newVal");
  if (!isAll(key,newVal))   return new GridFinishedFuture<>(cctx.kernalContext(),new GridCacheReturn<V>(false));
  return cache.replacexAsync(key,oldVal,newVal);
}
