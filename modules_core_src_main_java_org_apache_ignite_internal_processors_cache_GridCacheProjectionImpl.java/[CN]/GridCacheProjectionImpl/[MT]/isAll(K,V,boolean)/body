{
  IgniteBiPredicate<K,V> p=kvFilter(noNulls);
  if (p != null) {
    CacheFlag[] f=cctx.forceLocalRead();
    try {
      if (!p.apply(k,v))       return false;
    }
  finally {
      cctx.forceFlags(f);
    }
  }
  return true;
}
