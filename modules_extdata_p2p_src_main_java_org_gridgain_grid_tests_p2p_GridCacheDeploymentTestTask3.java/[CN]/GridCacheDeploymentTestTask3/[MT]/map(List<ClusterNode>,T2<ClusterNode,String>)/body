{
  final String val=tup.getValue();
  return F.asMap(new ComputeJobAdapter(){
    @GridInstanceResource private Ignite ignite;
    @Override public Object execute() throws GridException {
      X.println("Executing GridCacheDeploymentTestTask3 job on node " + ignite.cluster().localNode().id());
      ignite.<String,GridCacheDeploymentTestValue>cache(null).putx(val,new GridCacheDeploymentTestValue());
      return null;
    }
  }
,tup.get1());
}
