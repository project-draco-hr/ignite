{
  final String val=tup.getValue();
  return F.asMap(new GridComputeJobAdapter(){
    @GridInstanceResource private Grid grid;
    @Override public Object execute() throws GridException {
      X.println("Executing GridCacheDeploymentTestTask3 job on node " + grid.cluster().localNode().id());
      grid.<String,GridCacheDeploymentTestValue>cache(null).putx(val,new GridCacheDeploymentTestValue());
      return null;
    }
  }
,tup.get1());
}
