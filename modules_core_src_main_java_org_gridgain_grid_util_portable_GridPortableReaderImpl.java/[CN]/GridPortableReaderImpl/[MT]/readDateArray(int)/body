{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != DATE_ARR)     throw new GridPortableException("Invalid flag value: " + flag);
    return doReadDateArray(false);
  }
 else   return null;
}
