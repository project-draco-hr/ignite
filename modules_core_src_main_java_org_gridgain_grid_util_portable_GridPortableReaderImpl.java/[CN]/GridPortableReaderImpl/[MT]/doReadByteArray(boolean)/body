{
  int len=doReadInt(raw);
  if (len >= 0) {
    byte[] arr=PRIM.readByteArray(this.arr,raw ? rawOff : off,len);
    if (raw)     rawOff+=len;
 else     off+=len;
    return arr;
  }
 else   return null;
}
