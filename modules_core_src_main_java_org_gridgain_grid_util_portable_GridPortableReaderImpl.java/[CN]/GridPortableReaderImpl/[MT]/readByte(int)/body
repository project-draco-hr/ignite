{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != BYTE)     throw new GridPortableException("Invalid flag value: " + flag);
    return doReadByte(false);
  }
 else   return 0;
}
