{
  int len=doReadInt(raw);
  if (len >= 0) {
    long[] arr=PRIM.readLongArray(this.arr,raw ? rawOff : off,len);
    int bytes=len << 3;
    if (raw)     rawOff+=bytes;
 else     off+=bytes;
    return arr;
  }
 else   return null;
}
