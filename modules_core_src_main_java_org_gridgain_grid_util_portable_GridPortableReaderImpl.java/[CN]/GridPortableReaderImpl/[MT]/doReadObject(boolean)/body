{
  GridPortableReaderImpl reader=new GridPortableReaderImpl(ctx,arr,raw ? rawOff : off,poHandles,oHandles);
  if (raw)   rawOff+=reader.length();
 else   off+=reader.length();
  return reader.deserialize();
}
