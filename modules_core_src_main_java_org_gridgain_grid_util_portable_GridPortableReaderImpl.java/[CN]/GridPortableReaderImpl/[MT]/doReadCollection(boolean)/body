{
  int size=doReadInt(raw);
  if (size >= 0) {
    Collection<Object> col=new ArrayList<>(size);
    for (int i=0; i < size; i++)     col.add(doReadObject(raw));
    return col;
  }
 else   return null;
}
