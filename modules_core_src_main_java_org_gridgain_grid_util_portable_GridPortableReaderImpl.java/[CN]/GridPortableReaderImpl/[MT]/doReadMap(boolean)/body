{
  int size=doReadInt(raw);
  if (size >= 0) {
    Map<Object,Object> col=new HashMap<>(size);
    for (int i=0; i < size; i++)     col.put(doReadObject(raw),doReadObject(raw));
    return col;
  }
 else   return null;
}
