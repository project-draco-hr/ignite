{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != MAP)     throw new GridPortableException("Invalid flag value: " + flag);
    return (Map<K,V>)doReadMap(false);
  }
 else   return null;
}
