{
  int len=doReadInt(raw);
  if (len >= 0) {
    boolean[] arr=PRIM.readBooleanArray(this.arr,raw ? rawOff : off,len);
    if (raw)     rawOff+=len;
 else     off+=len;
    return arr;
  }
 else   return null;
}
