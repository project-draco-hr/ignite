{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != DOUBLE)     throw new GridPortableException("Invalid flag value: " + flag);
    return doReadDouble(false);
  }
 else   return 0;
}
