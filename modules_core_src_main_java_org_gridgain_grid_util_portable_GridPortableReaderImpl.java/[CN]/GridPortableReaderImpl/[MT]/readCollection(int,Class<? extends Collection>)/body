{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != COL)     throw new GridPortableException("Invalid flag value: " + flag);
    return (Collection<T>)doReadCollection(false,cls);
  }
 else   return null;
}
