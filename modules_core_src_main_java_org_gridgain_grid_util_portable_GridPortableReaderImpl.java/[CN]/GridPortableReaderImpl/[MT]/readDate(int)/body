{
  off=fieldOffset(fieldId);
  if (off >= 0) {
    byte flag=doReadByte(false);
    if (flag != DATE)     throw new GridPortableException("Invalid flag value: " + flag);
    return doReadDate(false);
  }
 else   return null;
}
