{
  int len=doReadInt(raw);
  if (len >= 0) {
    Object[] arr=new Object[len];
    for (int i=0; i < len; i++)     arr[i]=deep ? doReadObject(raw) : unmarshal(raw);
    return arr;
  }
 else   return null;
}
