{
  cpy=cpy && needCopy(ctx);
  if (cpy) {
    try {
      return (T)ctx.processor().unmarshal(ctx,valBytes,val.getClass().getClassLoader());
    }
 catch (    IgniteCheckedException e) {
      throw new IgniteException("Failed to unmarshal object.",e);
    }
  }
  return (T)val;
}
