{
  boolean contains=false;
  for (  UUID nodeId : nodes) {
    if (topMap.get(nodeId) != null) {
      contains=true;
      break;
    }
  }
  if (!contains)   throw new IgniteCheckedException("Task topology does not have alive nodes: " + top);
}
