{
  if (e instanceof IgniteFsOutOfSpaceException)   onDone(new IgniteCheckedException("Failed to write data (not enough space on node): " + ggfsCtx.kernalContext().localNodeId(),e));
 else   onDone(new IgniteCheckedException("Failed to wait for write completion (write failed on node): " + ggfsCtx.kernalContext().localNodeId(),e));
}
