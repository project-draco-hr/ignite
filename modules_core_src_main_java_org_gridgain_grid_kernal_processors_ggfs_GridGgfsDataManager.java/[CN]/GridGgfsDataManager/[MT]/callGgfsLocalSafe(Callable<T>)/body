{
  try {
    ggfsSvc.submit(c);
  }
 catch (  RejectedExecutionException ignored) {
    try {
      c.call();
    }
 catch (    Exception e) {
      log.warning("Failed to execute GGFS callable: " + c,e);
    }
  }
}
