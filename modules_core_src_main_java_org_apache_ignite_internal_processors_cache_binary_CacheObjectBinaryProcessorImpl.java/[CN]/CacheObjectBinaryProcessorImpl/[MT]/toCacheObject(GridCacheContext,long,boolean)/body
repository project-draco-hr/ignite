{
  if (!((CacheObjectBinaryContext)ctx.cacheObjectContext()).binaryEnabled())   return super.toCacheObject(ctx,valPtr,tmp);
  Object val=unmarshal(valPtr,!tmp);
  if (val instanceof CacheObject)   return (CacheObject)val;
  return toCacheObject(ctx.cacheObjectContext(),val,false);
}
