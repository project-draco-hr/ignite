{
  try {
    QueryCursor<List<?>> qry=grid(0).jcache(null).queryFields(new QuerySqlPredicate("update Person set name = ?").setArgs("Mary Poppins"));
    qry.getAll();
    fail("We don't support updates.");
  }
 catch (  Exception e) {
    X.println("___ " + e.getMessage());
  }
}
