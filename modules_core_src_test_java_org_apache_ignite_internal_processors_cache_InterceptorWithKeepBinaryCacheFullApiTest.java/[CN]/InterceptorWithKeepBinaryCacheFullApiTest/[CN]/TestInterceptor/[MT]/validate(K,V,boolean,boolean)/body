{
  assertNotNull(key);
  if (validate) {
    if (validateKey)     assertTrue("Key: " + key,key instanceof BinaryObject);
    if (val != null) {
      if (validateVal && interceptorBinaryObjExp)       assertTrue("Val: " + val,val instanceof BinaryObject);
    }
  }
}
