{
  if (validate) {
    validate(e.getKey(),e.getValue(),true,true);
    if (newVal != null)     assertEquals("NewVal: " + newVal,interceptorBinaryObjExp,newVal instanceof BinaryObject);
  }
  return newVal;
}
