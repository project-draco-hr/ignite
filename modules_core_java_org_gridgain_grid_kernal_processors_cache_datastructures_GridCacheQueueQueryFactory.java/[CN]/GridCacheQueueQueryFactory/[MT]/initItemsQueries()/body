{
  itemsQry=qry.createQuery(SQL,GridCacheQueueItemImpl.class.getName(),"qid=? " + "order by seq asc");
  itemsAtPosQry=qry.createQuery(SQL,GridCacheQueueItemImpl.class.getName(),"select * from " + "(select *, rownum as r from " + "(select * from GridCacheQueueItemImpl where qid=? "+ "order by seq asc" + ')' + ") where r-1 in (select * from table(x int=?))");
  posOfItemQry=qry.createReduceQuery(SQL,GridCacheQueueItemImpl.class.getName(),"qid=? " + "order by seq asc");
}
