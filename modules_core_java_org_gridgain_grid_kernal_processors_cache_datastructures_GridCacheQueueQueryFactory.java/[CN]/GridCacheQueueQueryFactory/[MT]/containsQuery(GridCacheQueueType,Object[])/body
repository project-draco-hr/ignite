{
  GridCacheReduceQuery<GridCacheQueueItemKey,GridCacheQueueItemImpl<T>,boolean[],Boolean> containsQry=containsQrys.get(type);
  return containsQry.remoteReducer(new ContainsQueryRemoteReducer<T>(items)).localReducer(new ContainsQueryLocalReducer(items.length));
}
