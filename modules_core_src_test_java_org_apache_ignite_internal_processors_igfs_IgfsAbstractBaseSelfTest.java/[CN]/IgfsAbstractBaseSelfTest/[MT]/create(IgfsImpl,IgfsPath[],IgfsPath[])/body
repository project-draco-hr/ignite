{
  if (dirs != null) {
    for (    IgfsPath dir : dirs)     igfs.mkdirs(dir);
  }
  if (files != null) {
    for (    IgfsPath file : files) {
      try (OutputStream ignored=igfs.create(file,true)){
      }
       igfs.await(file);
    }
  }
}
