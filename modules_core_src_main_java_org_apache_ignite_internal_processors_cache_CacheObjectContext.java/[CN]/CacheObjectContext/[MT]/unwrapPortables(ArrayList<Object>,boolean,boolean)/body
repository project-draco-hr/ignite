{
  int size=col.size();
  for (int i=0; i < size; i++) {
    Object o=col.get(i);
    Object unwrapped=unwrapPortable(o,keepPortable,cpy);
    if (o != unwrapped)     col.set(i,unwrapped);
  }
  return col;
}
