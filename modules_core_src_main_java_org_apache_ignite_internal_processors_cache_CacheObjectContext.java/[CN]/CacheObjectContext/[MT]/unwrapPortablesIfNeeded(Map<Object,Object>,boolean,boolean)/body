{
  if (keepPortable)   return map;
  Map<Object,Object> map0=PortableUtils.newMap(map);
  for (  Map.Entry<Object,Object> e : map.entrySet())   map0.put(unwrapPortable(e.getKey(),keepPortable,cpy),unwrapPortable(e.getValue(),keepPortable,cpy));
  return map0;
}
