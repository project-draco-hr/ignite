{
  StringBuilder b=new StringBuilder().append("GridClientLogRequest [path=").append(path);
  if (from != -1)   b.append(", from=").append(from);
  if (to != -1)   b.append(", to=").append(to);
  b.append(']');
  return b.toString();
}
