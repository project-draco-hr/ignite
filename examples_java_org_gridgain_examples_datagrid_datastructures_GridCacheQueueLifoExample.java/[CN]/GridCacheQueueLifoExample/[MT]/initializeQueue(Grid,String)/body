{
  GridCacheQueue<String> queue=g.cache(CACHE_NAME).dataStructures().queue(queueName,LIFO,0,false,true);
  for (int i=0; i < g.nodes().size() * RETRIES * 2; i++)   queue.put(Integer.toString(i));
  print("Queue size after initializing: " + queue.size());
  return queue;
}
