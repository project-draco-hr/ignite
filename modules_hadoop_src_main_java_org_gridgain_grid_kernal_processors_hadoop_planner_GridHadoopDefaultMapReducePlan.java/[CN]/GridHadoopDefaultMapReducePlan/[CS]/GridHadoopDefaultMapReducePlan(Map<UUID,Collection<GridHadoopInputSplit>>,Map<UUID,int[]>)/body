{
  this.mappers=mappers;
  this.reducers=reducers;
  if (mappers != null) {
    for (    Collection<GridHadoopInputSplit> splits : mappers.values())     mappersCnt+=splits.size();
  }
  if (reducers != null) {
    for (    int[] rdcrs : reducers.values())     reducersCnt+=rdcrs.length;
  }
}
