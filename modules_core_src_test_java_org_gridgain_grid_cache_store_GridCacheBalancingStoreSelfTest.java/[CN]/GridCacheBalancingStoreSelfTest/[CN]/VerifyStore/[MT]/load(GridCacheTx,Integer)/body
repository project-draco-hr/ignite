{
  boolean res=locks[key].tryLock();
  if (res) {
    try {
      return key;
    }
  finally {
      locks[key].unlock();
    }
  }
 else {
    fail("Failed to acquire lock for key: " + key);
  }
  return null;
}
