{
  commState.setBuffer(buf);
switch (commState.idx) {
case 0:
    IgniteUuid taskId0=commState.getGridUuid(null);
  if (taskId0 == GRID_UUID_NOT_READ)   return false;
taskId=taskId0;
commState.idx++;
case 1:
byte[] topicBytes0=commState.getByteArray(null);
if (topicBytes0 == BYTE_ARR_NOT_READ) return false;
topicBytes=topicBytes0;
commState.idx++;
}
return true;
}
