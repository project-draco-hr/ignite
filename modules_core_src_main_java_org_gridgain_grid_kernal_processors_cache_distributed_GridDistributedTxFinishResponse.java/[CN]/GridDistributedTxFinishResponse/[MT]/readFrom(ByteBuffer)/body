{
  commState.setBuffer(buf);
  if (!super.readFrom(buf))   return false;
switch (commState.idx) {
case 3:
    IgniteUuid futId0=commState.getGridUuid(null);
  if (futId0 == GRID_UUID_NOT_READ)   return false;
futId=futId0;
commState.idx++;
case 4:
GridCacheVersion txId0=commState.getCacheVersion(null);
if (txId0 == CACHE_VER_NOT_READ) return false;
txId=txId0;
commState.idx++;
}
return true;
}
