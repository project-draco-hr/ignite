{
  try {
    return ctx.affinity().localNode(key,topVer) ? dht().peekEx(key) : ctx.isNear() ? near().peekEx(key) : null;
  }
 catch (  GridDhtInvalidPartitionException ignore) {
    return ctx.isNear() ? near().peekEx(key) : null;
  }
}
