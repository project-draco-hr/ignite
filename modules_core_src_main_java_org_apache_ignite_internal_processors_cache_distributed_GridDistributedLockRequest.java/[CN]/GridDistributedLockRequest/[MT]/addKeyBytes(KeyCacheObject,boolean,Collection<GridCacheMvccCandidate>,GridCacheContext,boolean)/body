{
  if (keys == null)   keys=new ArrayList<>(keysCount());
  keys.add(key);
  candidatesByIndex(idx,cands);
  retVals[idx]=retVal;
  flags[idx]=skipStore ? (byte)(flags[idx] | 0x1) : (byte)(flags[idx] & 0xFE);
  idx++;
}
