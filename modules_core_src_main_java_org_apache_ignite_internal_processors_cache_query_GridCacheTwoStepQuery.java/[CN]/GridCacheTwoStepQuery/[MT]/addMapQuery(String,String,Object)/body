{
  A.ensure(!F.isEmpty(alias),"alias must not be empty");
  if (mapQrys == null)   mapQrys=new GridLeanMap<>();
  if (mapQrys.put(alias,new GridCacheSqlQuery(alias,qry,params)) != null)   throw new IgniteException("Failed to add query, alias already exists: " + alias + ".");
}
