{
  if (!tx.nearNodeId().equals(cctx.localNodeId())) {
    Throwable err=this.err.get();
    if (err != null && err instanceof IgniteFutureCancelledException)     return;
    cctx.io().send(tx.nearNodeId(),res,tx.ioPolicy());
  }
}
