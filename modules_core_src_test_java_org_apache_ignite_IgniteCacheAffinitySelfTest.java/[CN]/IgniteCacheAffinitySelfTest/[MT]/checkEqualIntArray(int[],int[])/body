{
  assertEquals(arr1.length,arr2.length);
  Collection<Integer> col1=new HashSet<>();
  for (int i=0; i < arr1.length; ++i)   col1.add(arr1[i]);
  for (int i=0; i < arr2.length; ++i) {
    assertTrue(col1.contains(arr2[i]));
    col1.remove(arr2[i]);
  }
  assertEquals(0,col1.size());
}
