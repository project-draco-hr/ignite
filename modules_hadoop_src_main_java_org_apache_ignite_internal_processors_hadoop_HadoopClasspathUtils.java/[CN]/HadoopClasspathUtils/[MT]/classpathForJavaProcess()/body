{
  List<String> res=new ArrayList<>();
  for (  final SearchDirectory dir : classpathDirectories()) {
    if (dir.hasFilter()) {
      for (      File file : dir.files())       res.add(file.getAbsolutePath());
    }
 else     res.add(dir.absolutePath() + File.separator + '*');
  }
  return res;
}
