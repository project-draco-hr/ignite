{
  log.info("Populating cache data...");
  if (TEST_INDEXED)   populateCacheIndexed(grid);
 else   populateCache(grid);
  Thread.sleep(20000);
  log.info("Current cache size " + grid.cache(null).size());
}
