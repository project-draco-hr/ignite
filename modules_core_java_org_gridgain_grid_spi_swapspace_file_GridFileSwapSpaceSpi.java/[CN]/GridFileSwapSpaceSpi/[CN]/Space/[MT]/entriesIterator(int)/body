{
  Map<GridSwapKey,SwapValue> partMap=partition(part,false);
  if (partMap == null)   return Collections.<Map.Entry<GridSwapKey,byte[]>>emptySet().iterator();
  return transform(partMap.entrySet().iterator());
}
