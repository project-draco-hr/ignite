{
  ConcurrentMap<GridSwapKey,SwapValue> map=parts.get(part);
  if (map == null && create) {
    ConcurrentMap<GridSwapKey,SwapValue> old=parts.putIfAbsent(part,map=new ConcurrentHashMap<>());
    if (old != null)     map=old;
  }
  return map;
}
