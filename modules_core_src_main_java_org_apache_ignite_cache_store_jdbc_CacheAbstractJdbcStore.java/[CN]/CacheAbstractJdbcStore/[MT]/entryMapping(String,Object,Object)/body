{
  EntryMapping em=cacheMappings(cacheName).get(keyTypeId);
  if (em == null)   throw new CacheException("Failed to find mapping description [key=" + key + ", cache="+ U.maskName(cacheName)+ "]");
  return em;
}
