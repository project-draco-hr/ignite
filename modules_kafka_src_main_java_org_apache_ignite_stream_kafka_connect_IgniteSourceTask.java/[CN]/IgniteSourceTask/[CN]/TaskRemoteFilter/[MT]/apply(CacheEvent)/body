{
  Affinity<Object> affinity=ignite.affinity(cacheName);
  if (affinity.isPrimary(ignite.cluster().localNode(),evt.key())) {
    if (filter != null && filter.apply(evt))     return false;
    return true;
  }
  return false;
}
