{
  GridNode locNode=g.localNode();
  long before=freeHeap(locNode);
  System.gc();
  if (dgc)   for (  GridCache<?,?> cache : g.cachesx())   cache.dgc();
  return new GridBiTuple<>(before,freeHeap(locNode));
}
