{
  startStopwatch();
  registerMBean(gridName,this,GridAttributesTopologySpiMBean.class);
  if (log.isDebugEnabled())   log.debug(configInfo("attrs",attrs));
  if (log.isDebugEnabled())   log.debug(startInfo());
}
