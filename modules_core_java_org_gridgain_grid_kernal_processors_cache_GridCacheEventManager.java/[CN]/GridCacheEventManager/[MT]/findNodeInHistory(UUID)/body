{
  for (long topVer=cctx.discovery().topologyVersion() - 1; topVer > 0; topVer--) {
    Collection<GridNode> top=cctx.discovery().topology(topVer);
    if (top == null)     break;
    for (    GridNode node : top)     if (F.eq(node.id(),nodeId))     return node;
  }
  return null;
}
