{
  Collection<Set<KeyCacheObject>> sets=new ArrayList<>();
  for (  GridDhtLocalPartition partition : ctx.topology().localPartitions())   sets.add(partition.keySet(filter));
  return new PartitionedReadOnlySet<>(sets);
}
