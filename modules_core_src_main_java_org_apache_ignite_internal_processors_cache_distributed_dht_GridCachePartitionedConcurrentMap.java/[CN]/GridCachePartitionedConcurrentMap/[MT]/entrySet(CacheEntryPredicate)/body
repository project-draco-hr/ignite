{
  Collection<Set<GridCacheMapEntry>> sets=new ArrayList<>();
  for (  GridDhtLocalPartition partition : ctx.topology().localPartitions())   sets.add(partition.entrySet(filter));
  return new PartitionedReadOnlySet<>(sets);
}
