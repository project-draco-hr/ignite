{
  Ignite ignite=startGrid();
  try {
    IgniteCache<Integer,Integer> cache=ignite.jcache(CACHE_NAME);
    for (int i=0; i < N_ENTRIES; i++)     load(cache,i,true);
    assertEquals(MAX_CACHE_ENTRIES,cache.size());
  }
  finally {
    stopGrid();
  }
}
