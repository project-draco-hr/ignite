{
  Ignite ignite=startGrid();
  try {
    Cache<Integer,Integer> cache=ignite.cache(CACHE_NAME);
    for (int i=0; i < N_ENTRIES; i++)     cache.reload(i);
    assertEquals(MAX_CACHE_ENTRIES,cache.size());
  }
  finally {
    stopGrid();
  }
}
