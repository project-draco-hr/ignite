{
  ThreadLocal threadLoc=threadLocMap.get(cacheName);
  if (threadLoc == null) {
    ThreadLocal old=threadLocMap.putIfAbsent(cacheName,threadLoc=new ThreadLocal());
    if (old != null)     threadLoc=old;
  }
  return threadLoc;
}
