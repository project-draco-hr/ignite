{
  for (int i=0; i < 3; i++) {
    GridEx grid=grid(i);
    GridNode node=grid.localNode();
    GridCache<Object,Object> cache=grid.cache(null);
    if (cache.affinity().isPrimary(node,key) || cache.affinity().isBackup(node,key))     assertEquals(val,cache.peek(key));
  }
}
