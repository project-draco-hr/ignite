{
  if (!enterBusy())   return;
  try {
    if (cctx.preloadEnabled()) {
      if (log.isDebugEnabled())       log.debug("Received demand message [node=" + nodeId + ", msg="+ d+ ']');
      queue.offer(new DemandMessage<>(nodeId,d));
    }
 else     U.warn(log,"Received demand message when preloading is disabled (will ignore): " + d);
  }
  finally {
    leaveBusy();
  }
}
