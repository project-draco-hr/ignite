{
  assert debugMode;
  if (msg instanceof GridTcpDiscoveryHeartbeatMessage)   return;
  rcvMsgs.add(MessageLogEntry.received(otherParty,rdrId,msg));
  int delta=debugMsgHist - rcvMsgs.size();
  for (int i=0; i < delta && rcvMsgs.size() > debugMsgHist; i++)   rcvMsgs.poll();
}
