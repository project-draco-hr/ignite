{
  if (ctxInitLatch.getCount() > 0)   ctxInitLatch.countDown();
  getSpiContext().deregisterPorts();
  ipFinder.onSpiContextDestroyed();
  if (metricsStore != null)   metricsStore.onSpiContextDestroyed();
}
