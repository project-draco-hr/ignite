{
  assert debugMode;
  if (!recordable(msg))   return;
  sentMsgs.add(MessageLogEntry.sent(otherParty,msg));
  int delta=sentMsgs.size() - debugMsgHist;
  for (int i=0; i < delta && sentMsgs.size() > debugMsgHist; i++)   sentMsgs.poll();
}
