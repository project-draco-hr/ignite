{
  try {
    writeToSocket(sock,msg);
  }
 catch (  GridException|IOException e) {
    if (log.isDebugEnabled())     U.error(log,"Caught exception on redirecting message to client [sock=" + sock + ", locNodeId="+ locNodeId+ ", rmtNodeId="+ nodeId+ ']',e);
  }
}
