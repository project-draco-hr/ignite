{
  if (log.isDebugEnabled())   log.debug("Message worker started [locNodeId=" + locNodeId + ']');
  while (!isInterrupted()) {
    GridTcpDiscoveryAbstractMessage msg=queue.poll(2000,TimeUnit.MILLISECONDS);
    if (msg == null)     continue;
    processMessage(msg);
  }
}
