{
  if (init)   return;
  init=true;
  try {
    if (rs != null && rs.next())     next=loadRow();
  }
 catch (  Exception e) {
    if (e instanceof SQLException)     onSqlException((SQLException)e);
    throw new GridException("Failed to load row.",e);
  }
}
