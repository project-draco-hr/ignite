{
  Map<String,Class<? extends GridComputeTask<?,?>>> locTasks=grid.compute().localTasks();
  if (log().isInfoEnabled())   log().info("Local tasks found: " + locTasks);
  return locTasks.get(taskName) != null;
}
