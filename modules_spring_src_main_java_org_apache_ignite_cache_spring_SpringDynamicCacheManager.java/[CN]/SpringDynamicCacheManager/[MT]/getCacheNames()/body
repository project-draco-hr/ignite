{
  Collection<String> names=F.view(super.getCacheNames(),new IgnitePredicate<String>(){
    @Override public boolean apply(    String name){
      return !F.eq(name,dataCacheName);
    }
  }
);
  return F.concat(false,names,F.transform(metaCache.entrySetx(),new IgniteClosure<Cache.Entry<MetaKey,org.springframework.cache.Cache>,String>(){
    @Override public String apply(    Cache.Entry<MetaKey,org.springframework.cache.Cache> e){
      return e.getKey().name;
    }
  }
));
}
