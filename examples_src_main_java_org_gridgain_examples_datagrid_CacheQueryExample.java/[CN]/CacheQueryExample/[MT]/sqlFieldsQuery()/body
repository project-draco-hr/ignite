{
  GridCache<?,?> cache=Ignition.grid().cache(CACHE_NAME);
  GridCacheQuery<List<?>> qry1=cache.queries().createSqlFieldsQuery("select concat(firstName, ' ', lastName) from Person");
  Collection<List<?>> res=qry1.execute().get();
  print("Names of all employees:",res);
}
