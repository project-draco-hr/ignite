{
  if (create) {
    GridCacheMapEntry<K,V> entry=new GridDhtCacheEntry<>(ctx,topVer,key,hash(key.hashCode()),val,null,0,0);
    return new GridTriple<>(entry,null,null);
  }
 else   return emptyTriple;
}
