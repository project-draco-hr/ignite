{
  if (create) {
    GridCacheMapEntry entry=ctx.useOffheapEntry() ? new GridDhtOffHeapCacheEntry(ctx,topVer,key,hash(key.hashCode()),val) : new GridDhtCacheEntry(ctx,topVer,key,hash(key.hashCode()),val);
    return new GridTriple<>(entry,null,null);
  }
 else   return emptyTriple;
}
