{
  CacheEvictionPolicy evictPlc=cfg.getEvictionPolicy();
  if (evictPlc instanceof CacheGgfsPerBlockLruEvictionPolicy && cfg.getEvictionFilter() == null)   cfg.setEvictionFilter(new CacheGgfsEvictionFilter());
}
