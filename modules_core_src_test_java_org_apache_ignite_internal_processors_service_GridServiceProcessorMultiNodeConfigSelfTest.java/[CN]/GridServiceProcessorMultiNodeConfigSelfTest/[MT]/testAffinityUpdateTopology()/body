{
  Ignite g=randomGrid();
  checkCount(AFFINITY,g.managed().deployedServices(),1);
  int nodeCnt=2;
  startExtraNodes(nodeCnt);
  try {
    checkCount(AFFINITY,g.managed().deployedServices(),1);
  }
  finally {
    stopExtraNodes(nodeCnt);
  }
}
