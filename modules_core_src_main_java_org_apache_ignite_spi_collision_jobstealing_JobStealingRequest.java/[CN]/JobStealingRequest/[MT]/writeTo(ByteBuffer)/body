{
  commState.setBuffer(buf);
  if (!commState.typeWritten) {
    if (!commState.writeType(directType()))     return false;
    commState.typeWritten=true;
  }
switch (commState.idx) {
case 0:
    if (!commState.putInt(null,delta))     return false;
  commState.idx++;
}
return true;
}
