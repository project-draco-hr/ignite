{
  if (args.length < 1)   throw new IllegalArgumentException("Path separator must be passed as the first argument.");
  String separator=args[0];
  StringBuilder sb=new StringBuilder();
  for (  String path : HadoopClasspathUtils.classpathForJavaProcess())   sb.append(path).append(separator);
  System.out.println(sb);
}
