{
  commState.setBuffer(buf);
  if (!commState.typeWritten) {
    if (!commState.putByte(null,directType()))     return false;
    commState.typeWritten=true;
  }
switch (commState.idx) {
case 0:
    if (!commState.putByteArray("dataBytes",dataBytes))     return false;
  commState.idx++;
case 1:
if (!commState.putUuid("routineId",routineId)) return false;
commState.idx++;
case 2:
if (!commState.putEnum("type",type)) return false;
commState.idx++;
}
return true;
}
