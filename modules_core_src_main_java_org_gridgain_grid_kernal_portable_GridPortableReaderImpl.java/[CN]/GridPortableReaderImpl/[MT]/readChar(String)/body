{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != CHAR)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readChar(fieldOff) : 0;
}
