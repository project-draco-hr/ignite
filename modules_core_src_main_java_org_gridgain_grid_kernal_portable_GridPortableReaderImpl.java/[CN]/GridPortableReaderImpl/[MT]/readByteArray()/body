{
  int len=readInt();
  if (len >= 0) {
    byte[] arr=new byte[len];
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,BYTE_ARR_OFF,len);
    rawOff+=len;
    return arr;
  }
 else   return null;
}
