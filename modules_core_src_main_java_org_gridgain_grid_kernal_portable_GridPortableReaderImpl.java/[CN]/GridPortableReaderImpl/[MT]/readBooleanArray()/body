{
  int len=readInt();
  if (len >= 0) {
    boolean[] arr=new boolean[len];
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,BOOLEAN_ARR_OFF,len);
    rawOff+=len;
    return arr;
  }
 else   return null;
}
