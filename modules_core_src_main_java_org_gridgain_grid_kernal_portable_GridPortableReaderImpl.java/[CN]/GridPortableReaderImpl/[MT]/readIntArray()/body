{
  int len=readInt();
  if (len >= 0) {
    int[] arr=new int[len];
    int bytes=len << 2;
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,INT_ARR_OFF,bytes);
    rawOff+=bytes;
    return arr;
  }
 else   return null;
}
