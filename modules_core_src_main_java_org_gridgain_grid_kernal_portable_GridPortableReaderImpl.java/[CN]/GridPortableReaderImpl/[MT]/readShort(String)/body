{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != SHORT)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readShort(fieldOff) : 0;
}
