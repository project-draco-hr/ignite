{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != UUID)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readUuid(fieldOff) : null;
}
