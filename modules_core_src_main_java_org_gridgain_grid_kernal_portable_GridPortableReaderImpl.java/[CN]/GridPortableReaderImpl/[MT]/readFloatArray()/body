{
  int len=readInt();
  if (len >= 0) {
    float[] arr=new float[len];
    int bytes=len << 2;
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,FLOAT_ARR_OFF,bytes);
    rawOff+=bytes;
    return arr;
  }
 else   return null;
}
