{
  int len=readInt();
  if (len >= 0) {
    long[] arr=new long[len];
    int bytes=len << 3;
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,LONG_ARR_OFF,bytes);
    rawOff+=bytes;
    return arr;
  }
 else   return null;
}
