{
  int len=readInt();
  if (len >= 0) {
    char[] arr=new char[len];
    int bytes=len << 1;
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,CHAR_ARR_OFF,bytes);
    rawOff+=bytes;
    return arr;
  }
 else   return null;
}
