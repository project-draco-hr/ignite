{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != STRING)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readString(fieldOff) : null;
}
