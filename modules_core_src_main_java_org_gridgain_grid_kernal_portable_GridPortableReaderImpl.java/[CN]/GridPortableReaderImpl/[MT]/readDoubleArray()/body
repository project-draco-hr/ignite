{
  int len=readInt();
  if (len >= 0) {
    double[] arr=new double[len];
    int bytes=len << 3;
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,DOUBLE_ARR_OFF,bytes);
    rawOff+=bytes;
    return arr;
  }
 else   return null;
}
