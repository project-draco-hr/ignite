{
  int len=readInt(off);
  if (len >= 0) {
    short[] arr=new short[len];
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + off + 4,arr,SHORT_ARR_OFF,len << 1);
    return arr;
  }
 else   return null;
}
