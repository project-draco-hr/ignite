{
  int len=readInt();
  if (len >= 0) {
    short[] arr=new short[len];
    int bytes=len << 1;
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + rawOff,arr,SHORT_ARR_OFF,bytes);
    rawOff+=bytes;
    return arr;
  }
 else   return null;
}
