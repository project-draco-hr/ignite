{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != DOUBLE_ARR)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readDoubleArray(fieldOff) : null;
}
