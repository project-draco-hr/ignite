{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != SHORT_ARR)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readShortArray(fieldOff) : null;
}
