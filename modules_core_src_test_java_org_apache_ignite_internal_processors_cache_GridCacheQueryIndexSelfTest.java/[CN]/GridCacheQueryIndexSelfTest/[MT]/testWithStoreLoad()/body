{
  for (int i=0; i < ENTRY_CNT; i++)   putToStore(i,new CacheValue(i));
  GridCache<Integer,CacheValue> cache0=grid(0).cache(null);
  cache0.loadCache(null,0);
  checkCache(cache0);
  checkQuery(cache0,true);
}
