{
  A.notNull(name,"name");
  A.notNull(svc,"svc");
  A.notNull(affKey,"affKey");
  guard();
  try {
    return ctx.service().deployForAffinityKey(name,svc,cacheName,affKey);
  }
  finally {
    unguard();
  }
}
