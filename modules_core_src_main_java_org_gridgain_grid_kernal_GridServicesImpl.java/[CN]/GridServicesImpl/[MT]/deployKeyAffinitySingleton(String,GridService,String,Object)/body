{
  A.notNull(name,"name");
  A.notNull(svc,"svc");
  A.notNull(affKey,"affKey");
  guard();
  try {
    result(ctx.service().deployKeyAffinitySingleton(name,svc,cacheName,affKey));
  }
  finally {
    unguard();
  }
}
