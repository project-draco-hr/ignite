{
  try {
    adapter=new GridHadoopIpcToNioAdapter<>(GridHadoopExternalCommunication.this.log,endpoint,srvLsnr,filters);
    adapter.serve();
  }
  finally {
    shmemWorkers.remove(this);
    endpoint.close();
  }
}
