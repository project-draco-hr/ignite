{
  try (IgniteDataLoader<Integer,Long> ldr=ignite.dataLoader(CACHE_NAME)){
    ldr.perNodeBufferSize(2048);
    ldr.updater(new IncrementingUpdater());
    for (int i=0; i < CNT; i++)     ldr.addData(RAND.nextInt(RANGE),1L);
  }
 }
