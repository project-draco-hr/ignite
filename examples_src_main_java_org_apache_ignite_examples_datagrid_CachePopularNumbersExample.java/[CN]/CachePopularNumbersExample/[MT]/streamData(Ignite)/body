{
  try (IgniteDataStreamer<Integer,Long> stmr=ignite.dataStreamer(CACHE_NAME)){
    stmr.perNodeBufferSize(2048);
    stmr.receiver(new IncrementingUpdater());
    for (int i=0; i < CNT; i++)     stmr.addData(RAND.nextInt(RANGE),1L);
  }
 }
