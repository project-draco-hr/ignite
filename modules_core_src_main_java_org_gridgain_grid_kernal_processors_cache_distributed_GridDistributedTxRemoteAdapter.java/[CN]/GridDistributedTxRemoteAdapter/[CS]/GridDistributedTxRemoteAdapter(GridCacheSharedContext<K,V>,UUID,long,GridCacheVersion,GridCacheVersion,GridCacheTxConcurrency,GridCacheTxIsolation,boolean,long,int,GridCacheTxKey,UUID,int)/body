{
  super(ctx,nodeId,xidVer,ctx.versions().last(),Thread.currentThread().getId(),concurrency,isolation,timeout,txSize,grpLockKey,subjId,taskNameHash);
  this.rmtThreadId=rmtThreadId;
  this.invalidate=invalidate;
  commitVersion(commitVer);
  started=true;
}
