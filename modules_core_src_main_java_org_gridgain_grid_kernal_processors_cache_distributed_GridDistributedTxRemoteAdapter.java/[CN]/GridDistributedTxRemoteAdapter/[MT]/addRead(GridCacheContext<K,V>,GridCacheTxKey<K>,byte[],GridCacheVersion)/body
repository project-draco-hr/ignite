{
  checkInternal(key);
  GridCacheTxEntry<K,V> txEntry=new GridCacheTxEntry<>(cacheCtx,this,READ,null,0L,-1L,cacheCtx.cache().entryEx(key.key()),drVer);
  txEntry.keyBytes(keyBytes);
  readMap.put(key,txEntry);
}
