{
  if (segChkWrk != null) {
    segChkWrk.cancel();
    U.join(segChkThread,log);
  }
  if (!locJoinEvt.isDone())   locJoinEvt.onDone(new GridException("Failed to wait for local node joined event (grid is stopping)."));
}
