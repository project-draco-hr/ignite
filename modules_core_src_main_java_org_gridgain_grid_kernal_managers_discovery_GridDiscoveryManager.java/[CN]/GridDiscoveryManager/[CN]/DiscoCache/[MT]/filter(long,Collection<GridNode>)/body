{
  if (nodes == null)   return Collections.emptyList();
  return nodes.isEmpty() || topVer < 0 || topVer >= maxOrder ? nodes : F.view(nodes,new P1<GridNode>(){
    @Override public boolean apply(    GridNode node){
      return node.order() <= topVer;
    }
  }
);
}
