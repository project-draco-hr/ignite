{
  commState.setBuffer(buf);
  if (!super.writeTo(buf))   return false;
  if (!commState.typeWritten) {
    if (!commState.putByte(null,directType()))     return false;
    commState.typeWritten=true;
  }
switch (commState.idx) {
case 3:
    if (!commState.putByteArray("entriesBytes",entriesBytes))     return false;
  commState.idx++;
case 4:
if (!commState.putLong("futId",futId)) return false;
commState.idx++;
case 5:
if (!commState.putLong("topVer",topVer)) return false;
commState.idx++;
}
return true;
}
