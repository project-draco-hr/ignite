{
  bw.write("\n");
  for (  Op op : ops) {
    boolean written=false;
    for (    Long ptr : ptrs) {
      if (F.eq(op.ptr,ptr)) {
        bw.write(op.toString() + "\n");
        break;
      }
    }
    if (!written) {
      if (op.offset != 34 || op.key.length != 6)       bw.write("SUSPICIOUS: " + op.toString() + "\n");
    }
  }
}
