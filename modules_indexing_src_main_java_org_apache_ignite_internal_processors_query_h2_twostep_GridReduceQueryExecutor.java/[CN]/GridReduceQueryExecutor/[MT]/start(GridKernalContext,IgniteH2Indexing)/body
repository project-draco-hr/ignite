{
  this.ctx=ctx;
  this.h2=h2;
  log=ctx.log(GridReduceQueryExecutor.class);
  ctx.io().addMessageListener(GridTopic.TOPIC_QUERY,this);
  h2.executeStatement("PUBLIC","CREATE ALIAS " + GridSqlQuerySplitter.TABLE_FUNC_NAME + " FOR \""+ GridReduceQueryExecutor.class.getName()+ ".mergeTableFunction\"");
}
