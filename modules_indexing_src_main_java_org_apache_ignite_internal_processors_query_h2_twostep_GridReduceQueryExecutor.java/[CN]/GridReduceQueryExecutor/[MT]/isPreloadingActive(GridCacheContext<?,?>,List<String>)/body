{
  if (hasMovingPartitions(cctx))   return true;
  if (extraSpaces != null) {
    for (    String extraSpace : extraSpaces) {
      if (hasMovingPartitions(cacheContext(extraSpace)))       return true;
    }
  }
  return false;
}
