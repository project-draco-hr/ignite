{
  final long qryReqId=msg.queryRequestId();
  final int qry=msg.query();
  final int pageSize=msg.rows().size();
  QueryRun r=runs.get(qryReqId);
  if (r == null)   return;
  GridMergeIndex idx=r.tbls.get(msg.query()).getScanIndex(null);
  idx.addPage(new GridResultPage(node.id(),msg){
    @Override public void fetchNextPage(){
      try {
        ctx.io().sendUserMessage(F.asList(node),new GridQueryNextPageRequest(qryReqId,qry,pageSize),GridTopic.TOPIC_QUERY,false,0);
      }
 catch (      IgniteCheckedException e) {
        throw new IgniteException(e);
      }
    }
  }
);
  if (msg.allRows() != -1)   r.latch.countDown();
}
