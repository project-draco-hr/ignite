{
  backups=1;
  int gridCnt=2;
  int priThreads=5;
  int nearThreads=5;
  startGrids(gridCnt);
  try {
    checkNearAndPrimary(gridCnt,priThreads,nearThreads);
  }
  finally {
    stopAllGrids();
  }
}
