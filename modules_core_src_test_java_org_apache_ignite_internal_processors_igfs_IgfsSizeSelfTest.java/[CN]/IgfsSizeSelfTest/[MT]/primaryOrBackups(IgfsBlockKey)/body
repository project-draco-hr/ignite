{
  IgniteEx grid=grid(0);
  Collection<UUID> ids=new HashSet<>();
  for (  ClusterNode node : grid.cluster().nodes()) {
    if (grid.cachex(DATA_CACHE_NAME).affinity().isPrimaryOrBackup(node,key))     ids.add(node.id());
  }
  return ids;
}
