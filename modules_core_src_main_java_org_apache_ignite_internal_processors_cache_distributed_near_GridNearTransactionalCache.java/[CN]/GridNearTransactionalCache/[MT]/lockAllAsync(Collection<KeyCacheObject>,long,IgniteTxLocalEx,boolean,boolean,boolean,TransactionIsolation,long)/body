{
  GridNearLockFuture<K,V> fut=new GridNearLockFuture<>(ctx,keys,(GridNearTxLocal)tx,isRead,retval,timeout,accessTtl,CU.empty0());
  if (!ctx.mvcc().addFuture(fut))   throw new IllegalStateException("Duplicate future ID: " + fut);
  fut.map();
  return fut;
}
