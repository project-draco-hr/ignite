{
  GridNearLockFuture<K,V> fut=new GridNearLockFuture<>(ctx,keys,(GridNearTxLocal<K,V>)tx,isRead,retval,timeout,accessTtl,filter);
  if (!ctx.mvcc().addFuture(fut))   throw new IllegalStateException("Duplicate future ID: " + fut);
  fut.map();
  return fut;
}
