{
  GridNearLockFuture<K,V> fut=new GridNearLockFuture<>(ctx,null,(GridNearTxLocal)tx,isRead,retval,timeout,accessTtl,filter);
  if (!ctx.mvcc().addFuture(fut))   throw new IllegalStateException("Duplicate future ID: " + fut);
  fut.map();
  return fut;
}
