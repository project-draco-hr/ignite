{
  return withReconnectHandling(new FileSystemClosure<Collection<GridGgfsPath>>(){
    @Override public Collection<GridGgfsPath> apply(    NewGridGgfsHadoopEx delegate) throws GridException, IOException {
      return delegate.listPaths(path);
    }
  }
);
}
