{
  return withReconnectHandling(new FileSystemClosure<Collection<GridGgfsFile>>(){
    @Override public Collection<GridGgfsFile> apply(    NewGridGgfsHadoopEx delegate) throws GridException, IOException {
      return delegate.listFiles(path);
    }
  }
);
}
