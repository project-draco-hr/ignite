{
  return withReconnectHandling(new FileSystemClosure<GridGgfsFile>(){
    @Override public GridGgfsFile apply(    NewGridGgfsHadoopEx hadoop,    GridGgfsHandshakeResponse hndResp) throws GridException, IOException {
      return hadoop.update(path,props);
    }
  }
,path);
}
