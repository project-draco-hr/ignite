{
  return withReconnectHandling(new FileSystemClosure<Boolean>(){
    @Override public Boolean apply(    NewGridGgfsHadoopEx delegate) throws GridException, IOException {
      return delegate.rename(src,dest);
    }
  }
);
}
