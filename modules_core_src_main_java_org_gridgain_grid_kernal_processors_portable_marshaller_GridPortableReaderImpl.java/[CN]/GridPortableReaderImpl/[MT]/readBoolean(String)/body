{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != BOOLEAN)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 && readBoolean(fieldOff);
}
