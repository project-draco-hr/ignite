{
  int len=readInt(off);
  if (len >= 0) {
    boolean[] arr=new boolean[len];
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + off + 4,arr,BOOLEAN_ARR_OFF,len);
    return arr;
  }
 else   return null;
}
