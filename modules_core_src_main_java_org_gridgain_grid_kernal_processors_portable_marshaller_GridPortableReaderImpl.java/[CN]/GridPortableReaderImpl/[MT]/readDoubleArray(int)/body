{
  int len=readInt(off);
  if (len >= 0) {
    double[] arr=new double[len];
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + off + 4,arr,DOUBLE_ARR_OFF,len << 3);
    return arr;
  }
 else   return null;
}
