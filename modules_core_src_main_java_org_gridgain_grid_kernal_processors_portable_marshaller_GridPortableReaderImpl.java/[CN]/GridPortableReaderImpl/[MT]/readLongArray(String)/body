{
  int fieldOff=fieldOffset(fieldName);
  byte flag=readByte(fieldOff++);
  if (flag != LONG_ARR)   throw new GridPortableException("Invalid flag value: " + flag);
  return fieldOff >= 0 ? readLongArray(fieldOff) : null;
}
