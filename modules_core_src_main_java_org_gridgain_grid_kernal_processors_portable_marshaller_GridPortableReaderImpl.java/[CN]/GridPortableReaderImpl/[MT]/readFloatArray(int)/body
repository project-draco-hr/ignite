{
  int len=readInt(off);
  if (len >= 0) {
    float[] arr=new float[len];
    UNSAFE.copyMemory(this.arr,BYTE_ARR_OFF + off + 4,arr,FLOAT_ARR_OFF,len << 2);
    return arr;
  }
 else   return null;
}
