{
  put("p1",concat(F.transform(nodes,new C1<ClusterNode,UUID>(){
    /** 
 * {@inheritDoc} 
 */
    @Override public UUID apply(    ClusterNode node){
      return node.id();
    }
  }
).toArray(),";"));
  return this;
}
