{
  cctx.denyOnFlag(LOCAL);
  V val=innerGet0(tx,readSwap,readThrough,evt,failFast,unmarshal,updateMetrics,filter);
  if (key.getClass().getName().contains("GridGgfsBlock") && val == null)   U.dumpStack("INNER GET: " + key + ", val="+ Arrays.toString((byte[])val));
  return val;
}
