{
  return (val instanceof byte[]) ? GridCacheValueBytes.plain(val) : GridCacheValueBytes.marshaled((cctx.offheapTiered() && cctx.portableEnabled()) ? cctx.portable().marshal(val,true).array() : CU.marshal(cctx,val));
}
