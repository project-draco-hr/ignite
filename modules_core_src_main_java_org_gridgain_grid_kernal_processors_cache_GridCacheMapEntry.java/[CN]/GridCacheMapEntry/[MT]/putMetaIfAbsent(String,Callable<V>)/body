{
  A.notNull(name,"name",c,"c");
synchronized (this) {
    V v=meta(name);
    if (v == null)     try {
      return addMeta(name,c.call());
    }
 catch (    Exception e) {
      throw F.wrap(e);
    }
    return v;
  }
}
