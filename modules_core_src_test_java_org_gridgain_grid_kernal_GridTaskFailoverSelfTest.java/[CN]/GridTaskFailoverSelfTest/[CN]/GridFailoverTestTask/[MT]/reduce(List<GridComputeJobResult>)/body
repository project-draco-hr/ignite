{
  if (log.isInfoEnabled())   log.info("Reducing job [job=" + this + ", results="+ results+ ']');
  int res=0;
  for (  GridComputeJobResult result : results)   res+=(Integer)result.getData();
  return res;
}
