{
  K key=threadLocKey.get();
  if (key == null)   return;
  if (!rollback) {
    if (rmv)     evtsCnt.decrement();
 else     evtsCnt.increment();
  }
  threadLocKey.remove();
  endUpdate0(sync,evt,key,rollback);
  unlockKey(key,sync);
  readUnlock();
}
