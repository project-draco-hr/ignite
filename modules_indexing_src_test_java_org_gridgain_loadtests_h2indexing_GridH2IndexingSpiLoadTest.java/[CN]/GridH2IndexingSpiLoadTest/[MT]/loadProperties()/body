{
  Properties p=new Properties();
  File propsFile=new File("idx-test.properties");
  if (propsFile.exists() && propsFile.isFile())   p.load(new FileReader(propsFile));
 else   System.out.println("Properties file not found.");
  return p;
}
