{
  int res=0;
  for (AbstractFileEntry e=head.get(); e != null; e=e.prev) {
switch (e.type()) {
case REMOVE:
      break;
case COMPOUND:
    res+=((CompoundEntry)e).liveSize;
  break;
default :
res+=e.len;
}
}
return res;
}
