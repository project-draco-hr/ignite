{
  GridLongList res=map.get(keyHash);
  if (res == null)   return null;
  for (int i=0, len=res.size(); i < len; i++) {
    long addr=res.get(i);
    DataEntry e=file(addr).read(addr);
    if (e.keyEquals(keyBytes,keyHash))     return e;
  }
  return null;
}
