{
  int prjSize=prj.nodes().size();
  if (prjSize < size) {
    System.out.println();
    System.out.println(">>> Please start at least " + size + " grid nodes to run example.");
    System.out.println();
    return false;
  }
  return true;
}
