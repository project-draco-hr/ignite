{
  if (grid.configuration().getStreamerConfiguration() != null) {
    for (    GridStreamerConfiguration cfg : grid.configuration().getStreamerConfiguration()) {
      if (name.equals(cfg.getName()))       return true;
    }
  }
  return false;
}
