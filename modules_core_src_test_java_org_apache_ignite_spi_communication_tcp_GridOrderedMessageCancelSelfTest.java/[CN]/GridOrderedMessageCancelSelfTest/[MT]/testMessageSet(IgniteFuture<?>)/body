{
  cancelLatch.await();
  assertTrue(fut.cancel());
  resLatch.countDown();
  finishLatch.await();
  Map map=U.field(((GridKernal)grid(0)).context().io(),"msgSetMap");
  info("Map: " + map);
  assertTrue(map.isEmpty());
}
