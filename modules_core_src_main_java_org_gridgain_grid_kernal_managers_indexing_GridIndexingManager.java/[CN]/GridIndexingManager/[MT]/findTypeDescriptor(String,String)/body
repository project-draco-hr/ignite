{
  for (  Map.Entry<TypeId,TypeDescriptor> e : types.entrySet()) {
    TypeId typeId=e.getKey();
    if (!F.eq(space,typeId.space))     continue;
    if (valTypeName.equals(typeId.typeName(this)))     return e.getValue();
  }
  return null;
}
