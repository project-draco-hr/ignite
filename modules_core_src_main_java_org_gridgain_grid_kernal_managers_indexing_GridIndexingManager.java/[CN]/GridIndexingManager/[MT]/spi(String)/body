{
  if (F.isEmpty(name))   return getSpis()[0];
  for (  IndexingSpi s : getSpis()) {
    if (name.equals(s.getName()))     return s;
  }
  throw new GridRuntimeException("Failed to find SPI for name: " + name);
}
