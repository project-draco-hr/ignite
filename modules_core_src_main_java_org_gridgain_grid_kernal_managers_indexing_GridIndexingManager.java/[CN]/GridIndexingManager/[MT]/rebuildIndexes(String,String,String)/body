{
  if (!busyLock.enterBusy())   throw new IllegalStateException("Failed to rebuild indexes (grid is stopping).");
  try {
    return rebuildIndexes(spi,space,findTypeDescriptor(space,valTypeName));
  }
  finally {
    busyLock.leaveBusy();
  }
}
