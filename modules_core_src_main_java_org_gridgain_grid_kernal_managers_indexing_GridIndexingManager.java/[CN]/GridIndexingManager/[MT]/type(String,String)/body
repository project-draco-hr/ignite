{
  TypeDescriptor type=typesByName.get(new TypeName(space,typeName));
  if (type == null || !type.registered())   throw new GridException("Failed to find type descriptor for type name: " + typeName);
  return type;
}
