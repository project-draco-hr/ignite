{
  try {
    if (log.isDebugEnabled())     log.debug("Store put [key=" + key + ", val="+ val+ ']');
    updateCache(key,val,StoreOperation.PUT);
  }
 catch (  GridInterruptedException e) {
    throw new CacheWriterException(e);
  }
}
