{
  Collection<String> resultHostNames=new HashSet<String>();
  Collection<ClusterNode> allNodes=nodes();
  if (!(allNodes.isEmpty())) {
    for (    ClusterNode currentNode : allNodes)     Collections.addAll(resultHostNames,currentNode.hostNames().toArray(new String[0]));
  }
  return resultHostNames;
}
