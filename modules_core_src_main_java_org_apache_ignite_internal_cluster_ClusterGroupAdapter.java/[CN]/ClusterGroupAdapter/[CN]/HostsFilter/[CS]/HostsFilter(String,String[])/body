{
  hashInputHostNames=new HashSet();
  if (name != null)   hashInputHostNames.add(name);
  if (names != null && (names.length > 0)) {
    for (    String currentInputHostName : names)     hashInputHostNames.add(currentInputHostName);
  }
}
