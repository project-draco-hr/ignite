{
  t.get1().collect(F.retain(t.get3(),true,subgrid));
  Map<GridComputeJob,GridNode> map=new HashMap<>(t.get2().size(),1);
  for (  Runnable r : t.get2())   map.put(F.job(r),t.get1().apply(r));
  return map;
}
