{
  t.get1().collect(F.retain(t.get4(),true,subgrid));
  Map<GridComputeJob,GridNode> map=new HashMap<>(t.get2().size(),1);
  for (  C c : t.get2())   map.put(F.job(c),t.get1().apply(c));
  return map;
}
