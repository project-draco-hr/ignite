{
  enterBusy();
  try {
    if (F.isEmpty(nodes))     return new GridFinishedFuture<>(ctx,makeException());
    ctx.task().setThreadContext(TC_SUBGRID,nodes);
    return ctx.task().execute(new T9<>(job,arg),null,false);
  }
  finally {
    leaveBusy();
  }
}
