{
  GridRiskEventJobAdapter<?,?> job=new GridRiskEventJobAdapter<Object,Object>(){
    @Override public Object call() throws Exception {
      throw new UnsupportedOperationException("Not implemented.");
    }
  }
;
  for (  GridRiskEvent evt : evts)   if (dataEntry.factor1().equals(evt.factor()) || dataEntry.factor2().equals(evt.factor()))   job.updateMetrics(dataEntry,evt.value());
}
