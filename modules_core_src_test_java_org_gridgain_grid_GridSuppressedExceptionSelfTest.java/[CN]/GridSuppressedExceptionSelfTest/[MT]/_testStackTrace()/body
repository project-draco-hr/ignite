{
  GridException me=new GridException("Test message.");
  for (int i=5; i < 20; i++) {
    try {
      generateException(i,null);
    }
 catch (    GridException e) {
      me.addSuppressed(e);
    }
  }
  me.printStackTrace();
}
