{
  CacheConfiguration cfg=new CacheConfiguration();
  cfg.setCacheMode(CacheMode.PARTITIONED);
  if (F.eq(gridName,getTestGridName(0)))   cfg.setDistributionMode(CacheDistributionMode.NEAR_ONLY);
  return cfg;
}
