{
  try {
    if (!fileSys.rename(convert(src),convert(dest)))     throw new GridGgfsException("Failed to rename (secondary file system returned false) " + "[src=" + src + ", dest="+ dest+ ']');
  }
 catch (  IOException e) {
    throw new GridException(e);
  }
}
