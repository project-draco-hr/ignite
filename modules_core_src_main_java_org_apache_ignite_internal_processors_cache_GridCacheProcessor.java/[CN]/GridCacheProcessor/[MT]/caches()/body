{
  return F.viewReadOnly(jCacheProxies.values(),new IgniteClosure<IgniteCacheProxy<?,?>,InternalCache<?,?>>(){
    @Override public InternalCache<?,?> apply(    IgniteCacheProxy<?,?> entries){
      return entries.legacyProxy();
    }
  }
);
}
