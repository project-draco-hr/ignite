{
  if (!ctx.isStopping()) {
    for (    GridCacheAdapter<?,?> cache : caches.values()) {
      if (cache.context().userCache() && cache.context().deploymentEnabled())       cache.onUndeploy(ldr);
    }
  }
}
