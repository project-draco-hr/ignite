{
  if (!ctx.isStopping()) {
    for (    GridCacheAdapter<?,?> cache : caches.values()) {
      if (!cache.context().system() && !CU.isAtomicsCache(cache.context().name()))       cache.onUndeploy(ldr);
    }
  }
}
