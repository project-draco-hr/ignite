{
  Collection<IgniteCacheProxy<?,?>> res=new ArrayList<>(jCacheProxies.size());
  for (  Map.Entry<String,IgniteCacheProxy<?,?>> entry : jCacheProxies.entrySet()) {
    if (!sysCaches.contains(entry.getKey()))     res.add(entry.getValue());
  }
  return res;
}
