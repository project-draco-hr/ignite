{
  DynamicCacheDescriptor desc=registeredCaches.get(req.cacheName());
  if (desc != null && desc.deploymentId().equals(req.deploymentId())) {
    if (req.isStart() || req.isClientStart())     return !desc.cancelled();
 else     return desc.cancelled();
  }
  return false;
}
