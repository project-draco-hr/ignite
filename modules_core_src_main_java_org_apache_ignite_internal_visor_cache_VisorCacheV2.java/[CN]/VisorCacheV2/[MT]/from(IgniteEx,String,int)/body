{
  super.from(ignite,cacheName,sample);
  GridCacheAdapter ca=ignite.context().cache().internalCache(cacheName);
  if (ca != null && ca.context().started())   near=ca.context().isNear();
  return this;
}
