{
  GridCacheContext<K,V> ctx=cached.context();
  return ctx.config().getCacheMode() != PARTITIONED || ctx.nodeId().equals(ctx.affinity().primary(cached.key(),ctx.affinity().affinityTopologyVersion()).id());
}
