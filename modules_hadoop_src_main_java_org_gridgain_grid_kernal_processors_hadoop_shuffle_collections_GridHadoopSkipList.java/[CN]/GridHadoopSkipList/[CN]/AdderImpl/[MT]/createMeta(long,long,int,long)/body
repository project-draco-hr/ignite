{
  int size=24 + 8 * level;
  long meta=allocate(size);
  key(meta,key);
  value(meta,val);
  nextMeta(meta,nextMeta);
  for (int i=24; i < size; i+=8)   mem.writeLong(meta + i,0L);
  return meta;
}
