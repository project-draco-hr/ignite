{
  if (dirs != null) {
    for (    IgfsPath dir : dirs)     ggfs.mkdirs(dir);
  }
  if (files != null) {
    for (    IgfsPath file : files) {
      OutputStream os=ggfs.create(file,true);
      os.close();
    }
  }
}
