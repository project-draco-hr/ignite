{
  doTest(new Callable<Object>(){
    @Override public Object call() throws IgniteCheckedException {
      return cache().queries().createFullTextQuery(String.class,"text").execute().get();
    }
  }
);
}
