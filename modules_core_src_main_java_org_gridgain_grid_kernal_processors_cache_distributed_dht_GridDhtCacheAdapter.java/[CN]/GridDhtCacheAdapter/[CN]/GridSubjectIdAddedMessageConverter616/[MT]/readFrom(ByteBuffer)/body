{
  commState.setBuffer(buf);
switch (commState.idx) {
case 0:
{
      UUID subjId0=commState.getUuid();
      if (subjId0 == GridTcpCommunicationMessageAdapter.UUID_NOT_READ)       return false;
      commState.idx++;
    }
}
return true;
}
