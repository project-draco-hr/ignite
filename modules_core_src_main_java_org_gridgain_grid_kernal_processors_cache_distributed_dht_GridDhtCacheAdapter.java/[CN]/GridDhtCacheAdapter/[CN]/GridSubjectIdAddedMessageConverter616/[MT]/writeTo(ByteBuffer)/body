{
  commState.setBuffer(buf);
switch (commState.idx) {
case 0:
{
      if (!commState.putUuid(GridTcpCommunicationMessageAdapter.UUID_NOT_READ))       return false;
      commState.idx++;
    }
}
return true;
}
