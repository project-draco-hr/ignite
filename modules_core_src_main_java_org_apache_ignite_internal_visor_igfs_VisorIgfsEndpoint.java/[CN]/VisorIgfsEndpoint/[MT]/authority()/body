{
  String addr=hostName + ":" + port;
  if (igfsName == null && gridName == null)   return addr;
 else   if (igfsName == null)   return gridName + "@" + addr;
 else   if (gridName == null)   return igfsName + "@" + addr;
 else   return igfsName + ":" + gridName+ "@"+ addr;
}
