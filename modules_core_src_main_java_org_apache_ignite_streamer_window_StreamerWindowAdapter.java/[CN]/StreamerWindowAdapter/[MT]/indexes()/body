{
  if (idxs != null) {
    Collection<StreamerIndex<E,?,?>> res=new ArrayList<>(idxs.length);
    for (    StreamerIndexProvider<E,?,?> idx : idxs)     res.add(idx.index());
    return res;
  }
 else   return Collections.emptyList();
}
