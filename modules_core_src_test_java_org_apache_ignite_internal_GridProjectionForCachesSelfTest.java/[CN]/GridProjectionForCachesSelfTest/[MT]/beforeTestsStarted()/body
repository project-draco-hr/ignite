{
  for (int i=0; i < 5; i++)   startGrid(i);
  NearCacheConfiguration nearCfg=new NearCacheConfiguration();
  nearCfg.setName(CACHE_NAME);
  grid(1).createCache(nearCfg);
  grid(2).jcache(null);
  grid(3).jcache(null);
}
