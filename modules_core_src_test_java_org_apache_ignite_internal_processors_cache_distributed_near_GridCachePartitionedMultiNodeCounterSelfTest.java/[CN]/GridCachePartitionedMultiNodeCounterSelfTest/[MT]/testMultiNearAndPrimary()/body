{
  backups=1;
  int gridCnt=4;
  int priThreads=2;
  int nearThreads=2;
  startGrids(gridCnt);
  try {
    checkNearAndPrimary(gridCnt,priThreads,nearThreads);
  }
  finally {
    stopAllGrids();
  }
}
