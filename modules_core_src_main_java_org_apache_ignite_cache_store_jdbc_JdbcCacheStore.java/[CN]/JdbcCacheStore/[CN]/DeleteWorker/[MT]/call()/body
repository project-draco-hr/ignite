{
  PreparedStatement stmt=null;
  try {
    stmt=conn.prepareStatement(m.remQry);
    for (    Object key : keys) {
      fillKeyParameters(stmt,m,key);
      stmt.addBatch();
    }
    stmt.executeBatch();
  }
  finally {
    U.closeQuiet(stmt);
  }
  return null;
}
