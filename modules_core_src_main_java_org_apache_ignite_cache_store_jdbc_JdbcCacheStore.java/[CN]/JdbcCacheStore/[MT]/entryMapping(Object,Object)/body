{
  String cacheName=session().cacheName();
  EntryMapping em=cacheMappings(cacheName).get(keyTypeId);
  if (em == null)   throw new CacheException("Failed to find mapping description for key: " + key + " in cache: "+ (cacheName != null ? cacheName : "<default>"));
  return em;
}
