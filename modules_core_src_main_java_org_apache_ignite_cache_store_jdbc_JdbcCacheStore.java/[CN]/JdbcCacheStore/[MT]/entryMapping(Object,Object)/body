{
  EntryMapping em=entryMappings.get(new IgniteBiTuple<>(session().cacheName(),keyId));
  if (em == null)   throw new CacheException("Failed to find mapping description for key: " + key);
  return em;
}
