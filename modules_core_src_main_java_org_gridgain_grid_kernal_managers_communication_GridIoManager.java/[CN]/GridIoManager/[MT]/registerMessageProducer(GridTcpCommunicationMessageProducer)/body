{
  int nextMsg=++pluginMsg;
  if (nextMsg > Byte.MAX_VALUE)   throw new IgniteException();
  if (pluginMsgs == null)   pluginMsgs=new HashMap<>();
  pluginMsgs.put((byte)nextMsg,producer);
  return (byte)nextMsg;
}
