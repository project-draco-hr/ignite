{
  super.prepareMarshal(ctx);
  if (missedKeys != null && missedKeyBytes == null)   missedKeyBytes=marshalCollection(missedKeys,ctx);
  if (infos != null) {
    marshalInfos(infos,ctx);
    infosBytes=ctx.marshaller().marshal(infos);
  }
}
