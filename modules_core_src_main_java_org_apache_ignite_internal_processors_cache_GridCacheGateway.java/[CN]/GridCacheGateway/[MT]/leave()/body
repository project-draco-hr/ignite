{
  try {
    ctx.tm().resetContext();
    ctx.mvcc().contextReset();
    if (!ctx.shared().closed(ctx))     CU.unwindEvicts(ctx);
  }
  finally {
    ctx.kernalContext().gateway().readUnlock();
  }
}
