{
  for (int i=0; i < dst.length; i++) {
    Message msg=src.next();
    if (msg instanceof GridH2ValueMessage)     dst[i]=((GridH2ValueMessage)msg).value();
 else     dst[i]=new GridH2ValueCacheObject(coctx,(CacheObject)msg);
  }
  return dst;
}
