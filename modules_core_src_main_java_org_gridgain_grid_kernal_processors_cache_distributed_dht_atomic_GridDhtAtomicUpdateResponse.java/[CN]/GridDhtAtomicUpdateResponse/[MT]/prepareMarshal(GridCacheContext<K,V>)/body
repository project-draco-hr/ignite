{
  super.prepareMarshal(ctx);
  failedKeysBytes=ctx.marshaller().marshal(failedKeys);
  errBytes=ctx.marshaller().marshal(err);
  if (nearEvictedBytes == null)   nearEvictedBytes=marshalCollection(nearEvicted,ctx);
}
