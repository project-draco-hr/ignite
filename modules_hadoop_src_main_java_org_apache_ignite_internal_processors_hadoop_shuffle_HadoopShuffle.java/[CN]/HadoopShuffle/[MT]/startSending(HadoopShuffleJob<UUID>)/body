{
  shuffleJob.startSending(ctx.kernalContext().gridName(),new IgniteInClosure2X<UUID,HadoopShuffleMessage>(){
    @Override public void applyx(    UUID dest,    HadoopShuffleMessage msg) throws IgniteCheckedException {
      send0(dest,msg);
    }
  }
);
}
