{
  if (locPhase == GridHadoopJobPhase.PHASE_REDUCE) {
    if (reducers == null || pendingReducers.get() == 0) {
      if (log.isDebugEnabled())       log.debug("Finished running all local tasks, shutting down the process.");
      shutdown();
      terminate();
    }
  }
}
