{
  if (!ctx.isNear() && !ctx.isReplicated() && modes.contains(NEAR_ONLY))   return null;
  V val=null;
  if (!modes.contains(PARTITIONED_ONLY))   val=super.peek(modes);
  if (val == null)   val=peekDht0(modes,CU.<K,V>empty());
  return val;
}
