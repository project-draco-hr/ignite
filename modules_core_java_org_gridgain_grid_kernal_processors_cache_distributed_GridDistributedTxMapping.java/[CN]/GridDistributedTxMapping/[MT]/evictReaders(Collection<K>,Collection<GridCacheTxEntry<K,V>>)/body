{
  if (entries == null || entries.isEmpty())   return;
  for (Iterator<GridCacheTxEntry<K,V>> it=entries.iterator(); it.hasNext(); ) {
    GridCacheTxEntry<K,V> entry=it.next();
    if (keys.contains(entry.key()))     it.remove();
  }
}
