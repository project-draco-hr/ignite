{
  GridCachePartitionedDistributionMode mode=configuration().getPartitionedDistributionMode();
  return (mode == PARTITIONED_ONLY || mode == NEAR_PARTITIONED) ? super.splitClearAll() : Collections.<GridCacheClearAllRunnable<K,V>>emptyList();
}
