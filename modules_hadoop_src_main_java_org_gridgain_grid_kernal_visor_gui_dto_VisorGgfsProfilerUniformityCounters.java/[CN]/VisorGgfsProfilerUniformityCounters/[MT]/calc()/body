{
  if (!counters.isEmpty()) {
    int cap=(int)(fileSize / blockSize + (fileSize % blockSize > 0 ? 1 : 0));
    capacity(cap);
    int sz=counters.size();
    int n=0;
    double mean=1.0 / sz;
    double sigma=0;
    return 1.0 - sigma;
  }
 else   return -1;
}
