{
  try {
    Path path=new Path(block.file());
    FileSystem fs=path.getFileSystem(((GridHadoopDefaultJobInfo)job.info()).configuration());
    if (fs instanceof GridGgfsHadoopFileSystem)     return grid.ggfs("ggfs");
    return null;
  }
 catch (  IOException ignored) {
    return null;
  }
}
