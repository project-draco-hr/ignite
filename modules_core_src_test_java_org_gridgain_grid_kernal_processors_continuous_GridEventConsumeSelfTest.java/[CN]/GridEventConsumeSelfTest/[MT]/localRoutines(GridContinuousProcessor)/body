{
  return F.view(U.<Map<UUID,LocalRoutineInfo>>field(proc,"locInfos").values(),new GridPredicate<LocalRoutineInfo>(){
    @Override public boolean apply(    LocalRoutineInfo info){
      return info.handler().isForEvents();
    }
  }
);
}
