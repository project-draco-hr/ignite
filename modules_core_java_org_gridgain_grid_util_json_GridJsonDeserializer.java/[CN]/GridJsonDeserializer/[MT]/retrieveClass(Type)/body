{
  Class cls;
  if (type == null)   cls=null;
 else   if (type instanceof Class)   cls=(Class)type;
 else   if (type instanceof ParameterizedType)   cls=(Class)((ParameterizedType)type).getRawType();
 else   throw new GridException("Can't determine class from type: " + type);
  return cls;
}
