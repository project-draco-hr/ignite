{
  if (subjId == null)   return this;
  guard();
  try {
    return ids != null ? new ClusterGroupAdapter(this,ctx,subjId,ids) : new ClusterGroupAdapter(this,ctx,subjId,p);
  }
  finally {
    unguard();
  }
}
