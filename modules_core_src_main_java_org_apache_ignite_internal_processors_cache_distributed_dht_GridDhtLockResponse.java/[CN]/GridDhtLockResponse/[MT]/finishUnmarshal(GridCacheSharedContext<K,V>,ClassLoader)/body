{
  super.finishUnmarshal(ctx,ldr);
  if (nearEvicted == null && nearEvictedBytes != null)   nearEvicted=unmarshalCollection(nearEvictedBytes,ctx,ldr);
  if (preloadEntries == null && preloadEntriesBytes != null)   preloadEntries=unmarshalCollection(preloadEntriesBytes,ctx,ldr);
  if (preloadEntries == null && preloadEntriesBytes != null) {
    preloadEntries=unmarshalCollection(preloadEntriesBytes,ctx,ldr);
    unmarshalInfos(preloadEntries,ctx.cacheContext(cacheId),ldr);
  }
}
