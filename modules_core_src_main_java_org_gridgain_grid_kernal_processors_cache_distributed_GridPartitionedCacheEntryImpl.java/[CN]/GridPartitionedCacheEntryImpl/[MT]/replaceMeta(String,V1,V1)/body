{
  boolean b=false;
  GridDhtCacheEntry<K,V> de=dht().peekExx(key);
  if (de != null)   b=de.replaceMeta(name,curVal,newVal);
  GridNearCacheEntry<K,V> ne=near().peekExx(key);
  if (ne != null)   b|=ne.replaceMeta(name,curVal,newVal);
  return b;
}
