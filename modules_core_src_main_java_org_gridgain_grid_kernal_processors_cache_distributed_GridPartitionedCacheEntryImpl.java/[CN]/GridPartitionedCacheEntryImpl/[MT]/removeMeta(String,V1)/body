{
  boolean b=false;
  GridDhtCacheEntry<K,V> de=dht().peekExx(key);
  if (de != null)   b=de.removeMeta(name,val);
  GridNearCacheEntry<K,V> ne=near().peekExx(key);
  if (ne != null)   b|=ne.removeMeta(name,val);
  return b;
}
