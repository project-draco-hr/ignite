{
  int len=idxs.size();
  Object[] snapshot=new ConcurrentNavigableMap[len - 1];
  for (int i=1; i < len; i++) {
    Object s=index(i).takeSnapshot(null);
    snapshot[i - 1]=s;
  }
  return snapshot;
}
