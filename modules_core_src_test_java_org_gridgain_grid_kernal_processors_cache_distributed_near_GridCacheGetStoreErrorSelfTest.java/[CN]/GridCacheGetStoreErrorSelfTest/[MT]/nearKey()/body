{
  String key="";
  for (int i=0; i < 1000; i++) {
    key=String.valueOf(i);
    GridCacheEntry<Object,Object> entry=grid(0).cache(null).entry(key);
    if (!entry.primary() && entry.backup())     break;
  }
  return key;
}
