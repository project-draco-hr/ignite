{
  short flags=in.readShortPositioned(start + GridPortableMarshaller.FLAGS_POS);
  if (hasSchema(flags))   return in.readIntPositioned(start + GridPortableMarshaller.SCHEMA_OR_RAW_OFF_POS);
 else   return length(in,start);
}
