{
  int footerStart=footerStartRelative(in,start);
  int footerEnd=length(in,start);
  if ((footerEnd - footerStart) % (4 + fieldOffsetSize) != 0)   footerEnd-=4;
  return F.t(start + footerStart,start + footerEnd);
}
