{
  int typeId=in.readInt();
  if (typeId != UNREGISTERED_TYPE_ID)   return new EnumType(typeId,null);
 else {
    String clsName=doReadClassName(in);
    return new EnumType(UNREGISTERED_TYPE_ID,clsName);
  }
}
