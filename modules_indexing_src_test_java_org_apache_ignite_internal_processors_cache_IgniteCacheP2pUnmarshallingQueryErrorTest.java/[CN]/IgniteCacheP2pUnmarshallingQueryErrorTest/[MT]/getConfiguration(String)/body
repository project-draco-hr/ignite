{
  IgniteConfiguration cfg=super.getConfiguration(gridName);
  if (cfg.getCacheConfiguration().length > 0)   cfg.getCacheConfiguration()[0].setIndexedTypes(TestKey.class,String.class);
  return cfg;
}
