{
  maxExecs=arg;
  t.start();
  if (g.cluster().nodes().size() == 1)   sendJob(g.cluster().localNode());
 else   for (  ClusterNode n : g.cluster().forRemotes().nodes())   sendJob(n);
  return null;
}
