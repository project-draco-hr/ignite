{
  A.notNull(cls,"cls");
  return new CO<T>(){
    @Override public T apply(){
      try {
        return cls.newInstance();
      }
 catch (      Exception e) {
        throw new IgniteException(e);
      }
    }
  }
;
}
