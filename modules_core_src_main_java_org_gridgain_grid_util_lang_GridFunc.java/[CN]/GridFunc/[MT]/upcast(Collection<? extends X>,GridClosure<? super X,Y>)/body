{
  A.notNull(c,"c",f,"f");
  if (c instanceof List) {
    for (ListIterator<X> iter=((List<X>)c).listIterator(); iter.hasNext(); )     iter.set(f.apply(iter.next()));
    return (Collection<Y>)c;
  }
 else   return viewReadOnly(c,f);
}
