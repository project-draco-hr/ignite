{
  A.notNull(c,"c",pdc,"pdc");
  A.ensure(cnt > 0,"cnt > 0");
  Collection<GridOutClosure<R>> ret=new ArrayList<>(cnt);
  for (int i=0; i < cnt; i++)   ret.add(curry(c,pdc.apply()));
  return ret;
}
