{
  A.notNull(m,"m");
  if (!isAlwaysFalse(p)) {
    if (f != null) {
      for (      Map.Entry<? extends K,? extends V> e : m.entrySet()) {
        GridBiTuple<K,V> t=F.t(e.getKey(),e.getValue());
        if (isAll(t,p))         f.apply(t);
      }
    }
  }
}
