{
  A.notNull(c,"c");
  int size=c.size();
  if (size == 0)   return null;
  int n=ThreadLocalRandom8.current().nextInt(size);
  int i=0;
  T res=null;
  for (  T t : c) {
    if (i++ == n)     return t;
    res=t;
  }
  return res;
}
