{
  if (qPlc != SINGLE_Q)   throw new IllegalStateException("Iterators are not supported in 'perSegmentQueue' modes.");
  modCnt=ConcurrentLinkedHashMap.this.modCnt.intValue();
  delegate=asc ? entryQ.iterator() : entryQ.descendingIterator();
  advance();
}
