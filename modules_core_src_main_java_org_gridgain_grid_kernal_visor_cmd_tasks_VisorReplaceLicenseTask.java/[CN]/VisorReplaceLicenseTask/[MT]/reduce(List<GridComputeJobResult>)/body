{
  Collection<VisorReplaceLicenseResult> res=new ArrayList<>(results.size());
  for (  GridComputeJobResult r : results) {
    if (r.getData() instanceof VisorReplaceLicenseState)     res.add(new VisorReplaceLicenseResult(r.getNode().id(),(VisorReplaceLicenseState)r.getData()));
 else     res.add(new VisorReplaceLicenseResult(r.getNode().id(),VisorReplaceLicenseState.FAILED));
  }
  return res;
}
