{
  UUID key=primaryKeyForCache(grid(0));
  cache(0).put(key,"val");
  try (IgniteTx ignored=cache(0).txStartPartition(cache(0).affinity().partition(key),concurrency,REPEATABLE_READ,0,1)){
    assertEquals("val",cache(0).get(key));
  }
 }
