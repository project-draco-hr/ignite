{
  try {
    startGridsMultiThreaded(2);
    Collection<String> names=grid(0).cacheNames();
    assertEquals(2,names.size());
    for (    String name : names)     assertTrue(name.equals(CACHE_NAME_1) || name.equals(CACHE_NAME_2));
  }
  finally {
    stopAllGrids();
  }
}
