{
  MultiMap<String> paramsMap=req.getParameters();
  Collection<NameValuePair> params=new ArrayList<>(paramsMap.size());
  for (  Map.Entry e : paramsMap.entrySet())   params.add(new BasicNameValuePair((String)e.getKey(),((List<String>)e.getValue()).get(0)));
  return params;
}
