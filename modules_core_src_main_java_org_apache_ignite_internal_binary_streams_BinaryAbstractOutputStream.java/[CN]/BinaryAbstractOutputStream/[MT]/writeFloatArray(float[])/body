{
  int cnt=val.length << 2;
  ensureCapacity(pos + cnt);
  if (LITTLE_ENDIAN)   copyAndShift(val,FLOAT_ARR_OFF,cnt);
 else {
    for (    float item : val) {
      writeIntFast(Integer.reverseBytes(Float.floatToIntBits(item)));
      shift(4);
    }
  }
}
