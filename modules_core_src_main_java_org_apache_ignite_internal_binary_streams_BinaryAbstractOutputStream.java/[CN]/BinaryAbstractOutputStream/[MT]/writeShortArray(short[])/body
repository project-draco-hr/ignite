{
  int cnt=val.length << 1;
  ensureCapacity(pos + cnt);
  if (BIG_ENDIAN) {
    for (    short item : val) {
      writeShortFast(item);
      shift(2);
    }
  }
 else   copyAndShift(val,GridUnsafe.SHORT_ARR_OFF,cnt);
}
