{
  int cnt=val.length << 2;
  ensureCapacity(pos + cnt);
  if (BIG_ENDIAN) {
    for (    int item : val) {
      writeIntFast(item);
      shift(4);
    }
  }
 else   copyAndShift(val,GridUnsafe.INT_ARR_OFF,cnt);
}
