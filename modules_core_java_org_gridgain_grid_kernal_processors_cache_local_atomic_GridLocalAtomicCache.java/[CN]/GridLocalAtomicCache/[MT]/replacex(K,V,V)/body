{
  A.notNull(key,"key");
  ctx.denyOnLocalRead();
  return (GridCacheReturn<V>)updateAllInternal(UPDATE,Collections.singleton(key),Collections.singleton(newVal),0,true,true,ctx.equalsPeekArray(oldVal),ctx.isStoreEnabled());
}
