{
  return withReconnectHandling(new ClientProjectionClosure<List<String>>(){
    @Override public GridClientFuture<List<String>> apply(    GridClientConnection conn,    UUID destNodeId) throws GridClientConnectionResetException, GridClientClosedException {
      return conn.log(path,lineFrom,lineTo,destNodeId);
    }
  }
);
}
