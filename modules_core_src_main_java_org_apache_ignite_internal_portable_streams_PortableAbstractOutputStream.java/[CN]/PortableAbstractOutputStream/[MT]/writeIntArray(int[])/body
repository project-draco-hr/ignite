{
  int cnt=val.length << 2;
  ensureCapacity(pos + cnt);
  if (LITTLE_ENDIAN)   copyAndShift(val,INT_ARR_OFF,cnt);
 else {
    for (    int item : val)     writeIntFast(Integer.reverseBytes(item));
    shift(cnt);
  }
}
