{
  int cnt=val.length << 3;
  ensureCapacity(pos + cnt);
  if (LITTLE_ENDIAN)   copyAndShift(val,LONG_ARR_OFF,cnt);
 else {
    for (    long item : val)     writeLongFast(Long.reverseBytes(item));
    shift(cnt);
  }
}
