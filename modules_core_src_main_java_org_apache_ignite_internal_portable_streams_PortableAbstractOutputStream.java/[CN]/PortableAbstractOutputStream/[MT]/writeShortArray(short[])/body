{
  int cnt=val.length << 1;
  ensureCapacity(pos + cnt);
  if (LITTLE_ENDIAN)   copyAndShift(val,SHORT_ARR_OFF,cnt);
 else {
    for (    short item : val)     writeShortFast(Short.reverseBytes(item));
    shift(cnt);
  }
}
