{
  int cnt=val.length << 1;
  ensureCapacity(pos + cnt);
  if (LITTLE_ENDIAN)   copyAndShift(val,CHAR_ARR_OFF,cnt);
 else {
    for (    char item : val)     writeCharFast(Character.reverseBytes(item));
    shift(cnt);
  }
}
