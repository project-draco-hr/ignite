{
  int cnt=val.length << 3;
  ensureCapacity(pos + cnt);
  if (LITTLE_ENDIAN)   copyAndShift(val,DOUBLE_ARR_OFF,cnt);
 else {
    for (    double item : val) {
      writeLongFast(Long.reverseBytes(Double.doubleToLongBits(item)));
      shift(8);
    }
  }
}
