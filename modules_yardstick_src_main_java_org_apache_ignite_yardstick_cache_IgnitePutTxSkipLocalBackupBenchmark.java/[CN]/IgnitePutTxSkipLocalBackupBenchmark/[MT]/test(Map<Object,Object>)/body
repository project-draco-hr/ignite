{
  int key;
  Affinity<Object> aff=ignite().affinity("tx");
  ClusterNode locNode=ignite().cluster().localNode();
  for (; ; ) {
    key=nextRandom(args.range());
    if (!aff.isBackup(locNode,key))     break;
  }
  cache.put(key,new SampleValue(key));
  return true;
}
