{
synchronized (mux) {
    if (!F.eq(metrics.clause(),clause) || !F.eq(metrics.className(),clsName))     metrics=new GridCacheQueryMetricsAdapter(new GridCacheQueryMetricsKey(type,clsName,clause));
  }
}
