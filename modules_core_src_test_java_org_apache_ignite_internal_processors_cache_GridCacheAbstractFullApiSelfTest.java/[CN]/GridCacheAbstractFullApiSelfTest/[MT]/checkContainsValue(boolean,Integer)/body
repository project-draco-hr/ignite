{
  if (nearEnabled())   assertEquals(exp,cache().containsValue(val));
 else {
    boolean contains=false;
    for (int i=0; i < gridCount(); i++)     if (containsValue(cache(i),val)) {
      contains=true;
      break;
    }
    assertEquals("Value: " + val,exp,contains);
  }
}
