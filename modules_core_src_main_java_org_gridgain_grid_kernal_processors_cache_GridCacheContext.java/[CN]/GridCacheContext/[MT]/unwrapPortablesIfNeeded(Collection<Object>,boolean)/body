{
  if (!config().isPortableEnabled())   return col;
  Collection<Object> unwrapped=new ArrayList<>(col.size());
  for (  Object o : col) {
    unwrapped.add(unwrapPortableIfNeeded(o,keepPortable));
  }
  return unwrapped;
}
