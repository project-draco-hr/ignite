{
  if (subjId != null)   return subjId;
  GridCacheProjectionImpl<K,V> prj=projectionPerCall();
  if (prj != null)   subjId=prj.subjectId();
  if (subjId == null)   subjId=ctx.localNodeId();
  return subjId;
}
