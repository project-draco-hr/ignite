{
  super.onKernalStart0();
  if (plcEnabled && evictSync && (cctx.isDht() || cctx.isColocated())) {
    backupWorker.addEvent(new GridDiscoveryEvent(cctx.localNodeId(),cctx.localNode(),"Dummy event.",EVT_NODE_JOINED,cctx.localNodeId()));
    backupWorkerThread=new GridThread(backupWorker);
    backupWorkerThread.start();
  }
}
