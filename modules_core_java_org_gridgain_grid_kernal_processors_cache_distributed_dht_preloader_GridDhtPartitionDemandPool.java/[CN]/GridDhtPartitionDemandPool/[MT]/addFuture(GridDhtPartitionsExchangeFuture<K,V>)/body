{
  if (fut.onAdded()) {
    exchWorker.addFuture(fut);
synchronized (dmdWorkers) {
      for (      DemandWorker w : dmdWorkers)       w.addMessage(DUMMY_TOP);
    }
    return true;
  }
  return false;
}
