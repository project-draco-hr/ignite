{
  String usr=UserGroupInformation.getCurrentUser().getShortUserName();
  return new Path(conf.get(MRJobConfig.MR_AM_STAGING_DIR,MRJobConfig.DEFAULT_MR_AM_STAGING_DIR) + Path.SEPARATOR + usr+ Path.SEPARATOR+ ".staging").toString();
}
