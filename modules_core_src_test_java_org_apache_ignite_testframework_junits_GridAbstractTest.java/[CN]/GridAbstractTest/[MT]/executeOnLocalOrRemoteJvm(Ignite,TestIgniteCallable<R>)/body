{
  if (!isMultiJvmObject(ignite))   try {
    return job.call(ignite);
  }
 catch (  Exception e) {
    throw new IgniteException(e);
  }
 else   return executeRemotely((IgniteProcessProxy)ignite,job);
}
