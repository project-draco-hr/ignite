{
  writeFieldName(fieldName);
  byte[] arr=null;
  int len=0;
  if (val != null) {
    arr=val.getBytes(UTF_8);
    len+=arr.length;
  }
  doWriteInt(5 + len);
  doWriteByte(STRING);
  doWriteByteArray(arr);
}
