{
  if (nodes != null) {
    for (    ClusterNode n : nodes) {
      List<T2<K,byte[]>> keys=map.get(n);
      if (keys == null)       map.put(n,keys=new LinkedList<>());
      keys.add(new T2<>(entry.key(),entry.getOrMarshalKeyBytes()));
    }
  }
}
