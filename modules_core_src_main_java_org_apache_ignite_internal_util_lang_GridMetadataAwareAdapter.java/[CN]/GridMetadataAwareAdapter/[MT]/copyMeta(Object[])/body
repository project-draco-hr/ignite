{
  A.notNull(data,"data");
synchronized (mux) {
    if (this.data.length < data.length)     this.data=Arrays.copyOf(this.data,data.length);
    for (int k=0; k < data.length; k++) {
      if (data[k] != null)       this.data[k]=data[k];
    }
  }
}
