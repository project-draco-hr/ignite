{
  A.notNull(name,"name",newVal,"newVal",curVal,"curVal");
synchronized (mux) {
    if (hasMeta(name)) {
      V val=this.<V>meta(name);
      if (val != null && val.equals(curVal)) {
        addMeta(name,newVal);
        return true;
      }
    }
    return false;
  }
}
