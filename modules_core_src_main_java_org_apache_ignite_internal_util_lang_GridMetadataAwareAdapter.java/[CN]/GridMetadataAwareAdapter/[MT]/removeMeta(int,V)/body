{
  A.notNull(key,"key",val,"val");
synchronized (mux) {
    if (data == null || data.length <= key)     return false;
    V old=(V)data[key];
    if (old != null && old.equals(val)) {
      data[key]=null;
      return true;
    }
    return false;
  }
}
