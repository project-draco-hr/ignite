{
  A.notNull(name,"name",c,"c");
synchronized (mux) {
    V v=(V)meta(name);
    if (v == null && c != null)     try {
      addMeta(name,v=c.call());
    }
 catch (    Exception e) {
      throw F.wrap(e);
    }
    return v;
  }
}
