{
  A.notNull(name,"name",val,"val");
synchronized (mux) {
    if (data == null)     return false;
    V old=(V)data.get(name);
    if (old != null && old.equals(val)) {
      data.remove(name);
      return true;
    }
    return false;
  }
}
