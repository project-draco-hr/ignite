{
  A.notNull(key,"key",newVal,"newVal",curVal,"curVal");
synchronized (mux) {
    if (hasMeta(key)) {
      V val=this.<V>meta(key);
      if (val != null && val.equals(curVal)) {
        addMeta(key,newVal);
        return true;
      }
    }
    return false;
  }
}
