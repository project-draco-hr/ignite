{
  if (!cctx.isDht() || !isNearEnabled(cctx) || cctx.localNodeId().equals(nearNodeId))   return false;
  if (cctx.config().getBackups() == 0)   return true;
  return !cctx.affinity().backups(key,topVer).contains(cctx.localNode());
}
