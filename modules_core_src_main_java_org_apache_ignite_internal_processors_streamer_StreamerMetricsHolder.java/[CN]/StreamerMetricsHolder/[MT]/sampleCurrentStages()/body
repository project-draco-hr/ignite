{
  long now=U.currentTimeMillis();
  int cur=(int)stageActiveCnt.sum();
  if (now - lastStageSampleTs > 1000) {
    stageActiveMaxLastSec.set(cur);
    lastStageSampleTs=now;
  }
 else   stageActiveMaxLastSec.setIfGreater(cur);
}
