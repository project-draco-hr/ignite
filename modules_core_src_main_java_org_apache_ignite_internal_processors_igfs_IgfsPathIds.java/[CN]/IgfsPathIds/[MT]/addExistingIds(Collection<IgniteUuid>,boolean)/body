{
  if (relaxed) {
    col.add(ids[lastExistingIdx]);
    if (lastExistingIdx == ids.length - 1 && lastExistingIdx > 0)     col.add(ids[lastExistingIdx - 1]);
  }
 else {
    for (int i=0; i <= lastExistingIdx; i++)     col.add(ids[i]);
  }
}
