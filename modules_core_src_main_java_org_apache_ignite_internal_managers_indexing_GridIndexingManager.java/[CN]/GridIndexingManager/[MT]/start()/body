{
  if (ctx.config().isDaemon())   return;
  if (!enabled())   U.warn(log,"Indexing is disabled (to enable please configure GridIndexingSpi).");
  startSpi();
  if (log.isDebugEnabled())   log.debug(startInfo());
}
