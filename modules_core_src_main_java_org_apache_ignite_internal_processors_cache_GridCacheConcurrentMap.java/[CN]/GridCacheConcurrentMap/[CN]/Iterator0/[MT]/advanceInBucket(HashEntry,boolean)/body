{
  if (e == null)   return false;
  nextEntry=e;
  do {
    if (!skipFirst) {
      next=nextEntry;
      if (!next.mapEntry.visitable(filter))       continue;
      if (isVal) {
        nextVal=next.mapEntry.<K,V>wrap().getValue();
        if (nextVal == null)         continue;
      }
      return true;
    }
    skipFirst=false;
  }
 while ((nextEntry=nextEntry.next) != null);
  next=null;
  nextVal=null;
  return false;
}
