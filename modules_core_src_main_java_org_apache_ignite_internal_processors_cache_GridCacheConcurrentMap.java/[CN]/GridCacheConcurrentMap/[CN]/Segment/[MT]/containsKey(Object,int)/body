{
  if (size != 0) {
    HashEntry e=getFirst(tbl,hash);
    while (e != null) {
      if (e.mapEntry.hash() == hash && key.equals(e.mapEntry.key()))       return true;
      e=e.next;
    }
  }
  return false;
}
