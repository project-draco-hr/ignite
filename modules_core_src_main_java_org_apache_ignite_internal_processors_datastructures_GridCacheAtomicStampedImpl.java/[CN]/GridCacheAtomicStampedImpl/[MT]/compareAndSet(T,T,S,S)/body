{
  checkRemoved();
  try {
    return CU.outTx(internalCompareAndSet(F.equalTo(expVal),wrapperClosure(newVal),F.equalTo(expStamp),wrapperClosure(newStamp)),ctx);
  }
 catch (  IgniteCheckedException e) {
    throw U.convertException(e);
  }
}
