{
  Collection<Counter> grpCounters=new ArrayList<>();
  for (  GridHadoopLongCounter counter : userCounters) {
    if (grpName.equals(counter.group()))     grpCounters.add(new GridHadoopV2Counter(counter));
  }
  return grpCounters.iterator();
}
