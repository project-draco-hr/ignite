{
  Set<Counter> groupCounters=new HashSet<>();
  for (  GridHadoopCounter counter : counters.all()) {
    if (groupName.equals(counter.group()))     groupCounters.add(new GridHadoopV2Counter(counter));
  }
  return groupCounters.iterator();
}
