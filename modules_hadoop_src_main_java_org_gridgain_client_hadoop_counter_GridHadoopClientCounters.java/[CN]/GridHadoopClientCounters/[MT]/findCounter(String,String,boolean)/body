{
  if (create)   return new GridHadoopV2Counter(counters.counter(groupName,counterName));
  for (  GridHadoopCounter counter : counters.all()) {
    if (groupName.equals(counter.group()) && counterName.equals(counter.name()))     return new GridHadoopV2Counter(counter);
  }
  return null;
}
