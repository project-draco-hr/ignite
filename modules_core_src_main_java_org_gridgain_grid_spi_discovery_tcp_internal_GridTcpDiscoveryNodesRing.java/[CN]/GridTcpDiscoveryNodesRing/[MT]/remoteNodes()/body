{
  rwLock.readLock().lock();
  try {
    return Collections.unmodifiableCollection(new ArrayList<>(F.view(nodes,F.<GridTcpDiscoveryNode>remoteNodes(locNode.id()))));
  }
  finally {
    rwLock.readLock().unlock();
  }
}
