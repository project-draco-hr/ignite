{
  rwLock.readLock().lock();
  try {
    return Collections.unmodifiableCollection(F.view(nodesMap.values(),F.<GridTcpDiscoveryNode>remoteNodes(locNode.id())));
  }
  finally {
    rwLock.readLock().unlock();
  }
}
