{
  GridCacheProjection<String,Integer> res=cache;
  if (filters != null) {
    for (    GridPredicate<GridCacheEntry<String,Integer>> filter : filters)     res=res.projection(filter);
  }
  return res;
}
