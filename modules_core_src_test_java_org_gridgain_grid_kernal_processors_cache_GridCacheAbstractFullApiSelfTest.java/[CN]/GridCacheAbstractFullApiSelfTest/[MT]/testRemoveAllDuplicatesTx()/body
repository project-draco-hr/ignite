{
  if (txEnabled()) {
    GridCacheTx tx=cache().txStart();
    try {
      cache().removeAll(Arrays.asList("key1","key1","key1"));
      tx.commit();
    }
  finally {
      tx.close();
    }
  }
}
