{
  IgniteCache<String,Integer> cache=jcache(0);
  for (int i=0; i < 100; ++i)   cache.put(Integer.toString(i),i);
  checkIteratorCacheSize(cache,100);
  removeCacheIterator(cache);
  checkIteratorCacheSize(cache,100 - 1);
}
