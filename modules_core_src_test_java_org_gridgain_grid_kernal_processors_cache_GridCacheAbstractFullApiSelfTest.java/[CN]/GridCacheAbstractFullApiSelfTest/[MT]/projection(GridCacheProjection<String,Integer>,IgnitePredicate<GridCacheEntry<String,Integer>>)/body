{
  GridCacheProjection<String,Integer> res=cache;
  if (filters != null) {
    for (    IgnitePredicate<GridCacheEntry<String,Integer>> filter : filters)     res=res.projection(filter);
  }
  return res;
}
