{
  if (txEnabled()) {
    GridCacheTx tx=cache().txStart(PESSIMISTIC,REPEATABLE_READ);
    try {
      cache().getAll(F.asList("key1","key1","key1"));
    }
  finally {
      tx.close();
    }
  }
}
