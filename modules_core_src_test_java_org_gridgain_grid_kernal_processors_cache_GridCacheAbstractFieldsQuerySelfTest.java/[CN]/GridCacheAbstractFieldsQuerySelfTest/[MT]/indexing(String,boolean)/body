{
  GridH2IndexingSpi spi=new GridH2IndexingSpi();
  if (name != null)   spi.setName(name);
  spi.setDefaultIndexPrimitiveKey(primitives);
  spi.setDefaultIndexPrimitiveValue(primitives);
  return spi;
}
