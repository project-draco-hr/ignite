{
  try {
    message(grid.cluster().forNodes(nodes)).send(null,new GridTestCommunicationMessage(cntr,grid.cluster().localNode().id()));
  }
 catch (  GridException e) {
    error("Failed to send message.",e);
  }
}
