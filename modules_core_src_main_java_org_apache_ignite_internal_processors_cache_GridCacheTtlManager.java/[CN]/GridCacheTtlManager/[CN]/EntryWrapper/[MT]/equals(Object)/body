{
  if (this == o)   return true;
  if (!(o instanceof EntryWrapper))   return false;
  EntryWrapper that=(EntryWrapper)o;
  return expireTime == that.expireTime && compareKeys(entry.context(),entry.key(),that.entry.context(),that.entry.key()) == 0;
}
