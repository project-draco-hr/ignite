{
  if (expireTime == o.expireTime) {
    if (entry.startVersion() == o.entry.startVersion())     return 0;
    return entry.startVersion() < o.entry.startVersion() ? -1 : 1;
  }
 else   return expireTime < o.expireTime ? -1 : 1;
}
