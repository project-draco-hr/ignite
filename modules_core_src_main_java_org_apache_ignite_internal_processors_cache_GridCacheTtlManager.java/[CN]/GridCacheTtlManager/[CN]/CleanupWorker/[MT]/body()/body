{
  while (!isCancelled()) {
    expire();
    EntryWrapper first=pendingEntries.firstx();
    if (first != null) {
      long waitTime=first.expireTime - U.currentTimeMillis();
      if (waitTime > 0)       U.sleep(waitTime);
    }
 else     U.sleep(500);
  }
}
