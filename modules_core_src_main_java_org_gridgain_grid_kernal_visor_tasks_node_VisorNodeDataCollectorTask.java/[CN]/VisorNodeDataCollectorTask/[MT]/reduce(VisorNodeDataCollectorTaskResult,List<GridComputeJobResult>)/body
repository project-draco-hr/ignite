{
  for (  GridComputeJobResult res : results) {
    VisorNodeDataCollectorJobResult jobResult=res.getData();
    if (jobResult != null) {
      UUID nid=res.getNode().id();
      GridException unhandledEx=res.getException();
      if (unhandledEx == null)       reduceJobResult(taskResult,jobResult,nid);
 else {
        if (!(unhandledEx instanceof GridEmptyProjectionException))         taskResult.unhandledEx().put(nid,unhandledEx);
      }
    }
  }
  return taskResult;
}
