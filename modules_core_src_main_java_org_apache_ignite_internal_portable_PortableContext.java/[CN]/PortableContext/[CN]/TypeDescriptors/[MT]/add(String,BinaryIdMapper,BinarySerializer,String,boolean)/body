{
  TypeDescriptor desc=new TypeDescriptor(clsName,idMapper,serializer,affKeyFieldName,canOverride);
  TypeDescriptor oldDesc=descs.get(clsName);
  if (oldDesc == null)   descs.put(clsName,desc);
 else   oldDesc.override(desc);
}
