{
  PortableIdMapper idMapper=mappers.get(typeId);
  if (idMapper != null)   return idMapper;
  if (userTypes.containsKey(typeId) || predefinedTypes.containsKey(typeId))   return DFLT_ID_MAPPER;
  return BASIC_CLS_ID_MAPPER;
}
