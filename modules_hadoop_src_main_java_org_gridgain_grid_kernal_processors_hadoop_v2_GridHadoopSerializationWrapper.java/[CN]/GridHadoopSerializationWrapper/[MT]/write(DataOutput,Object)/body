{
  try {
    boolean isFirstWrite=(currOut == null);
    currOut=out;
    if (isFirstWrite)     serializer.open(outStream);
    serializer.serialize((T)obj);
  }
 catch (  IOException e) {
    throw new GridException(e);
  }
}
