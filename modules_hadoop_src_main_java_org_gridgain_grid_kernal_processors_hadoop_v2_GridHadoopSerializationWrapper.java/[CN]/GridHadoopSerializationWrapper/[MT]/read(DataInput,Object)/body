{
  try {
    boolean isFirstRead=(currIn == null);
    currIn=in;
    if (isFirstRead)     deserializer.open(inStream);
    return deserializer.deserialize((T)obj);
  }
 catch (  IOException e) {
    throw new GridException(e);
  }
}
